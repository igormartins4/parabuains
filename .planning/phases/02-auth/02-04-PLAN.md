---
phase: 02-auth
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(auth)/register/page.tsx
  - apps/web/app/(auth)/forgot-password/page.tsx
  - apps/web/app/(auth)/reset-password/page.tsx
  - apps/web/app/(auth)/verify-2fa/page.tsx
  - apps/web/app/(auth)/layout.tsx
  - apps/web/middleware.ts
  - apps/web/lib/service-token.ts
  - apps/web/lib/api-client.ts
  - apps/web/lib/account-lockout.ts
  - apps/api/src/plugins/auth.ts
autonomous: true
requirements: [AUTH-08]

must_haves:
  truths:
    - "Paginas de auth criadas: login, register, forgot-password, reset-password, verify-2fa"
    - "Honeypot field 'website' em todos os formularios de auth"
    - "Account lockout: 10 tentativas em 15min bloqueia 30min (Redis counter)"
    - "BFF service token: JWT HS256 60s assinado com API_JWT_SECRET"
    - "Fastify preHandler hook verifica service token em todas as rotas protegidas"
  artifacts:
    - path: "apps/web/lib/service-token.ts"
      provides: "Funcao para gerar JWT service token BFF para Fastify"
      contains: "TOKEN_TTL_SECONDS = 60"
    - path: "apps/api/src/plugins/auth.ts"
      provides: "Fastify preHandler hook que verifica service token"
      contains: "fastify.addHook('preHandler'"
    - path: "apps/web/app/(auth)/login/page.tsx"
      provides: "Pagina de login com honeypot e account lockout UI"
      contains: "name=\"website\""
  key_links:
    - from: "apps/web/lib/api-client.ts"
      to: "apps/api (Fastify)"
      via: "Authorization: Bearer <serviceToken>"
      pattern: "Authorization: `Bearer ${serviceToken}`"
    - from: "apps/api/src/plugins/auth.ts"
      to: "apps/web/lib/service-token.ts"
      via: "JWT verificado com API_JWT_SECRET compartilhado"
      pattern: "request.jwtVerify()"
---

<objective>
Criar todas as paginas de auth (login, register, forgot-password, reset-password, verify-2fa), implementar honeypot fields, account lockout com Redis (10 tentativas/15min bloqueio 30min), BFF service token flow (JWT 60s para Fastify) e o preHandler hook no Fastify para verificar tokens.

Output: Fluxo completo de autenticacao no browser com 5 paginas + camada de seguranca BFF-API funcionando.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth/02-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth layout + paginas login e register</name>
  <files>apps/web/app/(auth)/layout.tsx, apps/web/app/(auth)/login/page.tsx, apps/web/app/(auth)/register/page.tsx</files>
  <read_first>
    - apps/web/lib/auth-client.ts (signIn, signUp exports)
    - .planning/phases/02-auth/02-CONTEXT.md (D-09: honeypot field website, D-11: password rules)
    - apps/web/components/auth/GoogleSignInButton.tsx
  </read_first>
  <action>
Criar apps/web/app/(auth)/layout.tsx — layout centralizado para todas as paginas de auth:
```typescript
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen items-center justify-center bg-gray-50 px-4">
      <div className="w-full max-w-sm">
        <div className="mb-8 text-center">
          <h1 className="text-2xl font-bold text-gray-900">Parabuains</h1>
          <p className="mt-1 text-sm text-gray-500">Aniversarios que importam</p>
        </div>
        {children}
      </div>
    </div>
  );
}
```

Criar apps/web/app/(auth)/login/page.tsx com:
- Campo honeypot: input type="text" name="website" com style position absolute fora da tela, tabIndex=-1, aria-hidden=true
- Se honeypot preenchido: simular sucesso (await 800ms + redirect) sem processar auth
- Formulario: email, password com autoComplete correto
- Botao GoogleSignInButton importado de @/components/auth/GoogleSignInButton
- Link para /register e /forgot-password
- Funcao getAuthErrorMessage mapeando codigos de erro (ACCOUNT_LOCKED, INVALID_EMAIL_OR_PASSWORD, EMAIL_NOT_VERIFIED)
- Erro exibido em role="alert" com estilo vermelho

Criar apps/web/app/(auth)/register/page.tsx com:
- Campo honeypot identico ao login
- Campos: name, email, password (minLength=8)
- Apos cadastro bem sucedido: mostrar tela de sucesso instruindo verificar email
- Botao GoogleSignInButton
- Link para /login
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['login','register'].forEach(p => { const c=fs.readFileSync('./apps/web/app/(auth)/'+p+'/page.tsx','utf8'); if(!c.includes('name=\"website\"')) throw new Error('honeypot missing in '+p); }); console.log('OK: honeypot present in login and register')"</automated>
  </verify>
  <acceptance_criteria>
    - login/page.tsx tem campo honeypot name="website" com position absolute off-screen
    - register/page.tsx tem campo honeypot name="website"
    - Honeypot preenchido: silenciosamente descarta sem processar
    - Ambas as paginas tem GoogleSignInButton
    - register/page.tsx exibe tela de sucesso com instrucao de verificar email
    - Mensagens de erro genericas (nao revelam existencia do email)
  </acceptance_criteria>
  <done>Paginas login e register criadas com honeypot, Google OAuth e tratamento de erros.</done>
</task>

<task type="auto">
  <name>Task 2: Paginas forgot-password, reset-password e verify-2fa</name>
  <files>apps/web/app/(auth)/forgot-password/page.tsx, apps/web/app/(auth)/reset-password/page.tsx, apps/web/app/(auth)/verify-2fa/page.tsx</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (D-12: reset token 1h, one-time use)
    - apps/web/lib/auth-client.ts
    - apps/web/components/auth/TwoFactorVerify.tsx
  </read_first>
  <action>
Criar apps/web/app/(auth)/forgot-password/page.tsx:
- Campo honeypot name="website"
- Formulario com email
- SEMPRE mostrar mensagem de sucesso apos submit (nao revelar se email existe)
- Chamar authClient.forgetPassword({ email, redirectTo: '/reset-password' })
- Envolver em try/finally — mesmo em erro mostrar mensagem de sucesso

Criar apps/web/app/(auth)/reset-password/page.tsx:
- Usar Suspense + useSearchParams para ler token da URL
- Se sem token: mostrar erro com link para /forgot-password
- Formulario: nova senha + confirmacao (ambas minLength=8)
- Validar senhas iguais antes de submeter
- Chamar authClient.resetPassword({ newPassword, token })
- Em sucesso: redirect para /login?reset=success

Criar apps/web/app/(auth)/verify-2fa/page.tsx:
- Usar Suspense + useSearchParams para ler callbackURL
- Renderizar TwoFactorVerify com onSuccess redirect para callbackURL
- onCancel redirect para /login
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['forgot-password','reset-password','verify-2fa'].forEach(p => { const e=fs.existsSync('./apps/web/app/(auth)/'+p+'/page.tsx'); if(!e) throw new Error(p+' missing'); }); console.log('OK: all 3 pages exist')"</automated>
  </verify>
  <acceptance_criteria>
    - forgot-password sempre mostra mensagem generica de sucesso (try/finally)
    - forgot-password tem honeypot field
    - reset-password valida senha >= 8 chars e confirmacao iguais
    - reset-password usa Suspense para useSearchParams (Next.js 15 requirement)
    - verify-2fa passa callbackURL ao TwoFactorVerify via onSuccess
  </acceptance_criteria>
  <done>Paginas forgot-password, reset-password e verify-2fa criadas.</done>
</task>

<task type="auto">
  <name>Task 3: Account lockout com Redis</name>
  <files>apps/web/lib/account-lockout.ts</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (D-07: 10 tentativas/15min, bloqueio 30min)
    - .planning/research/STACK.md (ioredis 5.10.1)
  </read_first>
  <action>
Instalar ioredis se nao existir:
```bash
pnpm add --filter @parabuains/web ioredis@5.10.1
```

Criar apps/web/lib/account-lockout.ts com as seguintes constantes e funcoes:

Constantes:
```typescript
const WINDOW_SECONDS = 15 * 60;   // 15 minutos
const MAX_ATTEMPTS = 10;
const LOCKOUT_SECONDS = 30 * 60;  // 30 minutos
```

Funcoes exportadas:
1. isAccountLocked(email: string): Promise<{ locked: boolean; ttl?: number }>
   - Usa redis.ttl(lockoutKey) onde lockoutKey = "auth:lockout:{email.toLowerCase()}"
   - ttl > 0 = locked:true com ttl; caso contrario locked:false
   - Fail open: se Redis indisponivel (catch), retornar locked:false

2. recordFailedAttempt(email: string): Promise<{ attempts: number; locked: boolean }>
   - redis.incr("auth:attempts:{email}") e redis.expire na primeira tentativa
   - Se attempts >= MAX_ATTEMPTS: redis.set(lockoutKey, '1', 'EX', LOCKOUT_SECONDS) + redis.del(attemptsKey)
   - Fail open: retornar { attempts: 0, locked: false } em erro

3. clearFailedAttempts(email: string): Promise<void>
   - redis.del(attemptsKey)
   - Ignorar erros

Redis singleton com lazyConnect: true e handler de error que apenas loga (nao crasha).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/account-lockout.ts','utf8'); if(!c.includes('MAX_ATTEMPTS = 10')) throw new Error('MAX_ATTEMPTS=10 missing'); if(!c.includes('WINDOW_SECONDS = 15 * 60')) throw new Error('15min window missing'); if(!c.includes('LOCKOUT_SECONDS = 30 * 60')) throw new Error('30min lockout missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - MAX_ATTEMPTS=10, WINDOW_SECONDS=15*60, LOCKOUT_SECONDS=30*60
    - Redis keys: auth:attempts:{email} e auth:lockout:{email} (email.toLowerCase())
    - Todas as 3 funcoes exportadas: isAccountLocked, recordFailedAttempt, clearFailedAttempts
    - Fail open em todas as funcoes quando Redis indisponivel
  </acceptance_criteria>
  <done>Account lockout com Redis: 10 tentativas/15min gera bloqueio de 30min. Fail open se Redis down.</done>
</task>

<task type="auto">
  <name>Task 4: BFF service token JWT 60s</name>
  <files>apps/web/lib/service-token.ts, apps/web/lib/api-client.ts</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (D-10: payload sub/sessionId/iat/exp, algoritmo HS256)
  </read_first>
  <action>
Instalar jose se nao existir:
```bash
pnpm add --filter @parabuains/web jose
```

Criar apps/web/lib/service-token.ts:
```typescript
import { SignJWT } from 'jose';

const SECRET = process.env.API_JWT_SECRET;
const TOKEN_TTL_SECONDS = 60;

if (!SECRET && process.env.NODE_ENV === 'production') {
  throw new Error('API_JWT_SECRET must be set in production');
}

export async function createServiceToken(userId: string, sessionId: string): Promise<string> {
  const secret = new TextEncoder().encode(SECRET ?? 'dev-secret-change-in-production');
  return new SignJWT({ sessionId })
    .setProtectedHeader({ alg: 'HS256' })
    .setSubject(userId)
    .setIssuedAt()
    .setExpirationTime(`${TOKEN_TTL_SECONDS}s`)
    .sign(secret);
}
```

Criar apps/web/lib/api-client.ts:
```typescript
import { auth } from './auth';
import { createServiceToken } from './service-token';
import { headers } from 'next/headers';

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001';

type FetchOptions = RequestInit & { authenticated?: boolean };

export async function apiFetch<T = unknown>(path: string, options: FetchOptions = {}): Promise<T> {
  const { authenticated = true, ...fetchOptions } = options;
  const reqHeaders: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(fetchOptions.headers as Record<string, string> ?? {}),
  };
  if (authenticated) {
    const session = await auth.api.getSession({ headers: await headers() });
    if (!session) throw new Error('Unauthenticated: no active session');
    const token = await createServiceToken(session.user.id, session.session.id);
    reqHeaders['Authorization'] = `Bearer ${token}`;
  }
  const response = await fetch(`${API_URL}${path}`, { ...fetchOptions, headers: reqHeaders });
  if (!response.ok) {
    const error = await response.json().catch(() => ({ message: 'Unknown error' }));
    throw new Error(error.message ?? `API error: ${response.status}`);
  }
  return response.json();
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/service-token.ts','utf8'); if(!c.includes('TOKEN_TTL_SECONDS = 60')) throw new Error('TTL 60s missing'); if(!c.includes('setSubject(userId)')) throw new Error('sub claim missing'); if(!c.includes('sessionId')) throw new Error('sessionId payload missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - TOKEN_TTL_SECONDS = 60
    - Algoritmo HS256
    - Payload: sub=userId, sessionId, iat, exp
    - api-client.ts adiciona Authorization: Bearer automaticamente
    - jose usado (ESM-native, Edge-compatible)
  </acceptance_criteria>
  <done>BFF service token JWT 60s criado. api-client.ts adiciona token automaticamente em requests ao Fastify.</done>
</task>

<task type="auto">
  <name>Task 5: Fastify auth preHandler hook</name>
  <files>apps/api/src/plugins/auth.ts</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (secao "Fastify Auth Guard")
    - .planning/research/STACK.md (@fastify/jwt 10.0.0, fastify-plugin 5.1.0)
  </read_first>
  <action>
Criar apps/api/src/plugins/auth.ts:
```typescript
import fp from 'fastify-plugin';
import { FastifyPluginAsync, FastifyRequest, FastifyReply } from 'fastify';

const authPlugin: FastifyPluginAsync = async (fastify) => {
  fastify.addHook('preHandler', async (request: FastifyRequest, reply: FastifyReply) => {
    const routeConfig = request.routeOptions?.config as Record<string, unknown> | undefined;
    if (routeConfig?.skipAuth) return;

    const authHeader = request.headers.authorization;
    if (!authHeader?.startsWith('Bearer ')) {
      return reply.code(401).send({ error: 'Unauthorized', message: 'Missing authorization token' });
    }

    try {
      await request.jwtVerify();
    } catch (err) {
      request.log.warn({ err }, 'JWT verification failed');
      return reply.code(401).send({ error: 'Unauthorized', message: 'Invalid or expired token' });
    }
  });

  fastify.decorateRequest('userId', null);
  fastify.addHook('preHandler', async (request: FastifyRequest) => {
    const routeConfig = request.routeOptions?.config as Record<string, unknown> | undefined;
    if (routeConfig?.skipAuth) return;
    const payload = request.user as { sub?: string } | undefined;
    (request as FastifyRequest & { userId: string | null }).userId = payload?.sub ?? null;
  });
};

export default fp(authPlugin, {
  name: 'auth-plugin',
  dependencies: ['@fastify/jwt'],
});

declare module 'fastify' {
  interface FastifyRequest { userId: string | null; }
  interface RouteOptions {
    config?: { skipAuth?: boolean; [key: string]: unknown; };
  }
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/api/src/plugins/auth.ts','utf8'); if(!c.includes('addHook')) throw new Error('addHook missing'); if(!c.includes('jwtVerify')) throw new Error('jwtVerify missing'); if(!c.includes('skipAuth')) throw new Error('skipAuth missing'); if(!c.includes('fp(authPlugin')) throw new Error('fastify-plugin wrapper missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Usa fp() do fastify-plugin (previne scope leak)
    - preHandler chama request.jwtVerify()
    - Rotas com config.skipAuth: true pulam verificacao
    - 401 com mensagem descritiva em falha
    - TypeScript: FastifyRequest extendido com userId
    - dependencies: ['@fastify/jwt']
  </acceptance_criteria>
  <done>Fastify auth preHandler hook com skipAuth, userId decorator e fastify-plugin wrapper.</done>
</task>

<task type="auto">
  <name>Task 6: Next.js middleware para protecao de rotas</name>
  <files>apps/web/middleware.ts</files>
  <read_first>
    - apps/web/lib/auth.ts
    - .planning/phases/02-auth/02-CONTEXT.md (D-03: email verification gate)
  </read_first>
  <action>
Criar ou atualizar apps/web/middleware.ts:
```typescript
import { NextRequest, NextResponse } from 'next/server';
import { getSessionCookie } from 'better-auth/cookies';

const PUBLIC_ROUTES = [
  '/login', '/register', '/forgot-password',
  '/reset-password', '/verify-2fa', '/api/auth',
];

const PROTECTED_PREFIXES = [
  '/settings', '/feed', '/profile', '/api/user',
];

export async function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  if (
    pathname.startsWith('/_next') ||
    pathname.startsWith('/favicon') ||
    PUBLIC_ROUTES.some(route => pathname.startsWith(route))
  ) {
    return NextResponse.next();
  }

  const needsAuth = PROTECTED_PREFIXES.some(prefix => pathname.startsWith(prefix));
  if (!needsAuth) return NextResponse.next();

  const sessionCookie = getSessionCookie(request);
  if (!sessionCookie) {
    const loginUrl = new URL('/login', request.url);
    loginUrl.searchParams.set('callbackURL', pathname);
    return NextResponse.redirect(loginUrl);
  }

  return NextResponse.next();
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
};
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/middleware.ts','utf8'); if(!c.includes('getSessionCookie')) throw new Error('session check missing'); if(!c.includes('PROTECTED_PREFIXES')) throw new Error('protected routes missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Usa getSessionCookie de better-auth/cookies (Edge-compatible, nao faz chamada ao DB)
    - /settings, /feed, /profile, /api/user sao protegidas
    - Redirect para /login?callbackURL= quando nao autenticado
    - Rotas publicas e /api/auth/* nunca redirecionadas
  </acceptance_criteria>
  <done>Next.js middleware configurado para protecao de rotas com redirect para login.</done>
</task>

<task type="auto">
  <name>Task 7: Testes Vitest para lockout e service token</name>
  <files>apps/web/lib/__tests__/account-lockout.test.ts, apps/web/lib/__tests__/service-token.test.ts</files>
  <read_first>
    - apps/web/lib/account-lockout.ts
    - apps/web/lib/service-token.ts
  </read_first>
  <action>
Criar apps/web/lib/__tests__/account-lockout.test.ts:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

const mockTtl = vi.fn();
const mockIncr = vi.fn();
const mockExpire = vi.fn();
const mockSet = vi.fn();
const mockDel = vi.fn();

vi.mock('ioredis', () => ({
  default: vi.fn(() => ({
    on: vi.fn(),
    ttl: mockTtl,
    incr: mockIncr,
    expire: mockExpire,
    set: mockSet,
    del: mockDel,
  })),
}));

describe('isAccountLocked', () => {
  beforeEach(() => vi.clearAllMocks());

  it('retorna locked:false quando chave nao existe (ttl=-2)', async () => {
    mockTtl.mockResolvedValue(-2);
    const { isAccountLocked } = await import('@/lib/account-lockout');
    expect((await isAccountLocked('user@example.com')).locked).toBe(false);
  });

  it('retorna locked:true com ttl quando lockout ativo', async () => {
    mockTtl.mockResolvedValue(1200);
    const { isAccountLocked } = await import('@/lib/account-lockout');
    const result = await isAccountLocked('user@example.com');
    expect(result.locked).toBe(true);
    expect(result.ttl).toBe(1200);
  });

  it('fail open: retorna locked:false quando Redis lanca erro', async () => {
    mockTtl.mockRejectedValue(new Error('Connection refused'));
    const { isAccountLocked } = await import('@/lib/account-lockout');
    expect((await isAccountLocked('user@example.com')).locked).toBe(false);
  });
});

describe('recordFailedAttempt', () => {
  beforeEach(() => vi.clearAllMocks());

  it('incrementa e retorna tentativas sem lockout (< 10)', async () => {
    mockIncr.mockResolvedValue(3);
    mockExpire.mockResolvedValue(1);
    const { recordFailedAttempt } = await import('@/lib/account-lockout');
    const result = await recordFailedAttempt('user@example.com');
    expect(result.attempts).toBe(3);
    expect(result.locked).toBe(false);
  });

  it('aplica lockout ao atingir 10 tentativas', async () => {
    mockIncr.mockResolvedValue(10);
    mockSet.mockResolvedValue('OK');
    mockDel.mockResolvedValue(1);
    const { recordFailedAttempt } = await import('@/lib/account-lockout');
    const result = await recordFailedAttempt('user@example.com');
    expect(result.locked).toBe(true);
    expect(mockSet).toHaveBeenCalled();
  });
});
```

Criar apps/web/lib/__tests__/service-token.test.ts:
```typescript
import { describe, it, expect, beforeAll } from 'vitest';
import { decodeJwt } from 'jose';

beforeAll(() => {
  process.env.API_JWT_SECRET = 'test-secret-min-32-chars-long-enough!';
});

describe('createServiceToken', () => {
  it('gera JWT com 3 partes (header.payload.signature)', async () => {
    const { createServiceToken } = await import('@/lib/service-token');
    const token = await createServiceToken('user-123', 'session-456');
    expect(token.split('.')).toHaveLength(3);
  });

  it('payload contem sub, sessionId e exp em ~60s', async () => {
    const { createServiceToken } = await import('@/lib/service-token');
    const token = await createServiceToken('user-123', 'session-456');
    const payload = decodeJwt(token);
    expect(payload.sub).toBe('user-123');
    expect(payload.sessionId).toBe('session-456');
    const now = Math.floor(Date.now() / 1000);
    expect(payload.exp).toBeGreaterThan(now + 55);
    expect(payload.exp).toBeLessThanOrEqual(now + 65);
  });
});
```

Executar:
```bash
pnpm --filter @parabuains/web test
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c1=fs.readFileSync('./apps/web/lib/__tests__/account-lockout.test.ts','utf8'); const c2=fs.readFileSync('./apps/web/lib/__tests__/service-token.test.ts','utf8'); if(!c1.includes('aplica lockout ao atingir 10')) throw new Error('lockout test missing'); if(!c2.includes('sessionId')) throw new Error('service token test missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - account-lockout.test.ts: locked:false sem lockout, locked:true com ttl, fail open em erro Redis, lockout ao atingir 10
    - service-token.test.ts: JWT 3 partes, sub=userId, sessionId, exp em 55-65s
    - pnpm test passa
  </acceptance_criteria>
  <done>Testes Vitest para lockout e service token cobrindo casos criticos incluindo fail open.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-04-01 | Spoofing | Account lockout bypass | mitigate | Lockout por email; mensagens de erro genericas nao revelam existencia do email |
| T-02-04-02 | Denial of Service | Honeypot subversion | mitigate | Honeypot: position absolute off-screen + tabIndex=-1 + aria-hidden=true |
| T-02-04-03 | Elevation of Privilege | Service token forjado | mitigate | HMAC-SHA256 com API_JWT_SECRET min 32 chars; TTL 60s limita replay window |
| T-02-04-04 | Spoofing | JWT replay attack | mitigate | TTL 60s + sessionId no payload permite revogacao futura |
| T-02-04-05 | Elevation of Privilege | Rota protegida sem auth | mitigate | Middleware Next.js + Fastify preHandler = dupla verificacao independente |
| T-02-04-06 | Denial of Service | Redis lockout indisponivel | mitigate | Fail open: se Redis down, lockout nao bloqueia usuarios legitimos |
| T-02-04-07 | Information Disclosure | Mensagens de erro auth | mitigate | Mensagens genericas no forgot-password (nao revela se email existe) |
</threat_model>

<verification>
1. `grep "name=\"website\"" apps/web/app/(auth)/login/page.tsx` — deve retornar match
2. `grep "name=\"website\"" apps/web/app/(auth)/register/page.tsx` — deve retornar match
3. `grep "MAX_ATTEMPTS = 10" apps/web/lib/account-lockout.ts` — deve retornar match
4. `grep "TOKEN_TTL_SECONDS = 60" apps/web/lib/service-token.ts` — deve retornar match
5. `grep "jwtVerify" apps/api/src/plugins/auth.ts` — deve retornar match
6. `grep "getSessionCookie" apps/web/middleware.ts` — deve retornar match
7. `pnpm --filter @parabuains/web test` — todos os testes passam
8. Fluxo manual: criar conta na /register, receber email, verificar, fazer login em /login, sessao persistente
</verification>

<success_criteria>
- 5 paginas de auth: login, register, forgot-password, reset-password, verify-2fa
- Honeypot field name="website" em login, register e forgot-password
- Account lockout: MAX_ATTEMPTS=10, WINDOW=15min, LOCKOUT=30min; fail open se Redis down
- BFF service token: JWT HS256, TOKEN_TTL_SECONDS=60, payload {sub, sessionId, iat, exp}
- Fastify preHandler verifica JWT com skipAuth para rotas publicas
- Next.js middleware protege /settings, /feed, /profile com redirect para /login
- Testes Vitest passando para lockout (inclusive fail open) e service token
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-04-SUMMARY.md`
</output>
