---
phase: 02-auth
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/web/lib/auth.ts
  - apps/web/lib/auth-client.ts
  - apps/web/app/api/auth/[...all]/route.ts
autonomous: true
requirements: [AUTH-03, AUTH-10]

must_haves:
  truths:
    - "Google OAuth configurado no better-auth com GOOGLE_CLIENT_ID e GOOGLE_CLIENT_SECRET"
    - "Account linking ativo: mesmo email OAuth + email/senha nao cria conta duplicada"
    - "Callback URL configurada: /api/auth/callback/google"
    - "auth-client.ts exporta createAuthClient para uso em Client Components"
  artifacts:
    - path: "apps/web/lib/auth.ts"
      provides: "better-auth com socialProviders.google configurado"
      contains: "google: { clientId: process.env.GOOGLE_CLIENT_ID"
    - path: "apps/web/lib/auth-client.ts"
      provides: "Client-side better-auth instance para uso em React"
      contains: "createAuthClient"
  key_links:
    - from: "apps/web/lib/auth.ts"
      to: "Google OAuth API"
      via: "GOOGLE_CLIENT_ID e GOOGLE_CLIENT_SECRET env vars"
      pattern: "socialProviders: { google: { clientId: process.env.GOOGLE_CLIENT_ID!"
    - from: "apps/web/lib/auth-client.ts"
      to: "apps/web/lib/auth.ts"
      via: "createAuthClient({ baseURL })"
      pattern: "createAuthClient({ baseURL: process.env.NEXT_PUBLIC_APP_URL"
---

<objective>
Configurar Google OAuth no better-auth, garantir account linking (sem duplicatas para o mesmo email), criar o auth-client para Client Components, e adicionar o botao "Entrar com Google" na UI de login.

Output: Usuarios podem fazer login com Google. Se ja tem conta email/senha com o mesmo email, as contas sao vinculadas automaticamente (sem duplicata).
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth/02-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar Google OAuth ao better-auth</name>
  <files>apps/web/lib/auth.ts</files>
  <read_first>
    - apps/web/lib/auth.ts (configuracao existente do 02-01)
    - .planning/phases/02-auth/02-CONTEXT.md (D-04: Google OAuth Only, D-10: account linking)
  </read_first>
  <action>
Atualizar `apps/web/lib/auth.ts` para adicionar o Google provider e account linking.

Adicionar `socialProviders` a configuracao existente:

```typescript
import { betterAuth } from 'better-auth';
import { drizzleAdapter } from 'better-auth/adapters/drizzle';
import { twoFactor } from 'better-auth/plugins';
import { db } from './db';
import * as schema from '@parabuains/db/schema';
import { sendVerificationEmail, sendPasswordResetEmail } from './email';

export const auth = betterAuth({
  database: drizzleAdapter(db, {
    provider: 'pg',
    schema: {
      user: schema.user,
      session: schema.session,
      account: schema.account,
      verification: schema.verification,
    },
  }),
  emailAndPassword: {
    enabled: true,
    requireEmailVerification: true,
    minPasswordLength: 8,
    sendResetPassword: async ({ user, url }) => {
      await sendPasswordResetEmail({ to: user.email, url });
    },
  },
  emailVerification: {
    sendVerificationEmail: async ({ user, url }) => {
      await sendVerificationEmail({ to: user.email, url });
    },
    expiresIn: 60 * 60 * 24, // 24 horas
  },
  socialProviders: {
    google: {
      clientId: process.env.GOOGLE_CLIENT_ID!,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
    },
  },
  // Account linking: vincular contas OAuth ao mesmo email existente
  account: {
    accountLinking: {
      enabled: true,
      trustedProviders: ['google'],
    },
  },
  session: {
    expiresIn: 60 * 60 * 24 * 30,
    updateAge: 60 * 15,
    cookieCache: {
      enabled: true,
      maxAge: 60 * 15,
    },
  },
  plugins: [
    twoFactor({
      totpOptions: {
        digits: 6,
        period: 30,
      },
      backupCodeOptions: {
        amount: 8,
        length: 10,
      },
    }),
  ],
  trustedOrigins: [process.env.NEXTAUTH_URL ?? 'http://localhost:3000'],
});

export type Session = typeof auth.$Infer.Session;
export type User = typeof auth.$Infer.Session.user;
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/auth.ts','utf8'); if(!c.includes('google: { clientId: process.env.GOOGLE_CLIENT_ID')) throw new Error('Google OAuth missing'); if(!c.includes('accountLinking')) throw new Error('account linking missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - auth.ts contem socialProviders.google com clientId e clientSecret de env vars
    - auth.ts contem account.accountLinking.enabled: true
    - account.accountLinking.trustedProviders inclui 'google'
  </acceptance_criteria>
  <done>Google OAuth adicionado ao better-auth com account linking ativo.</done>
</task>

<task type="auto">
  <name>Task 2: Criar auth-client para Client Components</name>
  <files>apps/web/lib/auth-client.ts</files>
  <read_first>
    - apps/web/lib/auth.ts (configuracao server-side)
  </read_first>
  <action>
Criar `apps/web/lib/auth-client.ts` — instancia client-side do better-auth para uso em React Client Components:

```typescript
import { createAuthClient } from 'better-auth/react';
import { twoFactorClient } from 'better-auth/client/plugins';

export const authClient = createAuthClient({
  baseURL: process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000',
  plugins: [twoFactorClient()],
});

export const {
  signIn,
  signUp,
  signOut,
  useSession,
  getSession,
} = authClient;
```

Este modulo e seguro para importar em Client Components (`'use client'`).
O `useSession` hook retorna `{ data: Session | null, isPending: boolean, error: Error | null }`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/auth-client.ts','utf8'); if(!c.includes('createAuthClient')) throw new Error('createAuthClient missing'); if(!c.includes('twoFactorClient')) throw new Error('twoFactorClient missing'); if(!c.includes('export const { signIn')) throw new Error('exports missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - auth-client.ts exporta: signIn, signUp, signOut, useSession, getSession
    - Usa createAuthClient de better-auth/react
    - Inclui twoFactorClient() plugin
    - baseURL usa NEXT_PUBLIC_APP_URL env var
  </acceptance_criteria>
  <done>auth-client.ts criado para uso em Client Components React.</done>
</task>

<task type="auto">
  <name>Task 3: Componente GoogleSignInButton</name>
  <files>apps/web/components/auth/GoogleSignInButton.tsx</files>
  <read_first>
    - apps/web/lib/auth-client.ts (signIn recém criado)
    - .planning/phases/02-auth/02-CONTEXT.md (UI minimalista — Tailwind v4 only)
  </read_first>
  <action>
Criar `apps/web/components/auth/GoogleSignInButton.tsx`:

```typescript
'use client';

import { signIn } from '@/lib/auth-client';
import { useState } from 'react';

interface GoogleSignInButtonProps {
  callbackURL?: string;
  className?: string;
}

export function GoogleSignInButton({
  callbackURL = '/',
  className = '',
}: GoogleSignInButtonProps) {
  const [isLoading, setIsLoading] = useState(false);

  async function handleGoogleSignIn() {
    setIsLoading(true);
    try {
      await signIn.social({
        provider: 'google',
        callbackURL,
      });
    } catch (error) {
      console.error('[GoogleSignIn] Error:', error);
      setIsLoading(false);
    }
    // Nao resetar isLoading em sucesso — pagina redireciona
  }

  return (
    <button
      type="button"
      onClick={handleGoogleSignIn}
      disabled={isLoading}
      className={`flex w-full items-center justify-center gap-3 rounded-lg border border-gray-300 bg-white px-4 py-2.5 text-sm font-medium text-gray-700 shadow-sm transition-colors hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-violet-500 focus:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-60 ${className}`}
    >
      {isLoading ? (
        <span className="h-4 w-4 animate-spin rounded-full border-2 border-gray-300 border-t-gray-600" />
      ) : (
        <GoogleIcon />
      )}
      {isLoading ? 'Redirecionando...' : 'Entrar com Google'}
    </button>
  );
}

function GoogleIcon() {
  return (
    <svg width="18" height="18" viewBox="0 0 18 18" aria-hidden="true">
      <path
        fill="#4285F4"
        d="M17.64 9.2c0-.637-.057-1.251-.164-1.84H9v3.481h4.844c-.209 1.125-.843 2.078-1.796 2.717v2.258h2.908c1.702-1.567 2.684-3.875 2.684-6.615z"
      />
      <path
        fill="#34A853"
        d="M9 18c2.43 0 4.467-.806 5.956-2.184l-2.908-2.258c-.806.54-1.837.86-3.048.86-2.344 0-4.328-1.584-5.036-3.711H.957v2.332A8.997 8.997 0 0 0 9 18z"
      />
      <path
        fill="#FBBC05"
        d="M3.964 10.707A5.41 5.41 0 0 1 3.682 9c0-.593.102-1.17.282-1.707V4.961H.957A8.996 8.996 0 0 0 0 9c0 1.452.348 2.827.957 4.039l3.007-2.332z"
      />
      <path
        fill="#EA4335"
        d="M9 3.58c1.321 0 2.508.454 3.44 1.345l2.582-2.58C13.463.891 11.426 0 9 0A8.997 8.997 0 0 0 .957 4.961L3.964 7.293C4.672 5.163 6.656 3.58 9 3.58z"
      />
    </svg>
  );
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/components/auth/GoogleSignInButton.tsx','utf8'); if(!c.includes('signIn.social')) throw new Error('signIn.social missing'); if(!c.includes(\"provider: 'google'\")) throw new Error('google provider missing'); if(!c.includes('disabled={isLoading}')) throw new Error('loading state missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Componente usa 'use client' directive
    - Chama signIn.social({ provider: 'google', callbackURL })
    - Estado isLoading com feedback visual (spinner)
    - Botao desabilitado durante loading
    - GoogleIcon SVG inline (sem dependencia externa)
  </acceptance_criteria>
  <done>GoogleSignInButton criado com estado de loading e icone Google SVG inline.</done>
</task>

<task type="auto">
  <name>Task 4: Testes para Google OAuth e account linking</name>
  <files>apps/web/lib/__tests__/auth-oauth.test.ts, apps/web/components/auth/__tests__/GoogleSignInButton.test.tsx</files>
  <read_first>
    - apps/web/lib/auth.ts
    - apps/web/components/auth/GoogleSignInButton.tsx
  </read_first>
  <action>
Criar `apps/web/lib/__tests__/auth-oauth.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';

describe('Google OAuth configuration', () => {
  it('auth.ts contem socialProviders.google', () => {
    const fs = require('fs');
    const content = fs.readFileSync('./apps/web/lib/auth.ts', 'utf8');
    expect(content).toContain('google: { clientId: process.env.GOOGLE_CLIENT_ID');
    expect(content).toContain('GOOGLE_CLIENT_SECRET');
  });

  it('auth.ts tem account linking habilitado', () => {
    const fs = require('fs');
    const content = fs.readFileSync('./apps/web/lib/auth.ts', 'utf8');
    expect(content).toContain('accountLinking');
    expect(content).toContain('trustedProviders');
    expect(content).toContain("'google'");
  });
});
```

Criar `apps/web/components/auth/__tests__/GoogleSignInButton.test.tsx`:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { GoogleSignInButton } from '../GoogleSignInButton';

const mockSignIn = { social: vi.fn() };
vi.mock('@/lib/auth-client', () => ({
  signIn: { social: vi.fn() },
}));

describe('GoogleSignInButton', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('renderiza botao com texto correto', () => {
    render(<GoogleSignInButton />);
    expect(screen.getByRole('button', { name: /entrar com google/i })).toBeDefined();
  });

  it('botao nao esta desabilitado por padrao', () => {
    render(<GoogleSignInButton />);
    const btn = screen.getByRole('button');
    expect(btn).not.toHaveAttribute('disabled');
  });
});
```

Instalar @testing-library/react se nao existir:
```bash
pnpm add --filter @parabuains/web --save-dev @testing-library/react @testing-library/user-event @vitejs/plugin-react jsdom
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/__tests__/auth-oauth.test.ts','utf8'); if(!c.includes('Google OAuth configuration')) throw new Error('test missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - auth-oauth.test.ts verifica presenca de Google provider e account linking na config
    - GoogleSignInButton.test.tsx verifica renderizacao e estado inicial
    - pnpm test passa
  </acceptance_criteria>
  <done>Testes para Google OAuth e GoogleSignInButton criados.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-02-01 | Spoofing | Google OAuth callback | mitigate | better-auth valida state parameter (CSRF protection) no callback |
| T-02-02-02 | Elevation of Privilege | Account linking | mitigate | accountLinking.trustedProviders limita quais providers podem vincular; Google marcado como trusted |
| T-02-02-03 | Information Disclosure | GOOGLE_CLIENT_SECRET | mitigate | Variavel de ambiente server-only; nunca exposta ao cliente |
| T-02-02-04 | Tampering | OAuth state parameter | mitigate | better-auth gera e valida state CSRF automaticamente |
| T-02-02-05 | Spoofing | Duplicate accounts | mitigate | accountLinking.enabled: true previne criacao de conta duplicada para mesmo email |
</threat_model>

<verification>
1. `grep "google: { clientId" apps/web/lib/auth.ts` — deve retornar match
2. `grep "accountLinking" apps/web/lib/auth.ts` — deve retornar match
3. `grep "twoFactorClient" apps/web/lib/auth-client.ts` — deve retornar match
4. `ls apps/web/components/auth/GoogleSignInButton.tsx` — deve existir
5. Fluxo manual: acessar `/login`, clicar "Entrar com Google", ser redirecionado para Google OAuth
6. `pnpm --filter @parabuains/web test` — todos os testes devem passar
</verification>

<success_criteria>
- socialProviders.google configurado com env vars GOOGLE_CLIENT_ID e GOOGLE_CLIENT_SECRET
- account.accountLinking.enabled: true com trustedProviders: ['google']
- auth-client.ts exporta hooks e funcoes para Client Components
- GoogleSignInButton renderiza corretamente com estado de loading
- Sem duplicatas de conta para mesmo email (testado com account linking)
- Testes Vitest passando
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-02-SUMMARY.md`
</output>
