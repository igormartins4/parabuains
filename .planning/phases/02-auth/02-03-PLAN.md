---
phase: 02-auth
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/web/lib/auth.ts
  - packages/db/src/schema/auth.ts
  - packages/db/migrations/ (nova migration)
  - apps/web/components/auth/TwoFactorSetup.tsx
  - apps/web/components/auth/TwoFactorVerify.tsx
  - apps/web/app/settings/two-factor/page.tsx
autonomous: true
requirements: [AUTH-07]

must_haves:
  truths:
    - "2FA TOTP opcional — usuario escolhe ativar nas configuracoes"
    - "8 backup codes gerados no momento da ativacao (mostrados uma vez)"
    - "Backup codes hasheados com bcrypt cost 12 na tabela totp_backup_codes"
    - "TOTP verificado em operacoes sensiveis: troca de email, desativar 2FA, deletar conta"
    - "QR code gerado com qrcode library e exibido no setup"
  artifacts:
    - path: "apps/web/components/auth/TwoFactorSetup.tsx"
      provides: "UI de setup 2FA com QR code + codigo manual + backup codes"
      contains: "qrCodeUrl"
    - path: "packages/db/src/schema/auth.ts"
      provides: "Tabela totp_backup_codes no schema Drizzle"
      contains: "export const totpBackupCodes = pgTable"
  key_links:
    - from: "apps/web/components/auth/TwoFactorSetup.tsx"
      to: "better-auth twoFactor plugin"
      via: "authClient.twoFactor.enable()"
      pattern: "authClient.twoFactor.enable({ password })"
    - from: "packages/db/src/schema/auth.ts"
      to: "totp_backup_codes table"
      via: "pgTable com references para user.id"
      pattern: "references(() => user.id, { onDelete: 'cascade' })"
---

<objective>
Implementar 2FA TOTP opcional: setup com QR code, 8 backup codes hasheados (mostrados uma vez), tabela totp_backup_codes no Drizzle, e verificacao TOTP em operacoes sensiveis (change email, disable 2FA, delete account).

Output: Usuario pode ativar 2FA nas configuracoes e usa TOTP nas operacoes sensiveis quando habilitado.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth/02-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependencias 2FA (otpauth, qrcode)</name>
  <files>apps/web/package.json</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (D-05: otpauth + qrcode)
    - apps/web/package.json (dependencias existentes)
  </read_first>
  <action>
Instalar dependencias de 2FA em apps/web:

```bash
pnpm add --filter @parabuains/web otpauth
pnpm add --filter @parabuains/web qrcode
pnpm add --filter @parabuains/web --save-dev @types/qrcode
```

O better-auth twoFactor plugin ja usa otpauth internamente, mas precisamos de qrcode para renderizar o QR code na UI.
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/web/package.json'); if(!p.dependencies['qrcode']) throw new Error('qrcode missing'); console.log('OK: qrcode@'+p.dependencies['qrcode'])"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json contem "qrcode" nas dependencies
    - apps/web/package.json contem "@types/qrcode" nas devDependencies
    - pnpm install nao produz erros
  </acceptance_criteria>
  <done>qrcode e @types/qrcode instalados em apps/web.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar tabela totp_backup_codes ao schema Drizzle</name>
  <files>packages/db/src/schema/auth.ts</files>
  <read_first>
    - packages/db/src/schema/auth.ts (schema existente)
    - .planning/phases/02-auth/02-CONTEXT.md (secao "Backup Codes Table")
  </read_first>
  <action>
Adicionar a tabela `totp_backup_codes` ao final de `packages/db/src/schema/auth.ts`:

```typescript
export const totpBackupCodes = pgTable('totp_backup_codes', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: text('user_id')
    .notNull()
    .references(() => user.id, { onDelete: 'cascade' }),
  codeHash: varchar('code_hash', { length: 255 }).notNull(),
  usedAt: timestamp('used_at', { withTimezone: true }),
  createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
});
```

Adicionar import de `uuid` e `varchar` ao topo do arquivo (se nao existirem):
```typescript
import { pgTable, text, timestamp, boolean, uuid, varchar } from 'drizzle-orm/pg-core';
```

Gerar nova migration:
```bash
pnpm --filter @parabuains/db db:generate
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./packages/db/src/schema/auth.ts','utf8'); if(!c.includes('totp_backup_codes')) throw new Error('table missing'); if(!c.includes('codeHash')) throw new Error('codeHash missing'); if(!c.includes('usedAt')) throw new Error('usedAt missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/db/src/schema/auth.ts exporta totpBackupCodes
    - Tabela tem: id (uuid pk), userId (fk user.id cascade), codeHash (varchar 255), usedAt (timestamp nullable), createdAt
    - Nova migration gerada em packages/db/migrations/
  </acceptance_criteria>
  <done>Tabela totp_backup_codes adicionada ao schema Drizzle com migration gerada.</done>
</task>

<task type="auto">
  <name>Task 3: Servico de backup codes (geracao, hash, verificacao)</name>
  <files>apps/web/lib/totp-backup.ts</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (D-06: backup codes, bcrypt cost 12)
    - packages/db/src/schema/auth.ts (totpBackupCodes table)
  </read_first>
  <action>
Criar `apps/web/lib/totp-backup.ts`:

```typescript
import { db } from './db';
import { totpBackupCodes } from '@parabuains/db/schema';
import { eq, and, isNull } from 'drizzle-orm';
import bcrypt from 'bcryptjs';
import { randomBytes } from 'crypto';

const BACKUP_CODE_COUNT = 8;
const BACKUP_CODE_LENGTH = 10;
const BCRYPT_COST = 12;

/**
 * Gera 8 backup codes alphanumericos, retorna plaintext (para mostrar ao usuario)
 * e hashes (para armazenar no banco)
 */
export async function generateBackupCodes(userId: string): Promise<{
  plainCodes: string[];
  hashedCodes: string[];
}> {
  const plainCodes: string[] = [];
  const hashedCodes: string[] = [];

  for (let i = 0; i < BACKUP_CODE_COUNT; i++) {
    const code = randomBytes(Math.ceil(BACKUP_CODE_LENGTH / 2))
      .toString('hex')
      .slice(0, BACKUP_CODE_LENGTH)
      .toUpperCase();
    plainCodes.push(code);
    hashedCodes.push(await bcrypt.hash(code, BCRYPT_COST));
  }

  return { plainCodes, hashedCodes };
}

/**
 * Persiste os backup codes hasheados no banco.
 * Remove codigos anteriores do usuario antes de inserir os novos.
 */
export async function storeBackupCodes(userId: string, hashedCodes: string[]): Promise<void> {
  // Remover codigos anteriores
  await db.delete(totpBackupCodes).where(eq(totpBackupCodes.userId, userId));

  // Inserir novos codigos
  await db.insert(totpBackupCodes).values(
    hashedCodes.map((codeHash) => ({
      userId,
      codeHash,
    }))
  );
}

/**
 * Verifica se um backup code e valido para o usuario.
 * Se valido, marca como usado (one-time use).
 * Retorna true se valido, false caso contrario.
 */
export async function verifyBackupCode(userId: string, code: string): Promise<boolean> {
  // Buscar codigos nao usados do usuario
  const unusedCodes = await db
    .select()
    .from(totpBackupCodes)
    .where(and(eq(totpBackupCodes.userId, userId), isNull(totpBackupCodes.usedAt)));

  for (const row of unusedCodes) {
    const isValid = await bcrypt.compare(code.toUpperCase(), row.codeHash);
    if (isValid) {
      // Marcar como usado
      await db
        .update(totpBackupCodes)
        .set({ usedAt: new Date() })
        .where(eq(totpBackupCodes.id, row.id));
      return true;
    }
  }

  return false;
}

/**
 * Conta backup codes nao usados restantes para o usuario.
 */
export async function countRemainingBackupCodes(userId: string): Promise<number> {
  const codes = await db
    .select({ id: totpBackupCodes.id })
    .from(totpBackupCodes)
    .where(and(eq(totpBackupCodes.userId, userId), isNull(totpBackupCodes.usedAt)));
  return codes.length;
}
```

Instalar bcryptjs se nao existir:
```bash
pnpm add --filter @parabuains/web bcryptjs
pnpm add --filter @parabuains/web --save-dev @types/bcryptjs
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/totp-backup.ts','utf8'); if(!c.includes('generateBackupCodes')) throw new Error('generateBackupCodes missing'); if(!c.includes('verifyBackupCode')) throw new Error('verifyBackupCode missing'); if(!c.includes('BCRYPT_COST = 12')) throw new Error('bcrypt cost 12 missing'); if(!c.includes('BACKUP_CODE_COUNT = 8')) throw new Error('8 codes missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - generateBackupCodes gera exatamente BACKUP_CODE_COUNT (8) codigos
    - Codigos hasheados com bcrypt cost 12
    - verifyBackupCode marca o codigo como usado apos verificacao positiva
    - storeBackupCodes limpa codigos anteriores antes de inserir novos
    - countRemainingBackupCodes retorna apenas codigos com usedAt IS NULL
  </acceptance_criteria>
  <done>Servico de backup codes com geracao, armazenamento hasheado e verificacao one-time-use.</done>
</task>

<task type="auto">
  <name>Task 4: API routes para 2FA setup e verificacao</name>
  <files>apps/web/app/api/auth/2fa/setup/route.ts, apps/web/app/api/auth/2fa/verify-sensitive/route.ts</files>
  <read_first>
    - apps/web/lib/auth.ts (twoFactor plugin)
    - apps/web/lib/auth-client.ts
    - apps/web/lib/totp-backup.ts
    - .planning/phases/02-auth/02-CONTEXT.md (D-05: operacoes sensiveis)
  </read_first>
  <action>
Criar `apps/web/app/api/auth/2fa/setup/route.ts` — endpoint para gerar QR code e backup codes na ativacao do 2FA:

```typescript
import { auth } from '@/lib/auth';
import { generateBackupCodes, storeBackupCodes } from '@/lib/totp-backup';
import { NextRequest, NextResponse } from 'next/server';
import QRCode from 'qrcode';

export async function POST(request: NextRequest) {
  const session = await auth.api.getSession({ headers: request.headers });
  if (!session) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  try {
    // better-auth gera o TOTP secret via seu plugin
    const totpSetup = await auth.api.enableTwoFactor({
      headers: request.headers,
      body: await request.json(),
    });

    // Gerar QR code URL
    const qrCodeDataUrl = await QRCode.toDataURL(totpSetup.totpURI, {
      errorCorrectionLevel: 'M',
      width: 256,
    });

    // Gerar e armazenar backup codes
    const { plainCodes, hashedCodes } = await generateBackupCodes(session.user.id);
    await storeBackupCodes(session.user.id, hashedCodes);

    return NextResponse.json({
      totpURI: totpSetup.totpURI,
      qrCodeDataUrl,
      backupCodes: plainCodes, // Mostrado UMA vez ao usuario
    });
  } catch (error) {
    console.error('[2FA Setup] Error:', error);
    return NextResponse.json({ error: 'Failed to setup 2FA' }, { status: 500 });
  }
}
```

Criar `apps/web/app/api/auth/2fa/verify-sensitive/route.ts` — verificacao TOTP para operacoes sensiveis:

```typescript
import { auth } from '@/lib/auth';
import { verifyBackupCode } from '@/lib/totp-backup';
import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  const session = await auth.api.getSession({ headers: request.headers });
  if (!session) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  // Se usuario nao tem 2FA habilitado, operacao e permitida
  if (!session.user.twoFactorEnabled) {
    return NextResponse.json({ verified: true });
  }

  const { code } = await request.json();
  if (!code || typeof code !== 'string') {
    return NextResponse.json({ error: 'Code required' }, { status: 400 });
  }

  // Tentar verificar como backup code primeiro
  const sanitizedCode = code.trim().replace(/[\s-]/g, '');
  if (sanitizedCode.length === 10) {
    const isBackupCodeValid = await verifyBackupCode(session.user.id, sanitizedCode);
    if (isBackupCodeValid) {
      return NextResponse.json({ verified: true, usedBackupCode: true });
    }
  }

  // Verificar TOTP via better-auth
  try {
    const result = await auth.api.verifyTOTP({
      headers: request.headers,
      body: { code: sanitizedCode },
    });
    return NextResponse.json({ verified: result.status });
  } catch {
    return NextResponse.json({ verified: false, error: 'Invalid code' }, { status: 400 });
  }
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/app/api/auth/2fa/setup/route.ts','utf8'); if(!c.includes('QRCode.toDataURL')) throw new Error('QR code missing'); if(!c.includes('generateBackupCodes')) throw new Error('backup codes missing'); if(!c.includes('backupCodes: plainCodes')) throw new Error('plainCodes not returned'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - /api/auth/2fa/setup retorna totpURI, qrCodeDataUrl, backupCodes (plaintext, shown once)
    - /api/auth/2fa/verify-sensitive aceita tanto TOTP (6 digitos) quanto backup codes (10 chars)
    - Backup code marca usedAt apos uso via verifyBackupCode
    - Usuarios sem 2FA: verify-sensitive retorna verified: true sem exigir codigo
    - Ambas as rotas exigem sessao autenticada (401 se nao autenticado)
  </acceptance_criteria>
  <done>API routes para setup e verificacao TOTP criadas.</done>
</task>

<task type="auto">
  <name>Task 5: Componentes UI de 2FA (Setup e Verify)</name>
  <files>apps/web/components/auth/TwoFactorSetup.tsx, apps/web/components/auth/TwoFactorVerify.tsx, apps/web/app/settings/two-factor/page.tsx</files>
  <read_first>
    - apps/web/app/api/auth/2fa/setup/route.ts
    - .planning/phases/02-auth/02-CONTEXT.md (UI minimalista Tailwind v4 only)
  </read_first>
  <action>
Criar `apps/web/components/auth/TwoFactorSetup.tsx`:

```typescript
'use client';

import { useState } from 'react';

interface TwoFactorSetupProps {
  onSuccess?: () => void;
}

interface SetupData {
  totpURI: string;
  qrCodeDataUrl: string;
  backupCodes: string[];
}

export function TwoFactorSetup({ onSuccess }: TwoFactorSetupProps) {
  const [step, setStep] = useState<'confirm' | 'qr' | 'backup' | 'verify'>('confirm');
  const [password, setPassword] = useState('');
  const [setupData, setSetupData] = useState<SetupData | null>(null);
  const [totpCode, setTotpCode] = useState('');
  const [error, setError] = useState('');
  const [isLoading, setIsLoading] = useState(false);
  const [backupCopied, setBackupCopied] = useState(false);

  async function handleStartSetup() {
    if (!password) { setError('Digite sua senha para continuar'); return; }
    setIsLoading(true);
    setError('');
    try {
      const res = await fetch('/api/auth/2fa/setup', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ password }),
      });
      if (!res.ok) { const d = await res.json(); throw new Error(d.error || 'Erro ao configurar 2FA'); }
      const data: SetupData = await res.json();
      setSetupData(data);
      setStep('qr');
    } catch (err: unknown) {
      setError(err instanceof Error ? err.message : 'Erro desconhecido');
    } finally {
      setIsLoading(false);
    }
  }

  async function handleVerifyTotp() {
    if (totpCode.length !== 6) { setError('Digite o codigo de 6 digitos'); return; }
    setIsLoading(true);
    setError('');
    try {
      const res = await fetch('/api/auth/2fa/verify-sensitive', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ code: totpCode }),
      });
      const data = await res.json();
      if (!data.verified) throw new Error('Codigo invalido');
      setStep('backup');
    } catch (err: unknown) {
      setError(err instanceof Error ? err.message : 'Erro desconhecido');
    } finally {
      setIsLoading(false);
    }
  }

  function handleCopyBackupCodes() {
    if (!setupData) return;
    navigator.clipboard.writeText(setupData.backupCodes.join('\n'));
    setBackupCopied(true);
    setTimeout(() => setBackupCopied(false), 2000);
  }

  if (step === 'confirm') {
    return (
      <div className="space-y-4">
        <p className="text-sm text-gray-600">Para ativar o 2FA, confirme sua senha:</p>
        <input
          type="password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          placeholder="Sua senha atual"
          className="w-full rounded-lg border border-gray-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-violet-500"
        />
        {error && <p className="text-sm text-red-600">{error}</p>}
        <button
          onClick={handleStartSetup}
          disabled={isLoading}
          className="w-full rounded-lg bg-violet-600 px-4 py-2 text-sm font-medium text-white hover:bg-violet-700 disabled:opacity-60"
        >
          {isLoading ? 'Gerando...' : 'Continuar'}
        </button>
      </div>
    );
  }

  if (step === 'qr' && setupData) {
    const manualCode = new URL(setupData.totpURI).searchParams.get('secret') ?? '';
    return (
      <div className="space-y-4">
        <p className="text-sm text-gray-600">
          Escaneie o QR code com seu app autenticador (Google Authenticator, Authy, etc.):
        </p>
        <div className="flex justify-center">
          <img src={setupData.qrCodeDataUrl} alt="QR Code 2FA" className="rounded-lg border border-gray-200 p-2" width={200} height={200} />
        </div>
        <details className="text-sm">
          <summary className="cursor-pointer text-violet-600">Nao consigo escanear — mostrar codigo manual</summary>
          <code className="mt-2 block break-all rounded bg-gray-100 p-2 font-mono text-xs">{manualCode}</code>
        </details>
        <div className="space-y-2">
          <label className="text-sm font-medium text-gray-700">Codigo do app (6 digitos):</label>
          <input
            type="text"
            inputMode="numeric"
            maxLength={6}
            value={totpCode}
            onChange={(e) => setTotpCode(e.target.value.replace(/\D/g, ''))}
            placeholder="000000"
            className="w-full rounded-lg border border-gray-300 px-3 py-2 text-center font-mono text-lg tracking-widest focus:outline-none focus:ring-2 focus:ring-violet-500"
          />
        </div>
        {error && <p className="text-sm text-red-600">{error}</p>}
        <button
          onClick={handleVerifyTotp}
          disabled={isLoading || totpCode.length !== 6}
          className="w-full rounded-lg bg-violet-600 px-4 py-2 text-sm font-medium text-white hover:bg-violet-700 disabled:opacity-60"
        >
          {isLoading ? 'Verificando...' : 'Verificar e ativar'}
        </button>
      </div>
    );
  }

  if (step === 'backup' && setupData) {
    return (
      <div className="space-y-4">
        <div className="rounded-lg bg-amber-50 border border-amber-200 p-4">
          <p className="text-sm font-medium text-amber-800">Salve estes codigos de recuperacao agora!</p>
          <p className="text-xs text-amber-700 mt-1">Eles nao serao mostrados novamente. Use-os se perder acesso ao seu app autenticador.</p>
        </div>
        <div className="grid grid-cols-2 gap-2">
          {setupData.backupCodes.map((code, i) => (
            <code key={i} className="rounded bg-gray-100 px-3 py-1.5 font-mono text-sm text-center">
              {code}
            </code>
          ))}
        </div>
        <button
          onClick={handleCopyBackupCodes}
          className="w-full rounded-lg border border-gray-300 px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50"
        >
          {backupCopied ? 'Copiado!' : 'Copiar todos os codigos'}
        </button>
        <button
          onClick={() => onSuccess?.()}
          className="w-full rounded-lg bg-violet-600 px-4 py-2 text-sm font-medium text-white hover:bg-violet-700"
        >
          Concluir ativacao
        </button>
      </div>
    );
  }

  return null;
}
```

Criar `apps/web/components/auth/TwoFactorVerify.tsx`:

```typescript
'use client';

import { useState } from 'react';

interface TwoFactorVerifyProps {
  onSuccess: () => void;
  onCancel?: () => void;
  title?: string;
}

export function TwoFactorVerify({
  onSuccess,
  onCancel,
  title = 'Verificacao de dois fatores',
}: TwoFactorVerifyProps) {
  const [code, setCode] = useState('');
  const [isBackupCode, setIsBackupCode] = useState(false);
  const [error, setError] = useState('');
  const [isLoading, setIsLoading] = useState(false);

  async function handleVerify() {
    if (!code.trim()) { setError('Digite o codigo'); return; }
    setIsLoading(true);
    setError('');
    try {
      const res = await fetch('/api/auth/2fa/verify-sensitive', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ code: code.trim() }),
      });
      const data = await res.json();
      if (!data.verified) throw new Error(data.error || 'Codigo invalido');
      onSuccess();
    } catch (err: unknown) {
      setError(err instanceof Error ? err.message : 'Codigo invalido');
    } finally {
      setIsLoading(false);
    }
  }

  return (
    <div className="space-y-4">
      <h3 className="text-base font-semibold text-gray-900">{title}</h3>
      {!isBackupCode ? (
        <>
          <p className="text-sm text-gray-600">Digite o codigo de 6 digitos do seu app autenticador:</p>
          <input
            type="text"
            inputMode="numeric"
            maxLength={6}
            value={code}
            onChange={(e) => setCode(e.target.value.replace(/\D/g, ''))}
            placeholder="000000"
            autoFocus
            className="w-full rounded-lg border border-gray-300 px-3 py-2 text-center font-mono text-2xl tracking-widest focus:outline-none focus:ring-2 focus:ring-violet-500"
          />
          <button onClick={() => setIsBackupCode(true)} className="text-xs text-violet-600 hover:underline">
            Usar codigo de recuperacao
          </button>
        </>
      ) : (
        <>
          <p className="text-sm text-gray-600">Digite um dos seus codigos de recuperacao:</p>
          <input
            type="text"
            value={code}
            onChange={(e) => setCode(e.target.value.toUpperCase())}
            placeholder="XXXXXXXXXX"
            maxLength={10}
            autoFocus
            className="w-full rounded-lg border border-gray-300 px-3 py-2 text-center font-mono text-lg tracking-widest focus:outline-none focus:ring-2 focus:ring-violet-500"
          />
          <button onClick={() => setIsBackupCode(false)} className="text-xs text-violet-600 hover:underline">
            Usar codigo do autenticador
          </button>
        </>
      )}
      {error && <p className="text-sm text-red-600">{error}</p>}
      <div className="flex gap-2">
        {onCancel && (
          <button onClick={onCancel} className="flex-1 rounded-lg border border-gray-300 px-4 py-2 text-sm text-gray-700 hover:bg-gray-50">
            Cancelar
          </button>
        )}
        <button
          onClick={handleVerify}
          disabled={isLoading || !code.trim()}
          className="flex-1 rounded-lg bg-violet-600 px-4 py-2 text-sm font-medium text-white hover:bg-violet-700 disabled:opacity-60"
        >
          {isLoading ? 'Verificando...' : 'Verificar'}
        </button>
      </div>
    </div>
  );
}
```

Criar `apps/web/app/settings/two-factor/page.tsx`:

```typescript
import { auth } from '@/lib/auth';
import { headers } from 'next/headers';
import { redirect } from 'next/navigation';
import { TwoFactorSetup } from '@/components/auth/TwoFactorSetup';

export default async function TwoFactorPage() {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) redirect('/login');

  return (
    <div className="mx-auto max-w-md px-4 py-8">
      <h1 className="mb-2 text-xl font-semibold text-gray-900">Autenticacao de dois fatores</h1>
      <p className="mb-6 text-sm text-gray-600">
        Proteja sua conta com um codigo gerado pelo seu app autenticador.
      </p>
      {session.user.twoFactorEnabled ? (
        <div className="rounded-lg border border-green-200 bg-green-50 p-4">
          <p className="text-sm font-medium text-green-800">2FA esta ativo na sua conta.</p>
        </div>
      ) : (
        <div className="rounded-lg border border-gray-200 bg-white p-6 shadow-sm">
          <TwoFactorSetup onSuccess={() => { /* redirect handled by client */ }} />
        </div>
      )}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/components/auth/TwoFactorSetup.tsx','utf8'); if(!c.includes('qrCodeDataUrl')) throw new Error('QR code display missing'); if(!c.includes('backupCodes')) throw new Error('backup codes display missing'); if(!c.includes('manualCode')) throw new Error('manual code missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - TwoFactorSetup tem 3 steps: confirm (password) → qr (scan + verify totp) → backup (show codes)
    - Backup codes mostrados em grid 2 colunas com botao de copiar
    - TwoFactorVerify aceita TOTP (6 digitos) e backup codes (10 chars) com toggle
    - /settings/two-factor protegida por sessao (redirect /login se nao autenticado)
    - QR code renderizado como <img src={qrCodeDataUrl}> (base64 data URL)
  </acceptance_criteria>
  <done>UI de 2FA com TwoFactorSetup (3 steps), TwoFactorVerify e pagina /settings/two-factor.</done>
</task>

<task type="auto">
  <name>Task 6: Testes Vitest para backup codes e TOTP</name>
  <files>apps/web/lib/__tests__/totp-backup.test.ts</files>
  <read_first>
    - apps/web/lib/totp-backup.ts
  </read_first>
  <action>
Criar `apps/web/lib/__tests__/totp-backup.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import bcrypt from 'bcryptjs';

// Mock do banco
const mockDelete = vi.fn().mockReturnThis();
const mockWhere = vi.fn().mockReturnThis();
const mockInsert = vi.fn().mockReturnThis();
const mockValues = vi.fn().mockResolvedValue(undefined);
const mockSelect = vi.fn().mockReturnThis();
const mockFrom = vi.fn().mockReturnThis();
const mockUpdate = vi.fn().mockReturnThis();
const mockSet = vi.fn().mockReturnThis();

vi.mock('@/lib/db', () => ({
  db: {
    delete: mockDelete,
    insert: mockInsert,
    select: mockSelect,
    update: mockUpdate,
  },
}));

vi.mock('@parabuains/db/schema', () => ({
  totpBackupCodes: { userId: 'userId', id: 'id', usedAt: 'usedAt', codeHash: 'codeHash' },
}));

vi.mock('drizzle-orm', () => ({
  eq: vi.fn(),
  and: vi.fn(),
  isNull: vi.fn(),
}));

describe('generateBackupCodes', () => {
  it('gera exatamente 8 codigos', async () => {
    const { generateBackupCodes } = await import('@/lib/totp-backup');
    const { plainCodes, hashedCodes } = await generateBackupCodes('user-123');
    expect(plainCodes).toHaveLength(8);
    expect(hashedCodes).toHaveLength(8);
  });

  it('cada codigo tem 10 caracteres', async () => {
    const { generateBackupCodes } = await import('@/lib/totp-backup');
    const { plainCodes } = await generateBackupCodes('user-123');
    for (const code of plainCodes) {
      expect(code).toHaveLength(10);
    }
  });

  it('codigos sao unicos', async () => {
    const { generateBackupCodes } = await import('@/lib/totp-backup');
    const { plainCodes } = await generateBackupCodes('user-123');
    const unique = new Set(plainCodes);
    expect(unique.size).toBe(8);
  });

  it('hashes sao diferentes dos plainCodes', async () => {
    const { generateBackupCodes } = await import('@/lib/totp-backup');
    const { plainCodes, hashedCodes } = await generateBackupCodes('user-123');
    for (let i = 0; i < 8; i++) {
      expect(hashedCodes[i]).not.toBe(plainCodes[i]);
      // Verificar que o hash e valido bcrypt
      const isValid = await bcrypt.compare(plainCodes[i], hashedCodes[i]);
      expect(isValid).toBe(true);
    }
  });
});
```

Executar:
```bash
pnpm --filter @parabuains/web test
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/__tests__/totp-backup.test.ts','utf8'); if(!c.includes('gera exatamente 8 codigos')) throw new Error('test missing'); if(!c.includes('hashes sao diferentes')) throw new Error('hash test missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - totp-backup.test.ts testa: quantidade de codigos (8), tamanho (10 chars), unicidade, validade dos hashes bcrypt
    - pnpm test passa
  </acceptance_criteria>
  <done>Testes Vitest para generateBackupCodes cobrindo quantidade, tamanho, unicidade e validade dos hashes.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-03-01 | Spoofing | TOTP codes | mitigate | Codigos TOTP com janela de 30s; better-auth verifica com tolerancia de +/-1 periodo |
| T-02-03-02 | Information Disclosure | Backup codes | mitigate | Plaintext mostrado apenas uma vez; hasheados com bcrypt cost 12; nunca armazenados em plaintext |
| T-02-03-03 | Elevation of Privilege | Sensitive ops sem 2FA | mitigate | verify-sensitive endpoint verifica twoFactorEnabled antes de processar; sem 2FA = permitido |
| T-02-03-04 | Repudiation | Backup code uso | mitigate | usedAt timestamp registrado ao consumir codigo |
| T-02-03-05 | Tampering | TOTP secret | mitigate | Secret gerenciado pelo better-auth; nunca exposto apos setup |
| T-02-03-06 | Denial of Service | Brute force TOTP | accept | Rate limiting em /api/auth/* cobrira em 02-04; TOTP tem janela curta (30s) limitando brute force |
</threat_model>

<verification>
1. `grep "totpBackupCodes" packages/db/src/schema/auth.ts` — deve retornar match
2. `grep "BACKUP_CODE_COUNT = 8" apps/web/lib/totp-backup.ts` — deve retornar match
3. `grep "BCRYPT_COST = 12" apps/web/lib/totp-backup.ts` — deve retornar match
4. `ls apps/web/app/api/auth/2fa/` — deve mostrar setup/ e verify-sensitive/
5. `ls apps/web/app/settings/two-factor/page.tsx` — deve existir
6. `pnpm --filter @parabuains/web test` — todos os testes devem passar
</verification>

<success_criteria>
- Tabela totp_backup_codes no schema Drizzle com migration gerada
- generateBackupCodes gera 8 codigos de 10 chars hasheados com bcrypt cost 12
- verifyBackupCode marca usedAt na primeira verificacao positiva (one-time use)
- /api/auth/2fa/setup retorna QR code base64, totpURI e backupCodes (plaintext, once)
- TwoFactorSetup exibe QR code + codigo manual + backup codes em 3 steps
- TwoFactorVerify aceita TOTP e backup codes com toggle visual
- Testes Vitest para backup codes passando
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-03-SUMMARY.md`
</output>
