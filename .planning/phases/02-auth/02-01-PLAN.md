---
phase: 02-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/lib/auth.ts
  - apps/web/lib/db.ts
  - apps/web/app/api/auth/[...all]/route.ts
  - packages/db/src/schema/auth.ts
  - packages/db/src/schema/index.ts
  - packages/db/drizzle.config.ts
  - apps/web/lib/email.ts
  - apps/web/lib/resend.ts
  - .env.example
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-05, AUTH-06, AUTH-09]

must_haves:
  truths:
    - "better-auth 1.6.9 instalado em apps/web"
    - "Drizzle adapter configurado com provider pg"
    - "Tabelas auth geradas via npx better-auth generate"
    - "Email verification gate ativo (requireEmailVerification: true)"
    - "Access token 15min, refresh token 30 dias"
    - "Resend SDK configurado para emails transacionais"
  artifacts:
    - path: "apps/web/lib/auth.ts"
      provides: "Instancia better-auth server-side com todos os plugins"
      contains: "betterAuth({ database: drizzleAdapter"
    - path: "apps/web/app/api/auth/[...all]/route.ts"
      provides: "Route handler que delega para better-auth"
      contains: "auth.handler(request)"
    - path: "packages/db/src/schema/auth.ts"
      provides: "Schema Drizzle gerado pelo better-auth (user, session, account, verification)"
      contains: "export const user = pgTable"
    - path: "apps/web/lib/email.ts"
      provides: "Servico de envio de email via Resend"
      contains: "resend.emails.send"
  key_links:
    - from: "apps/web/lib/auth.ts"
      to: "packages/db/src/schema/auth.ts"
      via: "drizzleAdapter(db, { schema })"
      pattern: "drizzleAdapter(db, { provider: 'pg', schema })"
    - from: "apps/web/app/api/auth/[...all]/route.ts"
      to: "apps/web/lib/auth.ts"
      via: "import { auth } from '@/lib/auth'"
      pattern: "export { GET, POST } = auth.handler"
---

<objective>
Instalar e configurar better-auth no apps/web com Drizzle adapter, gerar tabelas de autenticacao, configurar sessao (access 15min / refresh 30 dias), email verification gate, e servico Resend para emails transacionais (verificacao e reset de senha).

Output: Sistema de auth funcional com email/senha, verificacao de email obrigatoria, sessao persistente com rotation, e emails reais via Resend.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth/02-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar better-auth e resend em apps/web</name>
  <files>apps/web/package.json</files>
  <read_first>
    - .planning/research/STACK.md (versoes: better-auth 1.6.9, resend 6.12.2)
    - apps/web/package.json (dependencias existentes)
  </read_first>
  <action>
Instalar as dependencias necessarias:

```bash
pnpm add --filter @parabuains/web better-auth@1.6.9
pnpm add --filter @parabuains/web resend@6.12.2
pnpm add --filter @parabuains/web @react-email/components@1.0.12
```

Verificar que packages/db ja tem drizzle-orm instalado (dependencia do adapter):
```bash
pnpm add --filter @parabuains/db drizzle-orm@0.45.2
pnpm add --filter @parabuains/db pg@8.20.0
pnpm add --filter @parabuains/db --save-dev @types/pg
```
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/web/package.json'); if(!p.dependencies['better-auth']) throw new Error('better-auth missing'); if(!p.dependencies['resend']) throw new Error('resend missing'); console.log('OK: better-auth@'+p.dependencies['better-auth']+' resend@'+p.dependencies['resend'])"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json contem "better-auth": "1.6.9" (ou compativel com ^1.6.9)
    - apps/web/package.json contem "resend": "6.12.2"
    - pnpm install nao produz erros de resolucao
  </acceptance_criteria>
  <done>better-auth, resend e @react-email/components instalados em apps/web. drizzle-orm e pg instalados em packages/db.</done>
</task>

<task type="auto">
  <name>Task 2: Gerar schema Drizzle do better-auth</name>
  <files>packages/db/src/schema/auth.ts, packages/db/src/schema/index.ts</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (secao "DB Tables needed for better-auth")
    - packages/db/src/schema/index.ts (schema existente do Phase 1)
  </read_first>
  <action>
Criar o arquivo de schema de auth para o Drizzle. O better-auth precisa das tabelas: user, session, account, verification.

Criar `packages/db/src/schema/auth.ts`:
```typescript
import { pgTable, text, timestamp, boolean, uuid } from 'drizzle-orm/pg-core';

export const user = pgTable('user', {
  id: text('id').primaryKey(),
  name: text('name').notNull(),
  email: text('email').notNull().unique(),
  emailVerified: boolean('email_verified').notNull().default(false),
  image: text('image'),
  createdAt: timestamp('created_at').notNull().defaultNow(),
  updatedAt: timestamp('updated_at').notNull().defaultNow(),
  twoFactorEnabled: boolean('two_factor_enabled').default(false),
});

export const session = pgTable('session', {
  id: text('id').primaryKey(),
  expiresAt: timestamp('expires_at').notNull(),
  token: text('token').notNull().unique(),
  createdAt: timestamp('created_at').notNull().defaultNow(),
  updatedAt: timestamp('updated_at').notNull().defaultNow(),
  ipAddress: text('ip_address'),
  userAgent: text('user_agent'),
  userId: text('user_id')
    .notNull()
    .references(() => user.id, { onDelete: 'cascade' }),
});

export const account = pgTable('account', {
  id: text('id').primaryKey(),
  accountId: text('account_id').notNull(),
  providerId: text('provider_id').notNull(),
  userId: text('user_id')
    .notNull()
    .references(() => user.id, { onDelete: 'cascade' }),
  accessToken: text('access_token'),
  refreshToken: text('refresh_token'),
  idToken: text('id_token'),
  accessTokenExpiresAt: timestamp('access_token_expires_at'),
  refreshTokenExpiresAt: timestamp('refresh_token_expires_at'),
  scope: text('scope'),
  password: text('password'),
  createdAt: timestamp('created_at').notNull().defaultNow(),
  updatedAt: timestamp('updated_at').notNull().defaultNow(),
});

export const verification = pgTable('verification', {
  id: text('id').primaryKey(),
  identifier: text('identifier').notNull(),
  value: text('value').notNull(),
  expiresAt: timestamp('expires_at').notNull(),
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
});

export const twoFactor = pgTable('two_factor', {
  id: text('id').primaryKey(),
  secret: text('secret').notNull(),
  backupCodes: text('backup_codes').notNull(),
  userId: text('user_id')
    .notNull()
    .references(() => user.id, { onDelete: 'cascade' }),
});
```

Adicionar ao `packages/db/src/schema/index.ts`:
```typescript
export * from './auth';
// (manter exports existentes do Phase 1)
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./packages/db/src/schema/auth.ts','utf8'); if(!c.includes('pgTable')) throw new Error('pgTable not found'); if(!c.includes('export const user')) throw new Error('user table missing'); if(!c.includes('export const session')) throw new Error('session table missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/db/src/schema/auth.ts exporta: user, session, account, verification, twoFactor
    - packages/db/src/schema/index.ts re-exporta auth schema
    - Todas as tabelas usam pgTable do drizzle-orm/pg-core
  </acceptance_criteria>
  <done>Schema Drizzle de auth criado com todas as tabelas necessarias para better-auth + twoFactor.</done>
</task>

<task type="auto">
  <name>Task 3: Configurar better-auth (auth.ts) em apps/web</name>
  <files>apps/web/lib/auth.ts, apps/web/lib/db.ts</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (D-01 a D-06, secao "better-auth Configuration")
    - packages/db/src/schema/auth.ts (schema recém criado)
  </read_first>
  <action>
Criar `apps/web/lib/db.ts` (conexao Drizzle para o BFF):
```typescript
import { drizzle } from 'drizzle-orm/node-postgres';
import { Pool } from 'pg';
import * as schema from '@parabuains/db/schema';

const pool = new Pool({
  connectionString: process.env.DATABASE_URL!,
});

export const db = drizzle(pool, { schema });
export type DB = typeof db;
```

Criar `apps/web/lib/auth.ts`:
```typescript
import { betterAuth } from 'better-auth';
import { drizzleAdapter } from 'better-auth/adapters/drizzle';
import { twoFactor } from 'better-auth/plugins';
import { db } from './db';
import * as schema from '@parabuains/db/schema';
import { sendVerificationEmail, sendPasswordResetEmail } from './email';

export const auth = betterAuth({
  database: drizzleAdapter(db, {
    provider: 'pg',
    schema: {
      user: schema.user,
      session: schema.session,
      account: schema.account,
      verification: schema.verification,
    },
  }),
  emailAndPassword: {
    enabled: true,
    requireEmailVerification: true,
    minPasswordLength: 8,
    sendResetPassword: async ({ user, url }) => {
      await sendPasswordResetEmail({ to: user.email, url });
    },
  },
  emailVerification: {
    sendVerificationEmail: async ({ user, url }) => {
      await sendVerificationEmail({ to: user.email, url });
    },
    expiresIn: 60 * 60 * 24, // 24 horas
  },
  session: {
    expiresIn: 60 * 60 * 24 * 30,      // refresh: 30 dias
    updateAge: 60 * 15,                  // access: 15 minutos
    cookieCache: {
      enabled: true,
      maxAge: 60 * 15,
    },
  },
  plugins: [
    twoFactor({
      totpOptions: {
        digits: 6,
        period: 30,
      },
      backupCodeOptions: {
        amount: 8,
        length: 10,
      },
    }),
  ],
  trustedOrigins: [process.env.NEXTAUTH_URL ?? 'http://localhost:3000'],
});

export type Session = typeof auth.$Infer.Session;
export type User = typeof auth.$Infer.Session.user;
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/auth.ts','utf8'); if(!c.includes('betterAuth')) throw new Error('betterAuth not found'); if(!c.includes('drizzleAdapter')) throw new Error('drizzleAdapter missing'); if(!c.includes('requireEmailVerification: true')) throw new Error('email verification gate missing'); if(!c.includes('twoFactor')) throw new Error('2FA plugin missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/auth.ts contem drizzleAdapter com provider pg
    - requireEmailVerification: true esta configurado
    - session.expiresIn = 2592000 (30 dias em segundos)
    - plugin twoFactor() incluido com backupCodeOptions.amount: 8
    - Session e User types exportados
  </acceptance_criteria>
  <done>better-auth configurado com Drizzle adapter, email verification gate, session 15min/30dias, 2FA plugin com 8 backup codes.</done>
</task>

<task type="auto">
  <name>Task 4: Route handler better-auth no Next.js</name>
  <files>apps/web/app/api/auth/[...all]/route.ts</files>
  <read_first>
    - apps/web/lib/auth.ts (instancia recém criada)
    - .planning/phases/02-auth/02-CONTEXT.md (arquitetura BFF)
  </read_first>
  <action>
Criar o catch-all route handler para better-auth em `apps/web/app/api/auth/[...all]/route.ts`:

```typescript
import { auth } from '@/lib/auth';
import { toNextJsHandler } from 'better-auth/next-js';

export const { GET, POST } = toNextJsHandler(auth);
```

Este arquivo captura todas as rotas `/api/auth/*` e as delega para o better-auth handler.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/app/api/auth/[...all]/route.ts','utf8'); if(!c.includes('toNextJsHandler')) throw new Error('handler missing'); if(!c.includes('export const { GET, POST }')) throw new Error('exports missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em apps/web/app/api/auth/[...all]/route.ts
    - Exporta GET e POST via toNextJsHandler(auth)
    - Importa auth de @/lib/auth
  </acceptance_criteria>
  <done>Route handler better-auth registrado no App Router do Next.js.</done>
</task>

<task type="auto">
  <name>Task 5: Servico de email via Resend</name>
  <files>apps/web/lib/email.ts, apps/web/lib/resend.ts</files>
  <read_first>
    - .planning/phases/02-auth/02-CONTEXT.md (D-12: Forgot Password, secao "Email Sending in Phase 2")
    - .planning/research/STACK.md (Resend 6.12.2)
  </read_first>
  <action>
Criar `apps/web/lib/resend.ts` (instancia singleton do Resend):
```typescript
import { Resend } from 'resend';

if (!process.env.RESEND_API_KEY) {
  console.warn('[Resend] RESEND_API_KEY not set — emails will not be sent');
}

export const resend = new Resend(process.env.RESEND_API_KEY ?? 're_placeholder');
export const FROM_EMAIL = process.env.RESEND_FROM_EMAIL ?? 'noreply@parabuains.com';
```

Criar `apps/web/lib/email.ts` (servico de email tipado):
```typescript
import { resend, FROM_EMAIL } from './resend';

interface VerificationEmailParams {
  to: string;
  url: string;
}

interface PasswordResetEmailParams {
  to: string;
  url: string;
}

export async function sendVerificationEmail({ to, url }: VerificationEmailParams): Promise<void> {
  const { error } = await resend.emails.send({
    from: FROM_EMAIL,
    to,
    subject: 'Verifique seu e-mail — Parabuains',
    html: `
      <div style="font-family: sans-serif; max-width: 600px; margin: 0 auto;">
        <h2>Bem-vindo ao Parabuains!</h2>
        <p>Clique no link abaixo para verificar seu e-mail:</p>
        <a href="${url}" style="display:inline-block;padding:12px 24px;background:#7c3aed;color:#fff;border-radius:8px;text-decoration:none;">
          Verificar e-mail
        </a>
        <p style="color:#666;font-size:12px;margin-top:24px;">
          Este link expira em 24 horas. Se nao solicitou o cadastro, ignore este e-mail.
        </p>
      </div>
    `,
  });

  if (error) {
    console.error('[Email] sendVerificationEmail failed:', error);
    throw new Error(`Failed to send verification email: ${error.message}`);
  }
}

export async function sendPasswordResetEmail({ to, url }: PasswordResetEmailParams): Promise<void> {
  const { error } = await resend.emails.send({
    from: FROM_EMAIL,
    to,
    subject: 'Redefinir senha — Parabuains',
    html: `
      <div style="font-family: sans-serif; max-width: 600px; margin: 0 auto;">
        <h2>Redefinir sua senha</h2>
        <p>Clique no link abaixo para redefinir sua senha:</p>
        <a href="${url}" style="display:inline-block;padding:12px 24px;background:#7c3aed;color:#fff;border-radius:8px;text-decoration:none;">
          Redefinir senha
        </a>
        <p style="color:#666;font-size:12px;margin-top:24px;">
          Este link expira em 1 hora. Se nao solicitou a redefinicao, ignore este e-mail.
        </p>
      </div>
    `,
  });

  if (error) {
    console.error('[Email] sendPasswordResetEmail failed:', error);
    throw new Error(`Failed to send password reset email: ${error.message}`);
  }
}
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/email.ts','utf8'); if(!c.includes('resend.emails.send')) throw new Error('Resend send missing'); if(!c.includes('sendVerificationEmail')) throw new Error('sendVerificationEmail missing'); if(!c.includes('sendPasswordResetEmail')) throw new Error('sendPasswordResetEmail missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/email.ts exporta sendVerificationEmail e sendPasswordResetEmail
    - Ambas as funcoes usam resend.emails.send
    - Erros sao logados e re-lancados com mensagem descritiva
    - FROM_EMAIL usa variavel de ambiente RESEND_FROM_EMAIL com fallback
  </acceptance_criteria>
  <done>Servico de email Resend configurado com funcoes para verificacao de conta e reset de senha.</done>
</task>

<task type="auto">
  <name>Task 6: Migration Drizzle — tabelas de auth</name>
  <files>packages/db/migrations/ (gerado), packages/db/drizzle.config.ts</files>
  <read_first>
    - packages/db/drizzle.config.ts (config existente do Phase 1)
    - packages/db/src/schema/auth.ts (schema recém criado)
  </read_first>
  <action>
Verificar que drizzle.config.ts inclui o schema de auth e executar a geracao da migration:

O drizzle.config.ts deve apontar para `packages/db/src/schema/index.ts` (que ja exporta auth.ts).

Gerar a migration:
```bash
pnpm --filter @parabuains/db db:generate
```

A migration deve criar as tabelas: user, session, account, verification, two_factor.

Se o banco de dados PostgreSQL nao estiver disponivel localmente (esperado em CI), usar `db:push` como alternativa de verificacao de schema valido:
```bash
# Apenas gerar SQL — nao requer DB conectado
pnpm --filter @parabuains/db db:generate
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=fs.readdirSync('./packages/db/migrations').filter(f=>f.endsWith('.sql')); if(files.length===0) { console.log('WARNING: No migration files found — may need db:generate'); } else { const sql=fs.readFileSync('./packages/db/migrations/'+files[files.length-1],'utf8'); console.log('Migration found:', files[files.length-1]); }"</automated>
  </verify>
  <acceptance_criteria>
    - Existe ao menos 1 arquivo .sql em packages/db/migrations/ referente as tabelas de auth
    - SQL inclui CREATE TABLE "user" e CREATE TABLE "session"
  </acceptance_criteria>
  <done>Migration Drizzle gerada para as tabelas de autenticacao do better-auth.</done>
</task>

<task type="auto">
  <name>Task 7: Testes Vitest para auth e email</name>
  <files>apps/web/lib/__tests__/auth.test.ts, apps/web/lib/__tests__/email.test.ts</files>
  <read_first>
    - apps/web/lib/auth.ts
    - apps/web/lib/email.ts
  </read_first>
  <action>
Criar `apps/web/lib/__tests__/auth.test.ts`:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Mock do modulo db para evitar conexao real
vi.mock('@/lib/db', () => ({
  db: {},
}));

// Mock do better-auth
vi.mock('better-auth', () => ({
  betterAuth: vi.fn(() => ({
    handler: vi.fn(),
    $Infer: { Session: { user: {} } },
  })),
}));

vi.mock('better-auth/adapters/drizzle', () => ({
  drizzleAdapter: vi.fn(() => ({})),
}));

vi.mock('better-auth/plugins', () => ({
  twoFactor: vi.fn(() => ({})),
}));

vi.mock('@/lib/email', () => ({
  sendVerificationEmail: vi.fn(),
  sendPasswordResetEmail: vi.fn(),
}));

describe('auth configuration', () => {
  it('exports auth instance', async () => {
    const { auth } = await import('@/lib/auth');
    expect(auth).toBeDefined();
  });

  it('exports Session and User types', async () => {
    const module = await import('@/lib/auth');
    // Type exports existem em runtime como undefined — verificamos que o modulo carrega
    expect(module).toHaveProperty('auth');
  });
});
```

Criar `apps/web/lib/__tests__/email.test.ts`:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

const mockSend = vi.fn();
vi.mock('@/lib/resend', () => ({
  resend: { emails: { send: mockSend } },
  FROM_EMAIL: 'noreply@parabuains.com',
}));

describe('sendVerificationEmail', () => {
  beforeEach(() => {
    mockSend.mockReset();
  });

  it('envia email de verificacao com sucesso', async () => {
    mockSend.mockResolvedValue({ data: { id: 'test-id' }, error: null });
    const { sendVerificationEmail } = await import('@/lib/email');
    await expect(
      sendVerificationEmail({ to: 'user@example.com', url: 'https://parabuains.com/verify?token=abc' })
    ).resolves.not.toThrow();
    expect(mockSend).toHaveBeenCalledWith(
      expect.objectContaining({
        to: 'user@example.com',
        subject: expect.stringContaining('Verifique'),
      })
    );
  });

  it('lanca erro se Resend retorna erro', async () => {
    mockSend.mockResolvedValue({ data: null, error: { message: 'API Error' } });
    const { sendVerificationEmail } = await import('@/lib/email');
    await expect(
      sendVerificationEmail({ to: 'user@example.com', url: 'https://parabuains.com/verify?token=abc' })
    ).rejects.toThrow('Failed to send verification email');
  });
});

describe('sendPasswordResetEmail', () => {
  beforeEach(() => {
    mockSend.mockReset();
  });

  it('envia email de reset com sucesso', async () => {
    mockSend.mockResolvedValue({ data: { id: 'test-id' }, error: null });
    const { sendPasswordResetEmail } = await import('@/lib/email');
    await expect(
      sendPasswordResetEmail({ to: 'user@example.com', url: 'https://parabuains.com/reset?token=xyz' })
    ).resolves.not.toThrow();
  });

  it('lanca erro se Resend retorna erro', async () => {
    mockSend.mockResolvedValue({ data: null, error: { message: 'Rate limited' } });
    const { sendPasswordResetEmail } = await import('@/lib/email');
    await expect(
      sendPasswordResetEmail({ to: 'user@example.com', url: 'https://parabuains.com/reset?token=xyz' })
    ).rejects.toThrow('Failed to send password reset email');
  });
});
```

Executar os testes:
```bash
pnpm --filter @parabuains/web test
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('./apps/web/lib/__tests__/email.test.ts','utf8'); if(!c.includes('sendVerificationEmail')) throw new Error('test missing'); if(!c.includes('lanca erro se Resend')) throw new Error('error test missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/lib/__tests__/auth.test.ts existe com testes de configuracao
    - apps/web/lib/__tests__/email.test.ts existe com testes de sucesso e erro
    - pnpm test passa sem erros
  </acceptance_criteria>
  <done>Testes Vitest cobrindo configuracao do auth e servico de email com mocks.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-01-01 | Spoofing | better-auth session | mitigate | HttpOnly + Secure + SameSite=Strict cookies; token rotation em cada uso |
| T-02-01-02 | Information Disclosure | Email verification | mitigate | Links de verificacao com token opaco; expiracao em 24h |
| T-02-01-03 | Tampering | Drizzle schema | mitigate | Schema versionado em migrations; nenhuma DDL manual |
| T-02-01-04 | Elevation of Privilege | Email verification gate | mitigate | requireEmailVerification: true bloqueia acesso a rotas protegidas ate verificacao |
| T-02-01-05 | Information Disclosure | RESEND_API_KEY | mitigate | Variavel de ambiente; nunca no codigo; .env no .gitignore |
| T-02-01-06 | Denial of Service | Resend rate limits | accept | Resend tem limites por plano; erros sao logados; fase 6 adiciona queue |
</threat_model>

<verification>
1. `node -e "const p=require('./apps/web/package.json'); console.log('better-auth:', p.dependencies['better-auth'])"` — deve mostrar 1.6.9
2. `grep -r "requireEmailVerification: true" apps/web/lib/auth.ts` — deve retornar match
3. `grep "twoFactor()" apps/web/lib/auth.ts` — deve retornar match
4. `ls packages/db/migrations/*.sql` — deve listar ao menos 1 arquivo
5. `grep "export const user" packages/db/src/schema/auth.ts` — deve retornar match
6. `pnpm --filter @parabuains/web test` — todos os testes devem passar
</verification>

<success_criteria>
- better-auth 1.6.9 instalado e configurado com Drizzle adapter pg
- Email verification gate ativo (requireEmailVerification: true)
- Session: access 15min (cookieCache.maxAge), refresh 30 dias (expiresIn)
- twoFactor plugin configurado com 8 backup codes
- Resend SDK configurado com sendVerificationEmail e sendPasswordResetEmail
- Route handler /api/auth/[...all] registrado no Next.js App Router
- Migration Drizzle gerada para tabelas de auth
- Testes Vitest passando para email service
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth/02-01-SUMMARY.md`
</output>
