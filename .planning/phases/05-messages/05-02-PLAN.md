---
phase: 05-messages
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - apps/web/app/[username]/page.tsx
  - apps/web/app/[username]/WallSection.tsx
  - apps/web/app/[username]/ComposeMessageModal.tsx
  - apps/web/app/settings/wall/page.tsx
  - apps/web/app/settings/wall/pending/page.tsx
  - apps/web/lib/api/messages.ts
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-04, MSG-05, MSG-06, MSG-07]

must_haves:
  truths:
    - "WallSection renderiza em app/[username]/page.tsx abaixo do perfil"
    - "ComposeMessageModal tem seletor de tipo (public/private/anonymous), contador 500 chars e honeypot"
    - "Mensagens anonimas exibem 'Anonimo' — nunca nome ou avatar real"
    - "Botao deletar visivel apenas para owner ou author nao-anonimo"
    - "Botao reportar visivel para outros usuarios autenticados"
    - "Wall settings em app/settings/wall/page.tsx"
    - "Pending approval inbox em app/settings/wall/pending/page.tsx"
  artifacts:
    - path: "apps/web/app/[username]/WallSection.tsx"
      provides: "Secao de mural na pagina de perfil"
      contains: "ComposeMessageModal"
    - path: "apps/web/app/[username]/ComposeMessageModal.tsx"
      provides: "Modal de composicao com tipo, contador, honeypot"
      contains: "honeypot"
    - path: "apps/web/app/settings/wall/page.tsx"
      provides: "Configuracoes do mural (who_can_post, anonymous, approval)"
      contains: "wallWhoCanPost"
    - path: "apps/web/app/settings/wall/pending/page.tsx"
      provides: "Inbox de mensagens pendentes para aprovacao"
      contains: "approve"
  key_links:
    - from: "WallSection.tsx"
      to: "ComposeMessageModal.tsx"
      via: "React component import"
      pattern: "ComposeMessageModal"
    - from: "app/[username]/page.tsx"
      to: "WallSection.tsx"
      via: "RSC import"
      pattern: "WallSection"
    - from: "WallSection.tsx"
      to: "apps/web/lib/api/messages.ts"
      via: "API client calls"
      pattern: "getWallMessages"
---

<objective>
Implementar a UI do mural de mensagens no Next.js: secao de mural na pagina de perfil (WallSection com lista de mensagens e botao de composicao), modal de composicao (tipo de mensagem, contador de 500 chars, honeypot anti-bot), exibicao segura de mensagens anonimas (sem revelar identidade), botoes de acao contextuais (deletar/reportar), e paginas de configuracao do mural e inbox de aprovacao.

Purpose: Conectar o frontend ao backend (Plan 01) e entregar a experiencia completa do mural para usuarios.
Output: Mural funcionando end-to-end — postar, visualizar, deletar, reportar, configurar, e aprovar mensagens.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-messages/05-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client para mensagens (DAL)</name>
  <files>apps/web/lib/api/messages.ts</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (DAL Pattern section — como BFF chama Fastify API)
    - apps/web/lib/api/ (verificar como outros modulos fazem chamadas ao Fastify API)
    - apps/web/lib/dal.ts (ou equivalente — verificar padrao de autenticacao nas chamadas)
    - .planning/phases/05-messages/05-CONTEXT.md (specifics: Wall API Endpoints)
  </read_first>
  <action>
**`apps/web/lib/api/messages.ts`:**
```typescript
import 'server-only';
import { getSession } from '../auth/session'; // adjust import per actual auth setup

const API_BASE = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001';

async function apiFetch(path: string, options: RequestInit = {}, token?: string) {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(token ? { Authorization: `Bearer ${token}` } : {}),
  };
  const res = await fetch(`${API_BASE}${path}`, { ...options, headers });
  if (!res.ok) {
    const error = await res.json().catch(() => ({ error: res.statusText }));
    throw Object.assign(new Error(error.error ?? 'API error'), { status: res.status });
  }
  if (res.status === 204) return null;
  return res.json();
}

export interface WallMessageData {
  id: string;
  profileId: string;
  authorId?: string;
  author?: {
    id: string;
    username: string;
    displayName: string;
    avatarUrl: string | null;
  };
  isAnonymous: boolean;
  isPrivate: boolean;
  content: string;
  status: 'pending' | 'published' | 'rejected';
  createdAt: string;
}

export interface WallSettingsData {
  wallWhoCanPost: 'friends' | 'authenticated';
  wallAllowAnonymous: boolean;
  wallRequireApproval: boolean;
}

/** Get published messages on a user's wall (public endpoint) */
export async function getWallMessages(username: string): Promise<WallMessageData[]> {
  const session = await getSession().catch(() => null);
  const token = session?.serviceToken;
  const data = await apiFetch(`/v1/users/${username}/wall`, {}, token);
  return data?.messages ?? [];
}

/** Post a message on a user's wall */
export async function postWallMessage(
  username: string,
  content: string,
  type: 'public' | 'private' | 'anonymous',
): Promise<WallMessageData> {
  const session = await getSession();
  return apiFetch(
    `/v1/users/${username}/wall`,
    {
      method: 'POST',
      body: JSON.stringify({ content, type }),
    },
    session.serviceToken,
  );
}

/** Delete a message */
export async function deleteWallMessage(messageId: string): Promise<void> {
  const session = await getSession();
  await apiFetch(
    `/v1/messages/${messageId}`,
    { method: 'DELETE' },
    session.serviceToken,
  );
}

/** Approve a pending message */
export async function approveMessage(messageId: string): Promise<WallMessageData> {
  const session = await getSession();
  return apiFetch(
    `/v1/messages/${messageId}/approve`,
    { method: 'PATCH' },
    session.serviceToken,
  );
}

/** Reject a pending message */
export async function rejectMessage(messageId: string): Promise<void> {
  const session = await getSession();
  await apiFetch(
    `/v1/messages/${messageId}/reject`,
    { method: 'PATCH' },
    session.serviceToken,
  );
}

/** Report a message */
export async function reportMessage(
  messageId: string,
  reason: 'spam' | 'harassment' | 'inappropriate' | 'other',
): Promise<void> {
  const session = await getSession();
  await apiFetch(
    `/v1/messages/${messageId}/report`,
    { method: 'POST', body: JSON.stringify({ reason }) },
    session.serviceToken,
  );
}

/** Get pending messages awaiting approval */
export async function getPendingMessages(): Promise<WallMessageData[]> {
  const session = await getSession();
  const data = await apiFetch('/v1/users/me/wall/pending', {}, session.serviceToken);
  return data?.messages ?? [];
}

/** Update wall settings */
export async function updateWallSettings(
  settings: Partial<WallSettingsData>,
): Promise<void> {
  const session = await getSession();
  await apiFetch(
    '/v1/users/me/wall/settings',
    { method: 'PUT', body: JSON.stringify(settings) },
    session.serviceToken,
  );
}
```
  </action>
  <acceptance_criteria>
    - `apps/web/lib/api/messages.ts` existe e exporta todas as funcoes: getWallMessages, postWallMessage, deleteWallMessage, approveMessage, rejectMessage, reportMessage, getPendingMessages, updateWallSettings
    - Arquivo comeca com `import 'server-only'` (DAL pattern)
    - Funcao `postWallMessage` inclui campo `type` no body
    - Funcoes autenticadas buscam session antes de chamar API
    - Interface `WallMessageData` tem `authorId?` como campo opcional (compativel com anonimato)
  </acceptance_criteria>
  <done>DAL de mensagens criado com todas as funcoes necessarias, seguindo o padrao server-only da arquitetura BFF.</done>
</task>

<task type="auto">
  <name>Task 2: ComposeMessageModal — modal de composicao</name>
  <files>apps/web/app/[username]/ComposeMessageModal.tsx</files>
  <read_first>
    - .planning/phases/05-messages/05-CONTEXT.md (D-01: tipos, D-04: anonymous check, D-08: 500 chars)
    - apps/web/app/[username]/ (verificar estrutura da pagina de perfil existente)
    - apps/web/components/ (verificar componentes UI reutilizaveis — Button, Modal, etc.)
  </read_first>
  <action>
**`apps/web/app/[username]/ComposeMessageModal.tsx`:**
```typescript
'use client';

import { useState, useRef } from 'react';

interface ComposeMessageModalProps {
  username: string;
  wallAllowAnonymous: boolean;
  onClose: () => void;
  onSuccess: () => void;
}

const MAX_CHARS = 500;

const MESSAGE_TYPES = [
  { value: 'public', label: 'Publica', description: 'Visivel para todos' },
  { value: 'private', label: 'Privada', description: 'Apenas para o aniversariante' },
  { value: 'anonymous', label: 'Anonima', description: 'Sem revelar sua identidade' },
] as const;

type MessageType = 'public' | 'private' | 'anonymous';

export function ComposeMessageModal({
  username,
  wallAllowAnonymous,
  onClose,
  onSuccess,
}: ComposeMessageModalProps) {
  const [content, setContent] = useState('');
  const [type, setType] = useState<MessageType>('public');
  const [isSubmitting, setIsSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);
  // Honeypot ref — hidden from users, visible to bots
  const honeypotRef = useRef<HTMLInputElement>(null);

  const charsRemaining = MAX_CHARS - content.length;
  const isOverLimit = charsRemaining < 0;

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (isOverLimit || !content.trim()) return;

    setIsSubmitting(true);
    setError(null);

    try {
      const res = await fetch(`/api/wall/${username}/messages`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          content: content.trim(),
          type,
          // Include honeypot — server checks if filled
          website: honeypotRef.current?.value ?? '',
        }),
      });

      if (!res.ok) {
        const data = await res.json();
        setError(data.error ?? 'Erro ao enviar mensagem');
        return;
      }

      onSuccess();
    } catch {
      setError('Erro de conexao. Tente novamente.');
    } finally {
      setIsSubmitting(false);
    }
  }

  const availableTypes = wallAllowAnonymous
    ? MESSAGE_TYPES
    : MESSAGE_TYPES.filter((t) => t.value !== 'anonymous');

  return (
    <div
      role="dialog"
      aria-modal="true"
      aria-labelledby="compose-modal-title"
      className="fixed inset-0 z-50 flex items-center justify-center bg-black/50"
      onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}
    >
      <div className="w-full max-w-lg rounded-xl bg-white p-6 shadow-xl dark:bg-zinc-900">
        <div className="mb-4 flex items-center justify-between">
          <h2 id="compose-modal-title" className="text-lg font-semibold">
            Deixar mensagem para @{username}
          </h2>
          <button
            onClick={onClose}
            aria-label="Fechar modal"
            className="rounded-md p-1 hover:bg-zinc-100 dark:hover:bg-zinc-800"
          >
            ×
          </button>
        </div>

        <form onSubmit={handleSubmit} className="space-y-4">
          {/* Honeypot — hidden visually but accessible to bots */}
          <div aria-hidden="true" className="absolute -left-[9999px] -top-[9999px]">
            <label htmlFor="website">Website</label>
            <input
              id="website"
              name="website"
              type="text"
              tabIndex={-1}
              autoComplete="off"
              ref={honeypotRef}
            />
          </div>

          {/* Message type selector */}
          <fieldset>
            <legend className="mb-2 text-sm font-medium text-zinc-700 dark:text-zinc-300">
              Tipo de mensagem
            </legend>
            <div className="flex gap-2">
              {availableTypes.map((t) => (
                <label
                  key={t.value}
                  className={`flex flex-1 cursor-pointer flex-col items-center rounded-lg border p-3 text-center transition-colors ${
                    type === t.value
                      ? 'border-indigo-500 bg-indigo-50 text-indigo-700 dark:bg-indigo-950 dark:text-indigo-300'
                      : 'border-zinc-200 hover:border-zinc-300 dark:border-zinc-700'
                  }`}
                >
                  <input
                    type="radio"
                    name="type"
                    value={t.value}
                    checked={type === t.value}
                    onChange={() => setType(t.value)}
                    className="sr-only"
                  />
                  <span className="text-sm font-medium">{t.label}</span>
                  <span className="mt-0.5 text-xs text-zinc-500 dark:text-zinc-400">
                    {t.description}
                  </span>
                </label>
              ))}
            </div>
          </fieldset>

          {/* Message content */}
          <div>
            <label
              htmlFor="message-content"
              className="mb-1 block text-sm font-medium text-zinc-700 dark:text-zinc-300"
            >
              Mensagem
            </label>
            <textarea
              id="message-content"
              value={content}
              onChange={(e) => setContent(e.target.value)}
              rows={4}
              maxLength={MAX_CHARS + 1} // Allow typing past limit to show error, but block submit
              placeholder="Escreva sua mensagem aqui..."
              className={`w-full resize-none rounded-lg border px-3 py-2 text-sm focus:outline-none focus:ring-2 ${
                isOverLimit
                  ? 'border-red-400 focus:ring-red-400'
                  : 'border-zinc-300 focus:ring-indigo-500 dark:border-zinc-600'
              } dark:bg-zinc-800 dark:text-zinc-100`}
              disabled={isSubmitting}
              aria-describedby="char-counter"
            />
            <div
              id="char-counter"
              className={`mt-1 text-right text-xs ${
                isOverLimit
                  ? 'font-medium text-red-500'
                  : charsRemaining <= 50
                  ? 'text-amber-500'
                  : 'text-zinc-400'
              }`}
              aria-live="polite"
            >
              {isOverLimit
                ? `${Math.abs(charsRemaining)} caracteres acima do limite`
                : `${charsRemaining} caracteres restantes`}
            </div>
          </div>

          {error && (
            <p role="alert" className="rounded-lg bg-red-50 px-3 py-2 text-sm text-red-600 dark:bg-red-950 dark:text-red-400">
              {error}
            </p>
          )}

          <div className="flex justify-end gap-3">
            <button
              type="button"
              onClick={onClose}
              className="rounded-lg border border-zinc-300 px-4 py-2 text-sm hover:bg-zinc-50 dark:border-zinc-600 dark:hover:bg-zinc-800"
            >
              Cancelar
            </button>
            <button
              type="submit"
              disabled={isSubmitting || isOverLimit || !content.trim()}
              className="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-medium text-white hover:bg-indigo-700 disabled:cursor-not-allowed disabled:opacity-50"
            >
              {isSubmitting ? 'Enviando...' : 'Enviar mensagem'}
            </button>
          </div>
        </form>
      </div>
    </div>
  );
}
```
  </action>
  <acceptance_criteria>
    - `ComposeMessageModal.tsx` tem `'use client'` no topo
    - Componente renderiza campo `website` como honeypot com `aria-hidden="true"` e `tabIndex={-1}`
    - Componente tem seletor de tipo com 3 opcoes: public, private, anonymous
    - Quando `wallAllowAnonymous = false`, opcao anonymous esta ausente (filtrada)
    - Contador de caracteres exibe contagem regressiva a partir de 500
    - Contador muda de cor para vermelho quando acima do limite
    - Botao submit desabilitado quando `isOverLimit || !content.trim()`
    - Textarea tem `maxLength={501}` (MAX_CHARS + 1) para permitir mostrar erro visual
  </acceptance_criteria>
  <done>Modal de composicao implementado com tipo de mensagem, contador de 500 chars, honeypot anti-bot, e feedback visual de limite.</done>
</task>

<task type="auto">
  <name>Task 3: WallSection — lista de mensagens com acoes</name>
  <files>apps/web/app/[username]/WallSection.tsx</files>
  <read_first>
    - .planning/phases/05-messages/05-CONTEXT.md (D-09: who reads what, D-10: deletion, D-01: tipos)
    - apps/web/app/[username]/ComposeMessageModal.tsx (acabou de ser criado)
    - apps/web/lib/api/messages.ts (funcoes de API)
    - apps/web/app/[username]/page.tsx (verificar props e estrutura atual da pagina de perfil)
  </read_first>
  <action>
**`apps/web/app/[username]/WallSection.tsx`:**
```typescript
'use client';

import { useState } from 'react';
import type { WallMessageData } from '../../lib/api/messages';
import { ComposeMessageModal } from './ComposeMessageModal';

interface WallSectionProps {
  username: string;
  messages: WallMessageData[];
  currentUserId: string | null;
  profileOwnerId: string;
  wallAllowAnonymous: boolean;
  canPost: boolean; // pre-computed by server: respects wallWhoCanPost
}

interface MessageCardProps {
  message: WallMessageData;
  currentUserId: string | null;
  profileOwnerId: string;
  onDelete: (id: string) => void;
  onReport: (id: string) => void;
}

function MessageCard({
  message,
  currentUserId,
  profileOwnerId,
  onDelete,
  onReport,
}: MessageCardProps) {
  const isOwner = currentUserId === profileOwnerId;
  // Anonymous authors cannot delete their own message (identity hidden)
  const isAuthor = !message.isAnonymous && currentUserId === message.authorId;
  const canDelete = isOwner || isAuthor;
  // Users can report messages that are not theirs and not already reported
  const canReport = currentUserId !== null && !isOwner && !isAuthor;

  // CRITICAL: Never render real author info for anonymous messages
  const displayName = message.isAnonymous ? 'Anonimo' : (message.author?.displayName ?? 'Usuario');
  const avatarUrl = message.isAnonymous ? null : (message.author?.avatarUrl ?? null);
  const showAuthorLink = !message.isAnonymous && message.author;

  return (
    <article className="rounded-lg border border-zinc-200 bg-white p-4 dark:border-zinc-700 dark:bg-zinc-800/50">
      <div className="flex items-start gap-3">
        {/* Avatar */}
        <div className="flex-shrink-0">
          {avatarUrl ? (
            <img
              src={avatarUrl}
              alt={`Avatar de ${displayName}`}
              className="h-9 w-9 rounded-full object-cover"
            />
          ) : (
            <div
              className="flex h-9 w-9 items-center justify-center rounded-full bg-zinc-200 text-sm font-medium text-zinc-600 dark:bg-zinc-700 dark:text-zinc-300"
              aria-hidden="true"
            >
              {message.isAnonymous ? '?' : displayName[0]?.toUpperCase()}
            </div>
          )}
        </div>

        <div className="min-w-0 flex-1">
          {/* Author name */}
          <div className="flex items-center gap-2">
            {showAuthorLink ? (
              <a
                href={`/${message.author!.username}`}
                className="text-sm font-medium text-zinc-900 hover:underline dark:text-zinc-100"
              >
                {displayName}
              </a>
            ) : (
              <span className="text-sm font-medium text-zinc-500 dark:text-zinc-400">
                {displayName}
              </span>
            )}
            {message.isPrivate && (
              <span className="rounded-full bg-zinc-100 px-2 py-0.5 text-xs text-zinc-500 dark:bg-zinc-700">
                Privada
              </span>
            )}
          </div>

          {/* Message content */}
          <p className="mt-1 whitespace-pre-wrap break-words text-sm text-zinc-700 dark:text-zinc-300">
            {message.content}
          </p>

          {/* Timestamp + actions */}
          <div className="mt-2 flex items-center gap-3">
            <time
              dateTime={message.createdAt}
              className="text-xs text-zinc-400"
            >
              {new Date(message.createdAt).toLocaleDateString('pt-BR', {
                day: 'numeric',
                month: 'short',
                year: 'numeric',
              })}
            </time>

            {canDelete && (
              <button
                onClick={() => onDelete(message.id)}
                className="text-xs text-red-500 hover:underline"
                aria-label="Deletar mensagem"
              >
                Deletar
              </button>
            )}

            {canReport && (
              <button
                onClick={() => onReport(message.id)}
                className="text-xs text-zinc-400 hover:text-zinc-600 hover:underline dark:hover:text-zinc-300"
                aria-label="Reportar mensagem"
              >
                Reportar
              </button>
            )}
          </div>
        </div>
      </div>
    </article>
  );
}

export function WallSection({
  username,
  messages: initialMessages,
  currentUserId,
  profileOwnerId,
  wallAllowAnonymous,
  canPost,
}: WallSectionProps) {
  const [messages, setMessages] = useState(initialMessages);
  const [isComposing, setIsComposing] = useState(false);
  const [reportingId, setReportingId] = useState<string | null>(null);

  async function handleDelete(messageId: string) {
    if (!confirm('Deletar esta mensagem?')) return;
    try {
      const res = await fetch(`/api/messages/${messageId}`, { method: 'DELETE' });
      if (res.ok) {
        setMessages((prev) => prev.filter((m) => m.id !== messageId));
      }
    } catch {
      alert('Erro ao deletar mensagem.');
    }
  }

  async function handleReport(messageId: string) {
    const reason = prompt('Motivo do report: spam, harassment, inappropriate, ou other');
    const validReasons = ['spam', 'harassment', 'inappropriate', 'other'];
    if (!reason || !validReasons.includes(reason)) return;

    try {
      const res = await fetch(`/api/messages/${messageId}/report`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ reason }),
      });
      if (res.ok) {
        alert('Mensagem reportada. Obrigado!');
      }
    } catch {
      alert('Erro ao reportar mensagem.');
    }
    setReportingId(null);
  }

  function handleComposeSuccess() {
    setIsComposing(false);
    // Reload messages — in production use router.refresh() or revalidatePath
    window.location.reload();
  }

  return (
    <section aria-labelledby="wall-section-title" className="mt-8">
      <div className="mb-4 flex items-center justify-between">
        <h2 id="wall-section-title" className="text-xl font-semibold text-zinc-900 dark:text-zinc-100">
          Mural
        </h2>
        {canPost && currentUserId && (
          <button
            onClick={() => setIsComposing(true)}
            className="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-medium text-white hover:bg-indigo-700"
          >
            Escrever mensagem
          </button>
        )}
      </div>

      {messages.length === 0 ? (
        <div className="rounded-xl border border-dashed border-zinc-300 p-8 text-center dark:border-zinc-700">
          <p className="text-zinc-500 dark:text-zinc-400">
            Nenhuma mensagem ainda. Seja o primeiro a deixar uma mensagem!
          </p>
        </div>
      ) : (
        <div className="space-y-3">
          {messages.map((message) => (
            <MessageCard
              key={message.id}
              message={message}
              currentUserId={currentUserId}
              profileOwnerId={profileOwnerId}
              onDelete={handleDelete}
              onReport={handleReport}
            />
          ))}
        </div>
      )}

      {isComposing && (
        <ComposeMessageModal
          username={username}
          wallAllowAnonymous={wallAllowAnonymous}
          onClose={() => setIsComposing(false)}
          onSuccess={handleComposeSuccess}
        />
      )}
    </section>
  );
}
```
  </action>
  <acceptance_criteria>
    - `WallSection.tsx` tem `'use client'` no topo
    - Componente `MessageCard` renderiza `'Anonimo'` como nome quando `message.isAnonymous === true`
    - `MessageCard` NUNCA renderiza `message.author` quando `message.isAnonymous === true`
    - `MessageCard` NUNCA renderiza `message.authorId` quando `message.isAnonymous === true`
    - Botao "Deletar" renderizado apenas quando `canDelete === true` (isOwner || isAuthor nao-anonimo)
    - Botao "Reportar" renderizado apenas quando `canReport === true` (usuario autenticado que nao e o owner/author)
    - Estado vazio exibe mensagem amigavel
    - `ComposeMessageModal` renderizado apenas quando `isComposing === true`
    - Botao "Escrever mensagem" visivel apenas quando `canPost && currentUserId`
  </acceptance_criteria>
  <done>WallSection implementado com lista de mensagens, acoes contextuais (deletar/reportar), exibicao segura de mensagens anonimas, estado vazio, e integracao com ComposeMessageModal.</done>
</task>

<task type="auto">
  <name>Task 4: Adicionar WallSection em app/[username]/page.tsx</name>
  <files>apps/web/app/[username]/page.tsx</files>
  <read_first>
    - apps/web/app/[username]/page.tsx (LER COMPLETAMENTE — para integrar sem quebrar o perfil existente)
    - apps/web/app/[username]/WallSection.tsx (acabou de ser criado)
    - apps/web/lib/api/messages.ts (funcao getWallMessages)
    - apps/web/lib/auth/session.ts (como obter sessao atual em RSC)
  </read_first>
  <action>
Na pagina de perfil `apps/web/app/[username]/page.tsx`:

1. **Importar** `WallSection` e `getWallMessages`:
```typescript
import { WallSection } from './WallSection';
import { getWallMessages } from '../../lib/api/messages';
```

2. **Buscar mensagens** no corpo do RSC (apos buscar dados do perfil):
```typescript
// Fetch wall messages in parallel with profile data
const [profileData, wallMessages] = await Promise.all([
  getProfile(username),      // existing call
  getWallMessages(username), // new call
]);
```
Se a pagina ainda nao tem chamada paralela, adicionar `getWallMessages(username)` como chamada adicional.

3. **Computar permissao de post** (server-side, usa dados do perfil):
```typescript
// Determine if current user can post on this wall
const canPost = (() => {
  if (!session?.user) return false; // not authenticated
  if (profileData.wallWhoCanPost === 'authenticated') return true;
  if (profileData.wallWhoCanPost === 'friends') {
    // Check if currentUser is a friend — this check may need a separate API call
    // Simplified: pass flag from profile API response if available
    return profileData.viewerIsFriend ?? false;
  }
  return false;
})();
```

4. **Adicionar WallSection** no JSX, abaixo das informacoes do perfil:
```tsx
<WallSection
  username={username}
  messages={wallMessages}
  currentUserId={session?.user?.id ?? null}
  profileOwnerId={profileData.id}
  wallAllowAnonymous={profileData.wallAllowAnonymous}
  canPost={canPost}
/>
```

**IMPORTANTE:** Ler o arquivo COMPLETO antes de editar para preservar toda a estrutura existente. Adicionar apenas os imports e o componente WallSection — nao remover ou alterar codigo existente.
  </action>
  <acceptance_criteria>
    - `app/[username]/page.tsx` importa `WallSection`
    - `app/[username]/page.tsx` chama `getWallMessages(username)`
    - `app/[username]/page.tsx` renderiza `<WallSection .../>` com todas as props
    - Pagina de perfil continua funcionando (nenhum codigo existente removido)
    - `WallSection` recebe `currentUserId`, `profileOwnerId`, `wallAllowAnonymous`, `canPost`
  </acceptance_criteria>
  <done>WallSection integrada na pagina de perfil com todas as props necessarias, preservando funcionalidades existentes.</done>
</task>

<task type="auto">
  <name>Task 5: Wall Settings page — app/settings/wall/page.tsx</name>
  <files>apps/web/app/settings/wall/page.tsx</files>
  <read_first>
    - .planning/phases/05-messages/05-CONTEXT.md (D-02: wall config settings)
    - apps/web/app/settings/ (verificar estrutura e layout existente de settings para manter consistencia)
    - apps/web/lib/api/messages.ts (funcao updateWallSettings e interface WallSettingsData)
  </read_first>
  <action>
**`apps/web/app/settings/wall/page.tsx`:**
```typescript
import { redirect } from 'next/navigation';
import { getSession } from '../../../lib/auth/session';
import { WallSettingsForm } from './WallSettingsForm';

export const metadata = { title: 'Configuracoes do Mural — Parabuains' };

export default async function WallSettingsPage() {
  const session = await getSession();
  if (!session?.user) redirect('/login');

  // Fetch current wall settings from profile API
  const res = await fetch(
    `${process.env.NEXT_PUBLIC_API_URL}/v1/users/${session.user.username}/wall`,
    { headers: { Authorization: `Bearer ${session.serviceToken}` } },
  );
  // Wall settings are embedded in the user profile response
  const profileRes = await fetch(
    `${process.env.NEXT_PUBLIC_API_URL}/v1/users/${session.user.username}`,
    { headers: { Authorization: `Bearer ${session.serviceToken}` } },
  );
  const profile = profileRes.ok ? await profileRes.json() : null;

  const currentSettings = {
    wallWhoCanPost: profile?.wallWhoCanPost ?? 'friends',
    wallAllowAnonymous: profile?.wallAllowAnonymous ?? true,
    wallRequireApproval: profile?.wallRequireApproval ?? false,
  };

  return (
    <div className="mx-auto max-w-2xl px-4 py-8">
      <h1 className="mb-6 text-2xl font-bold text-zinc-900 dark:text-zinc-100">
        Configuracoes do Mural
      </h1>
      <WallSettingsForm initialSettings={currentSettings} />
    </div>
  );
}
```

**`apps/web/app/settings/wall/WallSettingsForm.tsx`** (Client Component separado):
```typescript
'use client';

import { useState } from 'react';
import type { WallSettingsData } from '../../../lib/api/messages';

interface WallSettingsFormProps {
  initialSettings: WallSettingsData;
}

export function WallSettingsForm({ initialSettings }: WallSettingsFormProps) {
  const [settings, setSettings] = useState(initialSettings);
  const [isSaving, setIsSaving] = useState(false);
  const [saveStatus, setSaveStatus] = useState<'idle' | 'saved' | 'error'>('idle');

  async function handleSave(e: React.FormEvent) {
    e.preventDefault();
    setIsSaving(true);
    setSaveStatus('idle');
    try {
      const res = await fetch('/api/wall/settings', {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(settings),
      });
      setSaveStatus(res.ok ? 'saved' : 'error');
    } catch {
      setSaveStatus('error');
    } finally {
      setIsSaving(false);
    }
  }

  return (
    <form onSubmit={handleSave} className="space-y-6 rounded-xl border border-zinc-200 bg-white p-6 dark:border-zinc-700 dark:bg-zinc-800/50">
      {/* who_can_post */}
      <fieldset>
        <legend className="mb-3 text-base font-medium text-zinc-900 dark:text-zinc-100">
          Quem pode postar no seu mural
        </legend>
        <div className="space-y-2">
          {[
            { value: 'friends', label: 'Apenas amigos', desc: 'So quem e seu amigo pode deixar mensagens' },
            { value: 'authenticated', label: 'Qualquer usuario autenticado', desc: 'Qualquer pessoa logada pode postar' },
          ].map((opt) => (
            <label key={opt.value} className="flex cursor-pointer items-start gap-3 rounded-lg border border-transparent p-3 hover:bg-zinc-50 dark:hover:bg-zinc-800">
              <input
                type="radio"
                name="wallWhoCanPost"
                value={opt.value}
                checked={settings.wallWhoCanPost === opt.value}
                onChange={() => setSettings((s) => ({ ...s, wallWhoCanPost: opt.value as 'friends' | 'authenticated' }))}
                className="mt-0.5"
              />
              <div>
                <span className="block text-sm font-medium text-zinc-900 dark:text-zinc-100">{opt.label}</span>
                <span className="text-xs text-zinc-500 dark:text-zinc-400">{opt.desc}</span>
              </div>
            </label>
          ))}
        </div>
      </fieldset>

      {/* allow_anonymous */}
      <div className="flex items-center justify-between rounded-lg border border-zinc-200 p-4 dark:border-zinc-700">
        <div>
          <p className="text-sm font-medium text-zinc-900 dark:text-zinc-100">Permitir mensagens anonimas</p>
          <p className="text-xs text-zinc-500 dark:text-zinc-400">O remetente pode ocultar sua identidade ao enviar</p>
        </div>
        <button
          type="button"
          role="switch"
          aria-checked={settings.wallAllowAnonymous}
          onClick={() => setSettings((s) => ({ ...s, wallAllowAnonymous: !s.wallAllowAnonymous }))}
          className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors ${
            settings.wallAllowAnonymous ? 'bg-indigo-600' : 'bg-zinc-300 dark:bg-zinc-600'
          }`}
        >
          <span className={`inline-block h-4 w-4 transform rounded-full bg-white transition-transform ${
            settings.wallAllowAnonymous ? 'translate-x-6' : 'translate-x-1'
          }`} />
        </button>
      </div>

      {/* require_approval */}
      <div className="flex items-center justify-between rounded-lg border border-zinc-200 p-4 dark:border-zinc-700">
        <div>
          <p className="text-sm font-medium text-zinc-900 dark:text-zinc-100">Aprovar mensagens antes de publicar</p>
          <p className="text-xs text-zinc-500 dark:text-zinc-400">Mensagens ficam pendentes ate voce aprovar</p>
        </div>
        <button
          type="button"
          role="switch"
          aria-checked={settings.wallRequireApproval}
          onClick={() => setSettings((s) => ({ ...s, wallRequireApproval: !s.wallRequireApproval }))}
          className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors ${
            settings.wallRequireApproval ? 'bg-indigo-600' : 'bg-zinc-300 dark:bg-zinc-600'
          }`}
        >
          <span className={`inline-block h-4 w-4 transform rounded-full bg-white transition-transform ${
            settings.wallRequireApproval ? 'translate-x-6' : 'translate-x-1'
          }`} />
        </button>
      </div>

      {saveStatus === 'saved' && (
        <p role="status" className="text-sm text-green-600 dark:text-green-400">Configuracoes salvas!</p>
      )}
      {saveStatus === 'error' && (
        <p role="alert" className="text-sm text-red-600 dark:text-red-400">Erro ao salvar. Tente novamente.</p>
      )}

      <div className="flex justify-end">
        <button
          type="submit"
          disabled={isSaving}
          className="rounded-lg bg-indigo-600 px-5 py-2 text-sm font-medium text-white hover:bg-indigo-700 disabled:opacity-50"
        >
          {isSaving ? 'Salvando...' : 'Salvar configuracoes'}
        </button>
      </div>
    </form>
  );
}
```
  </action>
  <acceptance_criteria>
    - `app/settings/wall/page.tsx` existe como RSC (sem `'use client'`)
    - `app/settings/wall/page.tsx` redireciona para `/login` se sem sessao
    - `app/settings/wall/WallSettingsForm.tsx` existe como Client Component com `'use client'`
    - `WallSettingsForm` contem seletor de `wallWhoCanPost` com opcoes `friends` e `authenticated`
    - `WallSettingsForm` contem toggle para `wallAllowAnonymous`
    - `WallSettingsForm` contem toggle para `wallRequireApproval`
    - Ambos os toggles tem `role="switch"` e `aria-checked` correto
    - Formulario tem feedback de estado: "Configuracoes salvas!" e mensagem de erro
  </acceptance_criteria>
  <done>Pagina de configuracoes do mural criada com RSC (auth guard) + Client Component (formulario interativo com os 3 toggles).</done>
</task>

<task type="auto">
  <name>Task 6: Pending approval inbox — app/settings/wall/pending/page.tsx</name>
  <files>apps/web/app/settings/wall/pending/page.tsx</files>
  <read_first>
    - .planning/phases/05-messages/05-CONTEXT.md (D-03: approval flow, pending inbox)
    - apps/web/lib/api/messages.ts (funcoes getPendingMessages, approveMessage, rejectMessage)
    - apps/web/app/settings/wall/page.tsx (acabou de ser criado — para manter consistencia de layout)
  </read_first>
  <action>
**`apps/web/app/settings/wall/pending/page.tsx`:**
```typescript
import { redirect } from 'next/navigation';
import { getSession } from '../../../../lib/auth/session';
import { getPendingMessages } from '../../../../lib/api/messages';
import { PendingMessagesClient } from './PendingMessagesClient';

export const metadata = { title: 'Aprovacao de Mensagens — Parabuains' };

export default async function PendingMessagesPage() {
  const session = await getSession();
  if (!session?.user) redirect('/login');

  const pendingMessages = await getPendingMessages();

  return (
    <div className="mx-auto max-w-2xl px-4 py-8">
      <div className="mb-6 flex items-center justify-between">
        <div>
          <h1 className="text-2xl font-bold text-zinc-900 dark:text-zinc-100">
            Mensagens Pendentes
          </h1>
          <p className="mt-1 text-sm text-zinc-500 dark:text-zinc-400">
            {pendingMessages.length} mensagem{pendingMessages.length !== 1 ? 's' : ''} aguardando aprovacao
          </p>
        </div>
        <a
          href="/settings/wall"
          className="text-sm text-indigo-600 hover:underline dark:text-indigo-400"
        >
          Configuracoes do mural
        </a>
      </div>

      <PendingMessagesClient initialMessages={pendingMessages} />
    </div>
  );
}
```

**`apps/web/app/settings/wall/pending/PendingMessagesClient.tsx`:**
```typescript
'use client';

import { useState } from 'react';
import type { WallMessageData } from '../../../../lib/api/messages';

interface PendingMessagesClientProps {
  initialMessages: WallMessageData[];
}

export function PendingMessagesClient({ initialMessages }: PendingMessagesClientProps) {
  const [messages, setMessages] = useState(initialMessages);

  async function handleApprove(messageId: string) {
    try {
      const res = await fetch(`/api/messages/${messageId}/approve`, { method: 'PATCH' });
      if (res.ok) {
        setMessages((prev) => prev.filter((m) => m.id !== messageId));
      }
    } catch {
      alert('Erro ao aprovar mensagem.');
    }
  }

  async function handleReject(messageId: string) {
    if (!confirm('Rejeitar esta mensagem?')) return;
    try {
      const res = await fetch(`/api/messages/${messageId}/reject`, { method: 'PATCH' });
      if (res.ok) {
        setMessages((prev) => prev.filter((m) => m.id !== messageId));
      }
    } catch {
      alert('Erro ao rejeitar mensagem.');
    }
  }

  if (messages.length === 0) {
    return (
      <div className="rounded-xl border border-dashed border-zinc-300 p-8 text-center dark:border-zinc-700">
        <p className="text-zinc-500 dark:text-zinc-400">
          Nenhuma mensagem pendente. Tudo em dia!
        </p>
      </div>
    );
  }

  return (
    <div className="space-y-4">
      {messages.map((message) => {
        // Display anonymous message safely — never reveal identity
        const displayName = message.isAnonymous
          ? 'Anonimo'
          : (message.author?.displayName ?? 'Usuario desconhecido');
        const avatarUrl = message.isAnonymous ? null : message.author?.avatarUrl;

        return (
          <article
            key={message.id}
            className="rounded-xl border border-zinc-200 bg-white p-4 dark:border-zinc-700 dark:bg-zinc-800/50"
          >
            <div className="flex items-start gap-3">
              {/* Avatar */}
              <div className="flex-shrink-0">
                {avatarUrl ? (
                  <img
                    src={avatarUrl}
                    alt={`Avatar de ${displayName}`}
                    className="h-9 w-9 rounded-full object-cover"
                  />
                ) : (
                  <div className="flex h-9 w-9 items-center justify-center rounded-full bg-zinc-200 text-sm font-medium text-zinc-600 dark:bg-zinc-700 dark:text-zinc-300">
                    {message.isAnonymous ? '?' : displayName[0]?.toUpperCase()}
                  </div>
                )}
              </div>

              <div className="min-w-0 flex-1">
                <div className="flex items-center gap-2">
                  <span className="text-sm font-medium text-zinc-900 dark:text-zinc-100">
                    {displayName}
                  </span>
                  {message.isAnonymous && (
                    <span className="rounded-full bg-zinc-100 px-2 py-0.5 text-xs text-zinc-500 dark:bg-zinc-700">
                      Anonima
                    </span>
                  )}
                  {message.isPrivate && (
                    <span className="rounded-full bg-zinc-100 px-2 py-0.5 text-xs text-zinc-500 dark:bg-zinc-700">
                      Privada
                    </span>
                  )}
                </div>

                <p className="mt-1 whitespace-pre-wrap break-words text-sm text-zinc-700 dark:text-zinc-300">
                  {message.content}
                </p>

                <time
                  dateTime={message.createdAt}
                  className="mt-1 block text-xs text-zinc-400"
                >
                  {new Date(message.createdAt).toLocaleDateString('pt-BR', {
                    day: 'numeric',
                    month: 'short',
                    year: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit',
                  })}
                </time>
              </div>
            </div>

            {/* Approval actions */}
            <div className="mt-3 flex items-center gap-3 border-t border-zinc-100 pt-3 dark:border-zinc-700">
              <button
                onClick={() => handleApprove(message.id)}
                className="rounded-lg bg-green-600 px-4 py-1.5 text-sm font-medium text-white hover:bg-green-700"
              >
                Aprovar
              </button>
              <button
                onClick={() => handleReject(message.id)}
                className="rounded-lg border border-red-300 px-4 py-1.5 text-sm font-medium text-red-600 hover:bg-red-50 dark:border-red-700 dark:text-red-400 dark:hover:bg-red-950"
              >
                Rejeitar
              </button>
            </div>
          </article>
        );
      })}
    </div>
  );
}
```
  </action>
  <acceptance_criteria>
    - `app/settings/wall/pending/page.tsx` existe como RSC
    - `app/settings/wall/pending/page.tsx` redireciona para `/login` se sem sessao
    - `app/settings/wall/pending/PendingMessagesClient.tsx` existe com `'use client'`
    - `PendingMessagesClient` renderiza botoes "Aprovar" e "Rejeitar" para cada mensagem
    - Mensagens anonimas exibem `'Anonimo'` como nome — nunca o nome real
    - Estado vazio exibe "Nenhuma mensagem pendente. Tudo em dia!"
    - Apos aprovar/rejeitar, mensagem e removida da lista (otimistic update via filter)
  </acceptance_criteria>
  <done>Inbox de aprovacao pendente criado com RSC + Client Component. Exibe mensagens com botoes de aprovar/rejeitar, tratamento seguro de mensagens anonimas, e estado vazio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component -> BFF | Chamadas fetch para /api/... que proxiam para Fastify |
| RSC -> Fastify API | Server-side fetches com service token — nunca expoe token ao cliente |
| Render de mensagem anonima | Componente NUNCA renderiza dados de author quando isAnonymous = true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information Disclosure | WallSection rendering | mitigate | Condicional `message.isAnonymous` guarda toda renderizacao de author — nao usa dados do campo author para anonimos |
| T-05-09 | Information Disclosure | PendingMessagesClient | mitigate | Mesmo padrao de anonimato aplicado na tela de aprovacao |
| T-05-10 | Tampering / XSS | message.content display | mitigate | Conteudo ja sanitizado pela API; `whitespace-pre-wrap` no CSS (nao `dangerouslySetInnerHTML`) |
| T-05-11 | Spoofing | ComposeMessageModal | mitigate | Honeypot com `aria-hidden` e `tabIndex=-1`; servidor verifica campo website |
| T-05-12 | Elevation of Privilege | Botoes de acao | mitigate | Botao deletar renderizado apenas quando `canDelete === true`; API valida novamente no servidor |
</threat_model>

<verification>
Da raiz do monorepo:
1. `cd apps/web && pnpm type-check` — sem erros TypeScript nos novos componentes
2. `cd apps/web && pnpm build` — build Next.js sem erros
3. Verificar que `WallSection.tsx` nunca renderiza `message.author` quando `message.isAnonymous`:
   `grep -n "message.author" apps/web/app/[username]/WallSection.tsx` — deve aparecer apenas dentro de `!message.isAnonymous`
4. Verificar honeypot em `ComposeMessageModal.tsx`:
   `grep -n "honeypot\|website" apps/web/app/[username]/ComposeMessageModal.tsx`
5. Verificar que `PendingMessagesClient` trata anonimato:
   `grep -n "isAnonymous" apps/web/app/settings/wall/pending/PendingMessagesClient.tsx`
6. Verificar que paginas RSC de settings tem auth guard:
   `grep -n "redirect" apps/web/app/settings/wall/page.tsx`
</verification>

<success_criteria>
- WallSection aparece na pagina de perfil abaixo das informacoes do usuario
- ComposeMessageModal abre ao clicar "Escrever mensagem" com: seletor de tipo, contador 500 chars, honeypot
- Mensagens anonimas exibem "Anonimo" — identidade nunca revelada na UI
- Botao "Deletar" aparece apenas para owner ou author nao-anonimo
- Botao "Reportar" aparece apenas para outros usuarios autenticados
- `/settings/wall` mostra form de configuracoes com 3 controles (who_can_post, allow_anonymous, require_approval)
- `/settings/wall/pending` mostra inbox com botoes Aprovar/Rejeitar
- Build Next.js sem erros TypeScript
</success_criteria>

<output>After completion, create `.planning/phases/05-messages/05-02-SUMMARY.md`</output>
