---
phase: 04-social
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - apps/web/app/feed/page.tsx
  - apps/web/app/feed/loading.tsx
  - apps/web/components/feed/birthday-card.tsx
  - apps/web/components/feed/countdown-display.tsx
  - apps/web/app/friends/page.tsx
  - apps/web/app/friends/requests/page.tsx
  - apps/web/app/search/page.tsx
  - apps/web/components/friends/friend-card.tsx
  - apps/web/components/friends/friend-request-card.tsx
  - apps/web/components/friends/friend-suggestions-section.tsx
  - apps/web/components/profile/add-friend-button.tsx
  - apps/web/components/profile/mutual-friends-display.tsx
  - apps/web/components/profile/private-profile-lock.tsx
autonomous: true
requirements: [SOCL-01, SOCL-03, SOCL-04, SOCL-07, SOCL-08]

must_haves:
  truths:
    - "app/feed/page.tsx exibe aniversariantes do dia destacados + lista upcoming com countdown"
    - "app/friends/page.tsx lista amigos aceitos com paginacao cursor-based"
    - "app/friends/requests/page.tsx exibe pedidos pendentes com aceitar/rejeitar"
    - "app/search/page.tsx busca usuarios com debounce e exibe status de amizade"
    - "Perfil privado mostra nome+avatar+'Conta privada'+botao Add Friend (nao 404)"
    - "Perfil publico integra botao Add Friend e exibe amigos em comum"
    - "FriendSuggestionsSection aparece no feed ou lista de amigos"
  artifacts:
    - path: "apps/web/app/feed/page.tsx"
      provides: "Feed page RSC com secao today destacada e cards upcoming"
      contains: "BirthdayCard"
    - path: "apps/web/components/profile/private-profile-lock.tsx"
      provides: "UI de perfil privado Instagram-style"
      contains: "Conta privada"
    - path: "apps/web/components/friends/friend-suggestions-section.tsx"
      provides: "Secao de sugestoes de amigos (friends-of-friends)"
      contains: "getFriendSuggestions"
  key_links:
    - from: "app/feed/page.tsx"
      to: "GET /v1/feed/birthdays"
      via: "DAL call no RSC"
      pattern: "feed/birthdays"
    - from: "app/search/page.tsx"
      to: "GET /v1/users/search"
      via: "Server Action ou fetch no client"
      pattern: "users/search"
---

<objective>
Implementar toda a UI social do Parabuains: paginas de feed, amigos, pedidos, busca de usuarios e integracoes no perfil. O feed exibe countdown cards para aniversarios dos proximos 30 dias. Perfis privados mostram UI "trancada" Instagram-style. Sugestoes de amigos aparecem como secao contextual.

Purpose: Entrega a experiencia completa do grafo social para o usuario final.
Output: 13 arquivos de UI com todas as paginas e componentes sociais funcionando.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-social/04-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BirthdayCard e CountdownDisplay components</name>
  <files>
    apps/web/components/feed/birthday-card.tsx
    apps/web/components/feed/countdown-display.tsx
  </files>
  <read_first>
    - .planning/phases/04-social/04-CONTEXT.md (D-06: campos retornados no feed)
    - apps/api/src/modules/feed/feed.types.ts (BirthdayFeedEntry)
  </read_first>
  <action>
Criar componentes de exibicao do feed de aniversarios.

**countdown-display.tsx** — exibe contagem regressiva:
- Props: daysUntilBirthday: number
- Se daysUntilBirthday === 0: exibe "Hoje! 🎂" com destaque visual
- Se daysUntilBirthday === 1: exibe "Amanha"
- Caso contrario: exibe "{N} dias"
- Componente 'use client' para animacoes futuras, mas pode ser server para MVP

**birthday-card.tsx** — card de aniversariante:
- Props: entry: BirthdayFeedEntry, isToday?: boolean
- Exibe: avatar (com fallback de iniciais), displayName, username, CountdownDisplay
- Se isToday=true: borda colorida, background highlight
- Se birthYear exibido: mostra idade calculada (ano atual - birthYear)
- Link para o perfil do usuario (/[username])
- Semantica: article element com role adequado

Ambos os componentes devem usar Tailwind v4 para estilo. Sem dependencias de bibliotecas de UI terceiras.
  </action>
  <acceptance_criteria>
    - birthday-card.tsx renderiza CountdownDisplay e link para /[username]
    - countdown-display.tsx diferencia hoje (daysUntilBirthday === 0) de outros dias
    - birthday-card.tsx tem isToday prop que altera visual
    - Nenhuma dependencia de biblioteca de UI de terceiros (so Tailwind)
  </acceptance_criteria>
  <done>BirthdayCard e CountdownDisplay implementados com visual diferenciado para hoje.</done>
</task>

<task type="auto">
  <name>Task 2: Feed page (app/feed/page.tsx)</name>
  <files>
    apps/web/app/feed/page.tsx
    apps/web/app/feed/loading.tsx
  </files>
  <read_first>
    - apps/web/components/feed/birthday-card.tsx (recem criado)
    - .planning/phases/04-social/04-CONTEXT.md (D-06: estrutura da resposta do feed)
    - .planning/research/ARCHITECTURE.md (DAL Pattern para RSC)
  </read_first>
  <action>
Criar pagina de feed como React Server Component.

**app/feed/page.tsx**:
- RSC (sem 'use client')
- Busca sessao e extrai timezone do usuario (ou usa 'America/Sao_Paulo' como fallback)
- Chama DAL para GET /v1/feed/birthdays?timezone={tz}
- Exibe:
  - Secao "Hoje" (se today.length > 0): heading destacado + grid de BirthdayCard com isToday=true
  - Se today.length === 0: nao renderiza secao
  - Secao "Proximos 30 dias": lista de BirthdayCard ordenados por daysUntilBirthday
  - Se upcoming.length === 0 E today.length === 0: empty state com mensagem "Adicione amigos para ver seus aniversarios"
  - Secao de sugestoes ao final (FriendSuggestionsSection — Task 7)
- Metadata: title "Feed de Aniversarios | Parabuains"

**app/feed/loading.tsx**:
- Skeleton loading state com Tailwind animate-pulse
- Simular layout de 3-4 cards

Redirecionar para /login se nao autenticado (usar redirect() do Next.js).
  </action>
  <acceptance_criteria>
    - app/feed/page.tsx e RSC (sem 'use client')
    - Exibe secao "Hoje" apenas quando today.length > 0
    - Redireciona para /login se nao autenticado
    - loading.tsx existe com skeleton state
    - Metadata exportada com title correto
  </acceptance_criteria>
  <done>Feed page RSC com secoes today/upcoming e empty state.</done>
</task>

<task type="auto">
  <name>Task 3: FriendCard e lista de amigos</name>
  <files>
    apps/web/components/friends/friend-card.tsx
    apps/web/app/friends/page.tsx
  </files>
  <read_first>
    - .planning/phases/04-social/04-CONTEXT.md (D-04: dados retornados de amigos)
  </read_first>
  <action>
Criar componente de card de amigo e pagina de lista de amigos.

**friend-card.tsx**:
- Props: user: PublicUserProfile (id, username, displayName, avatarUrl, privacyLevel)
- Exibe: avatar com fallback de iniciais, displayName, @username
- Acoes: botao "Ver perfil" (link para /[username]) + botao "Remover" (trigger Server Action)
- Botao Remover: usa DELETE /v1/friendships/:id — precisa de friendshipId como prop adicional

**app/friends/page.tsx**:
- RSC com paginacao cursor-based
- Searchparam: cursor (string opcional)
- Chama DAL: GET /v1/users/me/friends?cursor={cursor}&limit=20
- Renderiza lista de FriendCard
- Botao "Carregar mais" se nextCursor != null
- Contador de amigos no heading
- Empty state: "Voce ainda nao tem amigos. Busque pessoas para adicionar."
- Link para /search
  </action>
  <acceptance_criteria>
    - friend-card.tsx tem botao "Remover" que chama endpoint correto
    - app/friends/page.tsx implementa paginacao cursor-based via searchParams
    - Empty state tem link para /search
    - Redireciona para /login se nao autenticado
  </acceptance_criteria>
  <done>FriendCard e pagina de lista de amigos com paginacao cursor-based.</done>
</task>

<task type="auto">
  <name>Task 4: FriendRequestCard e inbox de pedidos</name>
  <files>
    apps/web/components/friends/friend-request-card.tsx
    apps/web/app/friends/requests/page.tsx
  </files>
  <read_first>
    - .planning/phases/04-social/04-CONTEXT.md (D-02: estados de amizade)
  </read_first>
  <action>
Criar componente de pedido de amizade e pagina de inbox.

**friend-request-card.tsx**:
- Props: friendship: FriendshipWithUser (com user = quem enviou o pedido)
- Exibe: avatar, displayName, @username, data de envio (formato relativo: "ha 2 dias")
- Botoes: "Aceitar" (verde) e "Recusar" (vermelho/outline)
- Aceitar: PATCH /v1/friendships/:id { action: 'accept' }
- Recusar: PATCH /v1/friendships/:id { action: 'reject' }
- Apos acao: remover card da lista (optimistic update)

**app/friends/requests/page.tsx**:
- RSC que busca GET /v1/users/me/friend-requests
- Exibe contador de pedidos no heading: "Pedidos de amizade (N)"
- Lista de FriendRequestCard
- Empty state: "Nenhum pedido pendente"
- Redireciona para /login se nao autenticado
  </action>
  <acceptance_criteria>
    - friend-request-card.tsx tem botoes Aceitar e Recusar
    - Aceitar chama PATCH com action:'accept', Recusar com action:'reject'
    - app/friends/requests/page.tsx exibe contador de pedidos
    - Empty state quando nao ha pedidos
  </acceptance_criteria>
  <done>FriendRequestCard e pagina de inbox de pedidos implementados.</done>
</task>

<task type="auto">
  <name>Task 5: Search page com debounce</name>
  <files>apps/web/app/search/page.tsx</files>
  <read_first>
    - .planning/phases/04-social/04-CONTEXT.md (D-03: private profile model, D-04: user search)
  </read_first>
  <action>
Criar pagina de busca de usuarios.

**app/search/page.tsx**:
- Client component ('use client') para debounce do input
- Input de busca com debounce de 300ms (usar setTimeout/clearTimeout)
- Ao digitar: chama GET /v1/users/search?q={query}
- Para cada resultado, exibe:
  - Avatar + displayName + @username
  - Badge de privacidade se privacyLevel === 'private': "Conta privada"
  - Status de amizade: "Amigos" | "Pedido enviado" | "Pedido recebido" | botao "Adicionar"
  - Botao "Adicionar" chama POST /v1/friendships { targetUsername }
- Loading state durante busca
- Empty state: "Nenhum resultado para '{query}'"
- Estado inicial: "Busque por nome ou @username"
- Minimo 2 caracteres antes de buscar

A pagina deve mostrar resultados com status de amizade. Para MVP, o status pode ser
determinado por uma segunda chamada ao endpoint de amigos/pedidos ou embutido na
resposta de busca se o backend retornar campo 'friendshipStatus'.
  </action>
  <acceptance_criteria>
    - app/search/page.tsx tem diretiva 'use client'
    - Debounce de 300ms no input (setTimeout com clearTimeout)
    - Minimo 2 caracteres antes de buscar
    - Botao "Adicionar" chama POST /v1/friendships
    - Exibe badge "Conta privada" para perfis privados
  </acceptance_criteria>
  <done>Pagina de busca com debounce, resultados e botao de adicionar amigo.</done>
</task>

<task type="auto">
  <name>Task 6: AddFriendButton e MutualFriendsDisplay para pagina de perfil</name>
  <files>
    apps/web/components/profile/add-friend-button.tsx
    apps/web/components/profile/mutual-friends-display.tsx
  </files>
  <read_first>
    - .planning/phases/04-social/04-CONTEXT.md (D-03: private profile, D-04: search results)
  </read_first>
  <action>
Criar componentes para integracao na pagina de perfil existente.

**add-friend-button.tsx**:
- Props: targetUsername: string, initialStatus: 'none' | 'pending_sent' | 'pending_received' | 'friends' | 'blocked'
- 'use client'
- Renderizacao por status:
  - 'none': botao "Adicionar amigo" (azul primario)
  - 'pending_sent': botao desabilitado "Pedido enviado"
  - 'pending_received': dois botoes "Aceitar" e "Recusar"
  - 'friends': botao outline "Remover amigo"
  - 'blocked': nada renderizado
- Ao clicar "Adicionar": POST /v1/friendships { targetUsername }
- Feedback visual: loading state durante acao

**mutual-friends-display.tsx**:
- Props: mutualFriends: Array<{ id: string; username: string; displayName: string; avatarUrl: string | null }>
- Se mutualFriends.length === 0: nao renderiza nada
- Exibe: "N amigos em comum" com avatares empilhados (max 3 avatares + "+N" se mais)
- Link para lista nao necessario no MVP
  </action>
  <acceptance_criteria>
    - add-friend-button.tsx renderiza diferente para cada status
    - add-friend-button.tsx tem 'use client'
    - mutual-friends-display.tsx nao renderiza nada se array vazio
    - mutual-friends-display.tsx mostra max 3 avatares + overflow count
  </acceptance_criteria>
  <done>AddFriendButton e MutualFriendsDisplay prontos para integracao no perfil.</done>
</task>

<task type="auto">
  <name>Task 7: PrivateProfileLock e FriendSuggestionsSection</name>
  <files>
    apps/web/components/profile/private-profile-lock.tsx
    apps/web/components/friends/friend-suggestions-section.tsx
  </files>
  <read_first>
    - .planning/phases/04-social/04-CONTEXT.md (D-03: private profile Instagram-style, D-05: suggestions)
  </read_first>
  <action>
Criar componente de perfil privado e secao de sugestoes.

**private-profile-lock.tsx**:
- Props: username: string, displayName: string, avatarUrl: string | null
- Exibe (nao retorna 404 — conteudo trancado):
  - Avatar grande no centro
  - displayName e @username
  - Icone de cadeado (SVG inline ou emoji)
  - Texto: "Esta conta e privada"
  - Subtexto: "Siga @{username} para ver os aniversarios e mensagens"
  - AddFriendButton integrado
- Layout centralizado, estilo Instagram-like

**friend-suggestions-section.tsx**:
- 'use client' ou RSC dependendo de como os dados chegam
- Props: suggestions: FriendSuggestion[] (pode receber como prop do parent RSC)
- Se suggestions.length === 0: nao renderiza nada
- Heading: "Pessoas que voce pode conhecer"
- Grid horizontal (scroll) ou lista de cards de sugestao
- Cada card: avatar, displayName, "{N} amigos em comum", botao "Adicionar"
- Botao Adicionar: POST /v1/friendships { targetUsername: suggestion.username }
  </action>
  <acceptance_criteria>
    - private-profile-lock.tsx exibe nome + avatar + texto "privada" + AddFriendButton
    - private-profile-lock.tsx NAO retorna 404 (e um componente visual, nao redirect)
    - friend-suggestions-section.tsx nao renderiza nada se array vazio
    - friend-suggestions-section.tsx exibe contagem de amigos em comum
    - Botao Adicionar em sugestoes chama POST /v1/friendships
  </acceptance_criteria>
  <done>PrivateProfileLock e FriendSuggestionsSection implementados.</done>
</task>

<task type="auto">
  <name>Task 8: Integracao na pagina de perfil existente</name>
  <files>apps/web/app/[username]/page.tsx</files>
  <read_first>
    - apps/web/app/[username]/page.tsx (arquivo EXISTENTE da Fase 3 — ler antes de editar)
    - .planning/phases/04-social/04-CONTEXT.md (D-03: private profile — authenticated non-friends)
    - apps/web/components/profile/add-friend-button.tsx
    - apps/web/components/profile/mutual-friends-display.tsx
    - apps/web/components/profile/private-profile-lock.tsx
  </read_first>
  <action>
Integrar componentes sociais na pagina de perfil existente. Ler o arquivo existente COMPLETAMENTE antes de fazer qualquer modificacao.

Adicionar:
1. Determinar status de amizade entre viewer e profile owner:
   - Chamar endpoint que verifica amizade bilateral
   - Mapear para: 'none' | 'pending_sent' | 'pending_received' | 'friends' | 'blocked'

2. Adicionar AddFriendButton abaixo do nome/bio (apenas se viewer !== profile owner)

3. Adicionar MutualFriendsDisplay abaixo do AddFriendButton

4. Para perfis privados (privacyLevel !== 'public') E viewer nao e amigo:
   - Renderizar PrivateProfileLock em vez do conteudo completo do perfil
   - Excecao: se viewer === profile owner, mostrar perfil completo normalmente
   - Excecao: unauthenticated visitors a perfis 'private' → retornar notFound() (404)

IMPORTANTE: Nao quebrar nenhuma funcionalidade existente da Fase 3.
Apenas adicionar os novos componentes nas posicoes corretas.
  </action>
  <acceptance_criteria>
    - AddFriendButton aparece na pagina de perfil quando viewer !== owner
    - MutualFriendsDisplay aparece apos AddFriendButton
    - Perfil privado + viewer nao-amigo: renderiza PrivateProfileLock (nao 404)
    - Perfil privado + unauthenticated: retorna notFound() (404)
    - Perfil do proprio usuario: nao exibe AddFriendButton
    - Nenhuma funcionalidade da Fase 3 foi quebrada
  </acceptance_criteria>
  <done>Pagina de perfil integrada com componentes sociais e logica de privacidade correta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| Dados de perfil privado | Verificacao server-side no RSC — nao apenas CSS hide |
| userId do viewer | Obtido da sessao no RSC — nunca de query params |
| Acoes de amizade no cliente | Chamam BFF que adiciona service token antes do Fastify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigacao |
|-----------|----------|-----------|-------------|-----------|
| T-04-10 | Information Disclosure | app/[username]/page.tsx | mitigate | Perfil privado: conteudo oculto server-side, nao apenas visual |
| T-04-11 | Spoofing | AddFriendButton POST /v1/friendships | mitigate | BFF adiciona sessao autenticada; Fastify valida JWT service token |
| T-04-12 | Information Disclosure | search results | mitigate | Backend ja filtra bloqueados; frontend nao precisa re-filtrar |
</threat_model>

<verification>
1. Verificar que feed page e RSC: `grep -v "'use client'" apps/web/app/feed/page.tsx | head -1`
2. Verificar debounce na search page: `grep "setTimeout" apps/web/app/search/page.tsx`
3. Verificar private profile lock nao usa notFound(): `grep "notFound\|redirect" apps/web/components/profile/private-profile-lock.tsx` — deve retornar vazio
4. Verificar integracao no perfil: `grep "AddFriendButton\|PrivateProfileLock" apps/web/app/\[username\]/page.tsx`
5. Verificar acessibilidade do toast: `grep "role=\"alert\"" apps/web/components/notifications/toast-notification.tsx`
</verification>

<success_criteria>
- Feed page exibe aniversariantes do dia destacados e lista de proximos 30 dias
- Friends list com paginacao cursor-based funciona
- Friend requests inbox exibe pedidos com aceitar/rejeitar
- Search page busca com debounce 300ms e mostra status de amizade
- Perfil privado mostra UI "trancada" com AddFriendButton (nao 404 para autenticados)
- Pagina de perfil integra AddFriendButton e MutualFriendsDisplay
- FriendSuggestionsSection visivel no feed ou lista de amigos
</success_criteria>

<output>After completion, create `.planning/phases/04-social/04-03-SUMMARY.md`</output>
