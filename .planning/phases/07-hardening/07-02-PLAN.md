---
phase: 07-hardening
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - apps/api/src/services/anomaly-detection.service.ts
  - apps/api/src/services/auth.service.ts
  - apps/api/src/services/user.service.ts
  - apps/api/src/services/message.service.ts
  - apps/api/src/services/friendship.service.ts
  - apps/api/src/errors/totp.errors.ts
  - apps/api/src/plugins/rate-limit.ts
  - apps/api/src/routes/auth.routes.ts
  - apps/api/src/routes/users.routes.ts
  - apps/api/src/routes/friendships.routes.ts
  - apps/api/src/routes/messages.routes.ts
  - apps/api/tests/services/anomaly-detection.test.ts
autonomous: true
requirements: [cross-cutting]

must_haves:
  truths:
    - "AnomalyDetectionService usa Redis INCR + EXPIRE (zero dependencias externas)"
    - "Cada anomalia e logada em audit_logs com action='anomaly.*'"
    - "2FA enforcement em change email, disable 2FA e delete account quando totp_enabled=true"
    - "Rate limiting verificado em todos os 6 endpoints de mutacao listados no CONTEXT D-05"
    - "Thresholds usados sao exatamente os definidos em 07-CONTEXT.md D-03"
  artifacts:
    - path: "apps/api/src/services/anomaly-detection.service.ts"
      provides: "AnomalyDetectionService com checkLoginFlood, checkMessageFlood, checkFriendshipFlood, checkResetFlood"
      contains: "class AnomalyDetectionService"
    - path: "apps/api/src/errors/totp.errors.ts"
      provides: "TotpRequiredError e InvalidTotpError para 2FA enforcement"
      contains: "TotpRequiredError"
    - path: "apps/api/tests/services/anomaly-detection.test.ts"
      provides: "Testes Vitest para cada regra de anomalia com Redis mockado"
      contains: "checkLoginFlood"
  key_links:
    - from: "apps/api/src/services/anomaly-detection.service.ts"
      to: "apps/api/src/repositories/audit.repository.ts"
      via: "injecao no construtor para log de anomalias"
      pattern: "auditRepo.insert"
    - from: "apps/api/src/services/auth.service.ts"
      to: "apps/api/src/services/anomaly-detection.service.ts"
      via: "chamada em recordFailedLogin antes de lancar erro"
      pattern: "anomalyService.checkLoginFlood"
---

<objective>
Implementar AnomalyDetectionService com regras threshold-based usando Redis counters, 2FA enforcement em operacoes sensiveis, e auditoria dos endpoints de rate limiting existentes.

Proposito: Detectar e bloquear comportamentos aberrantes (login flood, message flood, friendship flood, password reset flood) sem dependencias externas — apenas Redis INCR + EXPIRE. Garantir que operacoes destrutivas (troca de email, desabilitar 2FA, deletar conta) requerem verificacao TOTP quando o usuario tem 2FA ativo.
Saida: AnomalyDetectionService integrado nos services relevantes, erros TotpRequired/InvalidTotp, rate limiting auditado em todos os endpoints de mutacao, testes Vitest cobrindo cada regra.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hardening/07-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TotpRequiredError e InvalidTotpError</name>
  <files>apps/api/src/errors/totp.errors.ts</files>
  <read_first>
    - apps/api/src/errors/ (erros customizados existentes — convencoes de classe e HTTP status)
    - .planning/phases/07-hardening/07-CONTEXT.md (D-04: padrao de 2FA enforcement)
  </read_first>
  <action>
Criar `apps/api/src/errors/totp.errors.ts`:

```typescript
export class TotpRequiredError extends Error {
  readonly statusCode = 403;
  readonly code       = 'TOTP_REQUIRED';

  constructor() {
    super('This operation requires TOTP verification. Please provide your 2FA code.');
    this.name = 'TotpRequiredError';
  }
}

export class InvalidTotpError extends Error {
  readonly statusCode = 403;
  readonly code       = 'TOTP_INVALID';

  constructor() {
    super('Invalid or expired TOTP code.');
    this.name = 'InvalidTotpError';
  }
}
```

Exportar dos erros no barrel de erros se existir (`apps/api/src/errors/index.ts`).

Registrar handler de erro no Fastify para estes tipos (em `apps/api/src/app.ts` ou equivalente):
```typescript
fastify.setErrorHandler((error, request, reply) => {
  if (error instanceof TotpRequiredError || error instanceof InvalidTotpError) {
    return reply.status(error.statusCode).send({ code: error.code, message: error.message });
  }
  // ... handler existente
});
```
  </action>
  <acceptance_criteria>
    - apps/api/src/errors/totp.errors.ts exporta TotpRequiredError e InvalidTotpError
    - TotpRequiredError.statusCode === 403, code === 'TOTP_REQUIRED'
    - InvalidTotpError.statusCode === 403, code === 'TOTP_INVALID'
    - Erros produzem HTTP 403 quando lancados (verificar handler no app.ts)
  </acceptance_criteria>
  <done>TotpRequiredError e InvalidTotpError criados com status 403 e codigos de erro.</done>
</task>

<task type="auto">
  <name>Task 2: 2FA enforcement em operacoes sensiveis</name>
  <files>
    apps/api/src/services/user.service.ts,
    apps/api/src/services/auth.service.ts
  </files>
  <read_first>
    - apps/api/src/services/user.service.ts
    - apps/api/src/services/auth.service.ts
    - .planning/phases/07-hardening/07-CONTEXT.md (D-04: padrao exacto de implementacao)
    - .planning/research/ARCHITECTURE.md (secao "2FA Placement")
  </read_first>
  <action>
Adicionar verificacao TOTP nas tres operacoes sensiveis. Padrao identico para cada uma:

```typescript
private async requireTotpIfEnabled(userId: string, totpCode?: string): Promise<void> {
  const user = await this.userRepo.findById(userId);
  if (!user) throw new NotFoundError('User not found');
  if (!user.totpEnabled) return; // 2FA nao ativo — pular verificacao

  if (!totpCode) {
    throw new TotpRequiredError();
  }
  if (!this.totpService.verify(user.totpSecret!, totpCode)) {
    throw new InvalidTotpError();
  }
}
```

**UserService.changeEmail(userId, newEmail, totpCode?):**
```typescript
async changeEmail(userId: string, newEmail: string, totpCode?: string): Promise<void> {
  await this.requireTotpIfEnabled(userId, totpCode);
  // ... logica existente de mudanca de email
}
```

**AuthService.disable2FA(userId, totpCode?):**
```typescript
async disable2FA(userId: string, totpCode?: string): Promise<void> {
  await this.requireTotpIfEnabled(userId, totpCode);
  // ... logica existente de desabilitar 2FA
}
```

**UserService.deleteAccount(userId, totpCode?):**
```typescript
async deleteAccount(userId: string, totpCode?: string): Promise<void> {
  await this.requireTotpIfEnabled(userId, totpCode);
  // ... logica existente de soft delete
}
```

Atualizar as rotas correspondentes para passar `request.body.totpCode` (campo opcional) para os services.
Atualizar schemas JSON Schema/Zod das rotas para aceitar `totpCode?: string` no body.
  </action>
  <acceptance_criteria>
    - grep -n "TotpRequiredError\|InvalidTotpError" apps/api/src/services/user.service.ts retorna >= 2 ocorrencias
    - grep -n "TotpRequiredError\|InvalidTotpError" apps/api/src/services/auth.service.ts retorna >= 1 ocorrencia
    - Metodos changeEmail, disable2FA, deleteAccount aceitam totpCode opcional
    - Se totpEnabled=true e totpCode ausente: lanca TotpRequiredError
    - Se totpEnabled=true e totpCode invalido: lanca InvalidTotpError
    - Se totpEnabled=false: prossegue sem verificacao
  </acceptance_criteria>
  <done>2FA enforcement implementado em changeEmail, disable2FA e deleteAccount.</done>
</task>

<task type="auto">
  <name>Task 3: AnomalyDetectionService — Redis INCR + EXPIRE</name>
  <files>apps/api/src/services/anomaly-detection.service.ts</files>
  <read_first>
    - .planning/phases/07-hardening/07-CONTEXT.md (D-03: tabela de thresholds e D-03 specifics — padrao Redis)
    - apps/api/src/services/ (como Redis e injetado nos services existentes)
    - apps/api/src/repositories/audit.repository.ts
  </read_first>
  <action>
Criar `apps/api/src/services/anomaly-detection.service.ts`:

```typescript
import type { Redis } from 'ioredis';
import { AuditRepository } from '../repositories/audit.repository';

export class AnomalyDetectionService {
  constructor(
    private readonly redis: Redis,
    private readonly auditRepo: AuditRepository,
  ) {}

  /**
   * Login flood por IP: 20 falhas / 1 hora → bloqueia IP + loga anomalia
   */
  async checkLoginFlood(ip: string): Promise<void> {
    const key   = `anomaly:login:ip:${ip}`;
    const count = await this.redis.incr(key);
    await this.redis.expire(key, 3600); // sliding window 1h

    if (count > 20) {
      await this.auditRepo.insert({
        action:    'anomaly.login_flood',
        ipAddress: ip,
        metadata:  { ip, count, threshold: 20, windowSeconds: 3600 },
      });
      throw new TooManyRequestsError(`Login flood detected from IP ${ip}`);
    }
  }

  /**
   * Login flood por email: 10 falhas / 15 min → loga anomalia (lockout ja gerenciado pela Phase 2)
   */
  async checkLoginFloodByEmail(email: string): Promise<void> {
    const key   = `anomaly:login:email:${email}`;
    const count = await this.redis.incr(key);
    await this.redis.expire(key, 900); // 15 min

    if (count > 10) {
      await this.auditRepo.insert({
        action:   'anomaly.login_flood_email',
        metadata: { email, count, threshold: 10, windowSeconds: 900 },
      });
      // Lockout ja tratado pelo Phase 2 AccountLockout — aqui apenas auditamos
    }
  }

  /**
   * Message flood: 50 mensagens / 1 hora por userId → 429 + loga anomalia
   */
  async checkMessageFlood(userId: string): Promise<void> {
    const key   = `anomaly:message:user:${userId}`;
    const count = await this.redis.incr(key);
    await this.redis.expire(key, 3600);

    if (count > 50) {
      await this.auditRepo.insert({
        actorId:  userId,
        action:   'anomaly.message_flood',
        resource: `user:${userId}`,
        metadata: { userId, count, threshold: 50, windowSeconds: 3600 },
      });
      throw new TooManyRequestsError('Message rate limit exceeded');
    }
  }

  /**
   * Friendship flood: 30 requests / 1 hora por userId → 429 + loga anomalia
   */
  async checkFriendshipFlood(userId: string): Promise<void> {
    const key   = `anomaly:friendship:user:${userId}`;
    const count = await this.redis.incr(key);
    await this.redis.expire(key, 3600);

    if (count > 30) {
      await this.auditRepo.insert({
        actorId:  userId,
        action:   'anomaly.friendship_flood',
        resource: `user:${userId}`,
        metadata: { userId, count, threshold: 30, windowSeconds: 3600 },
      });
      throw new TooManyRequestsError('Friendship request rate limit exceeded');
    }
  }

  /**
   * Password reset flood: 5 requests / 1 hora por IP → silencioso (nao lanca erro) + loga anomalia
   */
  async checkResetFlood(ip: string): Promise<boolean> {
    const key   = `anomaly:reset:ip:${ip}`;
    const count = await this.redis.incr(key);
    await this.redis.expire(key, 3600);

    if (count > 5) {
      await this.auditRepo.insert({
        action:    'anomaly.reset_flood',
        ipAddress: ip,
        metadata:  { ip, count, threshold: 5, windowSeconds: 3600 },
      });
      return true; // ignorar silenciosamente (nao revelar ao atacante)
    }
    return false;
  }
}
```

Criar/importar `TooManyRequestsError` no barrel de erros:
```typescript
export class TooManyRequestsError extends Error {
  readonly statusCode = 429;
  readonly code       = 'TOO_MANY_REQUESTS';
  constructor(message = 'Too many requests') {
    super(message);
    this.name = 'TooManyRequestsError';
  }
}
```

Registrar `TooManyRequestsError` no error handler do Fastify.
  </action>
  <acceptance_criteria>
    - apps/api/src/services/anomaly-detection.service.ts existe e exporta AnomalyDetectionService
    - 5 metodos implementados: checkLoginFlood, checkLoginFloodByEmail, checkMessageFlood, checkFriendshipFlood, checkResetFlood
    - Cada metodo usa redis.incr + redis.expire (sem dependencias externas alem de ioredis)
    - Cada metodo chama auditRepo.insert com action='anomaly.*' quando threshold ultrapassado
    - checkResetFlood retorna boolean e NAO lanca excecao (silencioso)
    - Thresholds correspondem exatamente a D-03: login/IP=20, login/email=10, message=50, friendship=30, reset=5
  </acceptance_criteria>
  <done>AnomalyDetectionService implementado com 5 regras usando Redis INCR + EXPIRE.</done>
</task>

<task type="auto">
  <name>Task 4: Integrar AnomalyDetectionService nos services relevantes</name>
  <files>
    apps/api/src/services/auth.service.ts,
    apps/api/src/services/message.service.ts,
    apps/api/src/services/friendship.service.ts
  </files>
  <read_first>
    - apps/api/src/services/auth.service.ts
    - apps/api/src/services/message.service.ts
    - apps/api/src/services/friendship.service.ts
  </read_first>
  <action>
Injetar `AnomalyDetectionService` nos services e chamar as verificacoes nos pontos corretos:

**AuthService:**
- Em `login()` quando credenciais sao invalidas: `await this.anomalyService.checkLoginFlood(ip)`
- Em `forgotPassword()`: `const silent = await this.anomalyService.checkResetFlood(ip); if (silent) return;`
- Construtor: adicionar `anomalyService: AnomalyDetectionService` como parametro

**MessageService:**
- Em `createMessage()` antes de inserir: `await this.anomalyService.checkMessageFlood(userId)`
- Construtor: adicionar `anomalyService: AnomalyDetectionService`

**FriendshipService:**
- Em `sendRequest()` antes de criar: `await this.anomalyService.checkFriendshipFlood(requesterId)`
- Construtor: adicionar `anomalyService: AnomalyDetectionService`

Atualizar o container DI / factory de services para injetar `AnomalyDetectionService` (verificar como services sao instanciados no projeto — geralmente em `apps/api/src/container.ts` ou similar).

As rotas que chamam esses services precisam passar `request.ip` para os services via parametro ou via context.
  </action>
  <acceptance_criteria>
    - grep -n "anomalyService\|AnomalyDetection" apps/api/src/services/auth.service.ts retorna >= 2 ocorrencias
    - grep -n "anomalyService\|AnomalyDetection" apps/api/src/services/message.service.ts retorna >= 1 ocorrencia
    - grep -n "anomalyService\|AnomalyDetection" apps/api/src/services/friendship.service.ts retorna >= 1 ocorrencia
    - AnomalyDetectionService instanciado e injetado no container/factory
    - Arquivo compila sem erros TypeScript
  </acceptance_criteria>
  <done>AnomalyDetectionService integrado em AuthService, MessageService e FriendshipService.</done>
</task>

<task type="auto">
  <name>Task 5: Auditoria de rate limiting — verificar todos os endpoints de mutacao</name>
  <files>
    apps/api/src/plugins/rate-limit.ts,
    apps/api/src/routes/auth.routes.ts,
    apps/api/src/routes/users.routes.ts,
    apps/api/src/routes/friendships.routes.ts,
    apps/api/src/routes/messages.routes.ts
  </files>
  <read_first>
    - .planning/phases/07-hardening/07-CONTEXT.md (D-05: lista completa de endpoints e limites)
    - apps/api/src/routes/ (todos os arquivos de rotas para verificar @fastify/rate-limit)
    - apps/api/src/app.ts (como @fastify/rate-limit e configurado globalmente)
  </read_first>
  <action>
Verificar e garantir que `@fastify/rate-limit` esta configurado nos seguintes endpoints com os limites exatos de D-05:

| Endpoint | Limite | Chave |
|----------|--------|-------|
| POST /v1/auth/* (login, register) | 10 req/min | por IP |
| POST /v1/auth/forgot-password | 5 req/hora | por IP |
| POST /v1/users/:username/wall | 20 req/hora | por userId |
| POST /v1/friendships | 30 req/hora | por userId |
| PUT /v1/users/me/profile | 30 req/min | por userId |
| POST /v1/messages/:id/report | 10 req/hora | por userId |

Para cada endpoint, adicionar config de rate limit se ausente. Padrao:

```typescript
// Em cada route handler que precisa de rate limit especifico:
{
  config: {
    rateLimit: {
      max: 10,
      timeWindow: '1 minute',
      keyGenerator: (request) => request.ip, // ou request.user?.sub para por userId
    }
  }
}
```

OU configurar no plugin global com `skipRoutes` / `addContentTypeParser` conforme convencao do projeto.

Verificar se o rate limit global do Fastify cobre os endpoints de auth (se sim, apenas documentar — nao duplicar). Adicionar apenas onde FALTA cobertura.

Criar/atualizar `apps/api/src/plugins/rate-limit.ts` com configuracao centralizada se o projeto ainda nao tiver:

```typescript
import fp from 'fastify-plugin';
import rateLimit from '@fastify/rate-limit';

export const rateLimitPlugin = fp(async (fastify) => {
  await fastify.register(rateLimit, {
    global: true,
    max: 100,           // default: 100 req/min para rotas nao especificadas
    timeWindow: '1 minute',
    redis: fastify.redis, // usar Redis para contadores distribuidos
    keyGenerator: (request) => request.ip,
    errorResponseBuilder: (request, context) => ({
      code: 'RATE_LIMIT_EXCEEDED',
      message: `Too many requests. Retry after ${context.after}`,
      retryAfter: context.after,
    }),
  });
});
```
  </action>
  <acceptance_criteria>
    - grep -rn "rateLimit\|rate-limit\|rate_limit" apps/api/src/routes/auth.routes.ts retorna >= 1 ocorrencia
    - grep -rn "rateLimit\|rate-limit\|rate_limit" apps/api/src/routes/messages.routes.ts retorna >= 1 ocorrencia
    - grep -rn "rateLimit\|rate-limit\|rate_limit" apps/api/src/routes/friendships.routes.ts retorna >= 1 ocorrencia
    - Todos os 6 endpoints de D-05 tem rate limit configurado (verificar manualmente cada um)
    - @fastify/rate-limit usa Redis como store (nao memoria — para ser compativel com multi-instance)
  </acceptance_criteria>
  <done>Rate limiting auditado e configurado em todos os 6 endpoints de mutacao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Testes Vitest — AnomalyDetectionService</name>
  <files>apps/api/tests/services/anomaly-detection.test.ts</files>
  <read_first>
    - apps/api/src/services/anomaly-detection.service.ts
    - apps/api/tests/ (convencoes de testes, helpers de mock)
    - .planning/phases/07-hardening/07-CONTEXT.md (D-03: thresholds para usar nos testes)
  </read_first>
  <action>
**RED:** Criar testes falhando primeiro.

`apps/api/tests/services/anomaly-detection.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { AnomalyDetectionService } from '../../src/services/anomaly-detection.service';

describe('AnomalyDetectionService', () => {
  let service: AnomalyDetectionService;
  let redisMock: { incr: ReturnType<typeof vi.fn>; expire: ReturnType<typeof vi.fn> };
  let auditMock: { insert: ReturnType<typeof vi.fn> };

  beforeEach(() => {
    redisMock = { incr: vi.fn(), expire: vi.fn().mockResolvedValue(1) };
    auditMock = { insert: vi.fn().mockResolvedValue(undefined) };
    service = new AnomalyDetectionService(redisMock as any, auditMock as any);
  });

  describe('checkLoginFlood (IP)', () => {
    it('nao lanca erro quando abaixo do threshold (20)', async () => {
      redisMock.incr.mockResolvedValue(15);
      await expect(service.checkLoginFlood('1.2.3.4')).resolves.not.toThrow();
      expect(auditMock.insert).not.toHaveBeenCalled();
    });

    it('lanca TooManyRequestsError e loga anomalia quando count > 20', async () => {
      redisMock.incr.mockResolvedValue(21);
      await expect(service.checkLoginFlood('1.2.3.4')).rejects.toThrow('Login flood');
      expect(auditMock.insert).toHaveBeenCalledWith(
        expect.objectContaining({ action: 'anomaly.login_flood', ipAddress: '1.2.3.4' })
      );
    });

    it('usa chave Redis com formato anomaly:login:ip:{ip}', async () => {
      redisMock.incr.mockResolvedValue(1);
      await service.checkLoginFlood('10.0.0.1');
      expect(redisMock.incr).toHaveBeenCalledWith('anomaly:login:ip:10.0.0.1');
    });

    it('define expire de 3600 segundos (1 hora)', async () => {
      redisMock.incr.mockResolvedValue(1);
      await service.checkLoginFlood('1.2.3.4');
      expect(redisMock.expire).toHaveBeenCalledWith('anomaly:login:ip:1.2.3.4', 3600);
    });
  });

  describe('checkMessageFlood', () => {
    it('lanca erro e loga anomalia quando count > 50', async () => {
      redisMock.incr.mockResolvedValue(51);
      await expect(service.checkMessageFlood('user-123')).rejects.toThrow();
      expect(auditMock.insert).toHaveBeenCalledWith(
        expect.objectContaining({ action: 'anomaly.message_flood', actorId: 'user-123' })
      );
    });

    it('nao lanca erro quando count = 50 (limite exato)', async () => {
      redisMock.incr.mockResolvedValue(50);
      await expect(service.checkMessageFlood('user-123')).resolves.not.toThrow();
    });
  });

  describe('checkFriendshipFlood', () => {
    it('lanca erro e loga anomalia quando count > 30', async () => {
      redisMock.incr.mockResolvedValue(31);
      await expect(service.checkFriendshipFlood('user-456')).rejects.toThrow();
      expect(auditMock.insert).toHaveBeenCalledWith(
        expect.objectContaining({ action: 'anomaly.friendship_flood' })
      );
    });
  });

  describe('checkResetFlood', () => {
    it('retorna false (nao bloquear) quando abaixo do threshold (5)', async () => {
      redisMock.incr.mockResolvedValue(3);
      const result = await service.checkResetFlood('1.2.3.4');
      expect(result).toBe(false);
      expect(auditMock.insert).not.toHaveBeenCalled();
    });

    it('retorna true e loga anomalia quando count > 5 (silencioso — sem excecao)', async () => {
      redisMock.incr.mockResolvedValue(6);
      const result = await service.checkResetFlood('1.2.3.4');
      expect(result).toBe(true); // silencioso
      expect(auditMock.insert).toHaveBeenCalledWith(
        expect.objectContaining({ action: 'anomaly.reset_flood' })
      );
    });

    it('NAO lanca excecao (ataque silencioso)', async () => {
      redisMock.incr.mockResolvedValue(100);
      await expect(service.checkResetFlood('1.2.3.4')).resolves.not.toThrow();
    });
  });
});
```

**GREEN:** Verificar que os testes passam com a implementacao da Task 3.

Executar: `pnpm --filter @parabuains/api test`
  </action>
  <acceptance_criteria>
    - pnpm --filter @parabuains/api test retorna PASS para todos os testes de anomaly-detection
    - Testes cobrem: cada uma das 4 regras de anomalia
    - Testes verificam: formato da chave Redis, TTL (expire), action do audit log, comportamento silencioso do checkResetFlood
    - Testes verificam que limites exatos sao respeitados (count = threshold nao dispara, count = threshold+1 dispara)
  </acceptance_criteria>
  <done>Testes Vitest passando para AnomalyDetectionService com todos os thresholds validados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| Redis counter → service | Contador nao e autenticado — proteger contra manipulacao com chave previsivel via IP |
| TOTP code → service layer | totpCode vem do cliente via HTTPS — validar com constant-time comparison |
| checkResetFlood resultado | Retorno booleano — logica de "ignorar silenciosamente" deve estar no service, nao exposta ao cliente |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposicao | Mitigacao |
|-----------|-----------|-----------|------------|-----------|
| T-07-02-01 | Spoofing | checkLoginFlood IP | mitigate | Usar request.ip com trust proxy configurado; checar X-Forwarded-For apenas se proxy confiavel |
| T-07-02-02 | Elevation of Privilege | 2FA enforcement | mitigate | Verificar totpEnabled antes de pular verificacao; nao confiar em parametro do cliente |
| T-07-02-03 | Denial of Service | Redis INCR | accept | Redis e in-memory; perda de counters em restart e aceitavel (janela de vulnerabilidade curta) |
| T-07-02-04 | Information Disclosure | checkResetFlood silencioso | mitigate | Nunca revelar ao cliente que o reset foi ignorado — responder com sucesso falso |
</threat_model>

<verification>
1. `grep -rn "anomalyService\|AnomalyDetection" apps/api/src/services/` — deve aparecer em auth, message e friendship services
2. `grep -rn "TotpRequired\|InvalidTotp" apps/api/src/services/` — deve aparecer em changeEmail, disable2FA, deleteAccount
3. `pnpm --filter @parabuains/api test` — todos os testes de anomaly-detection passam
4. `grep -rn "rateLimit" apps/api/src/routes/` — todos os 6 endpoints de D-05 cobertos
5. `pnpm --filter @parabuains/api type-check` — sem erros TypeScript
</verification>

<success_criteria>
- AnomalyDetectionService com 5 regras usando apenas Redis INCR + EXPIRE (zero dependencias externas)
- Cada anomalia logada em audit_logs com action='anomaly.*' e metadata com threshold e count
- 2FA enforcement em changeEmail, disable2FA e deleteAccount (TotpRequiredError / InvalidTotpError)
- Rate limiting auditado e confirmado em todos os 6 endpoints de mutacao de D-05
- Testes Vitest passando para cada regra de anomalia com thresholds exatos de D-03
</success_criteria>

<output>After completion, create `.planning/phases/07-hardening/07-02-SUMMARY.md`</output>
