---
phase: 07-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/plugins/audit.ts
  - apps/api/src/repositories/audit.repository.ts
  - apps/api/src/db/schema/audit-logs.ts
  - apps/api/src/routes/auth.routes.ts
  - apps/api/src/routes/users.routes.ts
  - apps/api/src/routes/friendships.routes.ts
  - apps/api/src/routes/messages.routes.ts
  - apps/api/src/worker/jobs/cleanup-audit-logs.ts
  - apps/api/src/worker/index.ts
  - apps/api/src/types/fastify.d.ts
  - apps/api/tests/plugins/audit.test.ts
  - apps/api/tests/jobs/cleanup-audit-logs.test.ts
autonomous: true
requirements: [cross-cutting]

must_haves:
  truths:
    - "Todas as acoes sensiveis sao auditadas no banco via onSend hook"
    - "Nenhuma rota sensivel fica sem auditAction decorado no handler"
    - "Cleanup job roda diariamente as 03:00 UTC com batched deletes de 1000 rows"
    - "Audit records sao inseridos apenas para respostas HTTP 2xx (status < 400)"
    - "Tabela audit_logs tem indices em actor_id e created_at DESC"
  artifacts:
    - path: "apps/api/src/plugins/audit.ts"
      provides: "Fastify plugin auditPlugin com onSend hook e decoradores de request"
      contains: "fastify.decorateRequest('auditAction'"
    - path: "apps/api/src/repositories/audit.repository.ts"
      provides: "AuditRepository com metodo insert para escrita no banco"
      contains: "class AuditRepository"
    - path: "apps/api/src/worker/jobs/cleanup-audit-logs.ts"
      provides: "BullMQ job que deleta audit_logs com mais de 90 dias em batches"
      contains: "cleanupAuditLogs"
    - path: "apps/api/tests/plugins/audit.test.ts"
      provides: "Testes Vitest verificando insercao de audit record por acao sensivel"
      contains: "auditAction"
  key_links:
    - from: "apps/api/src/plugins/audit.ts"
      to: "apps/api/src/repositories/audit.repository.ts"
      via: "injecao via fastify decorator fastify.auditRepo"
      pattern: "fastify.decorate('auditRepo'"
    - from: "apps/api/src/routes/auth.routes.ts"
      to: "apps/api/src/plugins/audit.ts"
      via: "request.auditAction = 'user.login'"
      pattern: "request.auditAction"
---

<objective>
Implementar o sistema de Audit Log para o Fastify API: plugin auditPlugin com onSend hook, AuditRepository, schema Drizzle da tabela audit_logs, wiring em todas as rotas sensiveis, e BullMQ scheduled job para cleanup de 90 dias.

Proposito: Rastreabilidade e compliance — toda acao sensivel (login, logout, register, delete account, change email, 2FA toggle, friendship create/accept, message delete/report) deve gerar um registro auditavel com actor, IP, timestamp e metadata.
Saida: Plugin Fastify registrado globalmente, rotas decoradas com auditAction/auditResource/auditMetadata, job de cleanup agendado, testes Vitest cobrindo cada acao sensivel.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hardening/07-CONTEXT.md
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema Drizzle — tabela audit_logs</name>
  <files>apps/api/src/db/schema/audit-logs.ts</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (secao "Audit Log Schema")
    - apps/api/src/db/schema/users.ts (para referencia de UUID e convencoes de schema existentes)
  </read_first>
  <action>
Criar o schema Drizzle para a tabela audit_logs em `apps/api/src/db/schema/audit-logs.ts`:

```typescript
import { pgTable, uuid, varchar, inet, text, jsonb, timestamp, index } from 'drizzle-orm/pg-core';
import { users } from './users';

export const auditLogs = pgTable(
  'audit_logs',
  {
    id:         uuid('id').defaultRandom().primaryKey(),
    actorId:    uuid('actor_id').references(() => users.id, { onDelete: 'set null' }),
    action:     varchar('action', { length: 100 }).notNull(),
    resource:   varchar('resource', { length: 100 }),
    ipAddress:  inet('ip_address'),
    userAgent:  text('user_agent'),
    metadata:   jsonb('metadata'),
    createdAt:  timestamp('created_at', { withTimezone: true }).defaultNow().notNull(),
  },
  (t) => ({
    idxActor:   index('idx_audit_logs_actor').on(t.actorId),
    idxCreated: index('idx_audit_logs_created').on(t.createdAt),
  }),
);

export type AuditLog       = typeof auditLogs.$inferSelect;
export type NewAuditLog    = typeof auditLogs.$inferInsert;
```

Exportar auditLogs do barrel de schema (`apps/api/src/db/schema/index.ts`) se existir.
Gerar e aplicar migration: `pnpm --filter @parabuains/db db:generate && pnpm --filter @parabuains/db db:push`
  </action>
  <acceptance_criteria>
    - apps/api/src/db/schema/audit-logs.ts existe e exporta `auditLogs`
    - Schema contem campos: id, actor_id, action, resource, ip_address, user_agent, metadata, created_at
    - Dois indices definidos: idx_audit_logs_actor e idx_audit_logs_created
    - Migration gerada sem erros de tipo TypeScript
  </acceptance_criteria>
  <done>Tabela audit_logs definida no Drizzle schema com indices corretos e migration gerada.</done>
</task>

<task type="auto">
  <name>Task 2: AuditRepository</name>
  <files>apps/api/src/repositories/audit.repository.ts</files>
  <read_first>
    - apps/api/src/db/schema/audit-logs.ts (schema recem criado)
    - apps/api/src/repositories/user.repository.ts (convencoes do projeto para repositories)
  </read_first>
  <action>
Criar `apps/api/src/repositories/audit.repository.ts`:

```typescript
import { DrizzleDB } from '../db/types';
import { auditLogs, NewAuditLog } from '../db/schema/audit-logs';

export interface AuditInsertParams {
  actorId?:   string | null;
  action:     string;
  resource?:  string | null;
  ipAddress?: string | null;
  userAgent?: string | null;
  metadata?:  Record<string, unknown> | null;
}

export class AuditRepository {
  constructor(private readonly db: DrizzleDB) {}

  async insert(params: AuditInsertParams): Promise<void> {
    await this.db.insert(auditLogs).values({
      actorId:   params.actorId   ?? null,
      action:    params.action,
      resource:  params.resource  ?? null,
      ipAddress: params.ipAddress ?? null,
      userAgent: params.userAgent ?? null,
      metadata:  params.metadata  ?? null,
    } satisfies NewAuditLog);
  }

  /** Usado pelo cleanup job: retorna numero de rows deletadas */
  async deleteBefore(cutoff: Date): Promise<number> {
    const { rowCount } = await this.db
      .delete(auditLogs)
      .where(lt(auditLogs.createdAt, cutoff))
      .limit(1000);
    return rowCount ?? 0;
  }
}
```

Importar `lt` de `drizzle-orm`. Exportar `AuditRepository` do barrel de repositories se existir.
  </action>
  <acceptance_criteria>
    - apps/api/src/repositories/audit.repository.ts existe e exporta `AuditRepository`
    - Metodo `insert` aceita AuditInsertParams e insere na tabela
    - Metodo `deleteBefore` aceita Date e deleta com LIMIT 1000 retornando rowCount
    - Arquivo compila sem erros TypeScript
  </acceptance_criteria>
  <done>AuditRepository implementado com insert e deleteBefore.</done>
</task>

<task type="auto">
  <name>Task 3: auditPlugin — Fastify plugin com onSend hook e decoradores</name>
  <files>apps/api/src/plugins/audit.ts, apps/api/src/types/fastify.d.ts</files>
  <read_first>
    - .planning/phases/07-hardening/07-CONTEXT.md (secao "Audit Log Fastify Plugin" em Specifics)
    - apps/api/src/plugins/ (listar plugins existentes para entender convencoes de registro)
    - apps/api/src/types/fastify.d.ts (augmentacoes de tipo existentes, se houver)
  </read_first>
  <action>
Criar `apps/api/src/plugins/audit.ts`:

```typescript
import fp from 'fastify-plugin';
import { AuditRepository } from '../repositories/audit.repository';

export const auditPlugin = fp(async (fastify) => {
  const auditRepo = new AuditRepository(fastify.db);
  fastify.decorate('auditRepo', auditRepo);

  fastify.decorateRequest('auditAction',   null);
  fastify.decorateRequest('auditResource', null);
  fastify.decorateRequest('auditMetadata', null);

  fastify.addHook('onSend', async (request, reply, payload) => {
    if (!request.auditAction) return payload;
    // Somente logar operacoes bem-sucedidas (2xx)
    if (reply.statusCode >= 400) return payload;

    try {
      await auditRepo.insert({
        actorId:   (request.user as any)?.sub ?? null,
        action:    request.auditAction,
        resource:  request.auditResource ?? null,
        ipAddress: request.ip,
        userAgent: request.headers['user-agent'] ?? null,
        metadata:  request.auditMetadata ?? null,
      });
    } catch (err) {
      // Falha no audit log NAO deve quebrar a resposta ao cliente
      fastify.log.error({ err, action: request.auditAction }, 'audit log insert failed');
    }

    return payload;
  });
});
```

Atualizar `apps/api/src/types/fastify.d.ts` com augmentacao de tipos para os novos decoradores:

```typescript
import 'fastify';
import { AuditRepository } from '../repositories/audit.repository';

declare module 'fastify' {
  interface FastifyInstance {
    auditRepo: AuditRepository;
  }
  interface FastifyRequest {
    auditAction:   string | null;
    auditResource: string | null;
    auditMetadata: Record<string, unknown> | null;
  }
}
```

Registrar `auditPlugin` no app principal (`apps/api/src/app.ts` ou equivalente) APOS o plugin de DB e ANTES do registro das rotas.
  </action>
  <acceptance_criteria>
    - apps/api/src/plugins/audit.ts existe e exporta `auditPlugin`
    - Plugin usa `fp` (fastify-plugin) para encapsulamento correto
    - onSend hook: nao insere se statusCode >= 400
    - onSend hook: nao quebra resposta se insert falhar (try/catch com log)
    - Decoradores auditAction, auditResource, auditMetadata sao declarados no tipo FastifyRequest
    - Plugin registrado no app.ts antes das rotas
  </acceptance_criteria>
  <done>auditPlugin implementado e registrado. Decoradores de request tipados corretamente.</done>
</task>

<task type="auto">
  <name>Task 4: Wiring de auditAction em todas as rotas sensiveis</name>
  <files>
    apps/api/src/routes/auth.routes.ts,
    apps/api/src/routes/users.routes.ts,
    apps/api/src/routes/friendships.routes.ts,
    apps/api/src/routes/messages.routes.ts
  </files>
  <read_first>
    - apps/api/src/routes/auth.routes.ts
    - apps/api/src/routes/users.routes.ts
    - apps/api/src/routes/friendships.routes.ts
    - apps/api/src/routes/messages.routes.ts
  </read_first>
  <action>
Adicionar decoracao `request.auditAction` (e quando aplicavel `auditResource` e `auditMetadata`) no INICIO do handler de cada rota sensivel, antes de qualquer logica de negocio:

**auth.routes.ts:**
- `POST /v1/auth/login`         → `request.auditAction = 'user.login'`
- `POST /v1/auth/logout`        → `request.auditAction = 'user.logout'`
- `POST /v1/auth/register`      → `request.auditAction = 'user.register'`
- `POST /v1/auth/forgot-password` → nao auditado (operacao anonima)

**users.routes.ts:**
- `DELETE /v1/users/me`         → `request.auditAction = 'user.delete'; request.auditResource = 'user:' + request.user.sub`
- `PUT /v1/users/me/email`      → `request.auditAction = 'user.email_change'; request.auditResource = 'user:' + request.user.sub`
- `PUT /v1/users/me/password`   → `request.auditAction = 'user.password_change'; request.auditResource = 'user:' + request.user.sub`
- `POST /v1/users/me/2fa/enable`  → `request.auditAction = 'user.2fa_enable'; request.auditResource = 'user:' + request.user.sub`
- `POST /v1/users/me/2fa/disable` → `request.auditAction = 'user.2fa_disable'; request.auditResource = 'user:' + request.user.sub`

**friendships.routes.ts:**
- `POST /v1/friendships`        → `request.auditAction = 'friendship.create'; request.auditResource = 'user:' + body.targetUserId`
- `PUT /v1/friendships/:id/accept` → `request.auditAction = 'friendship.accept'; request.auditResource = 'friendship:' + params.id`

**messages.routes.ts:**
- `DELETE /v1/messages/:id`     → `request.auditAction = 'message.delete'; request.auditResource = 'message:' + params.id`
- `POST /v1/messages/:id/report` → `request.auditAction = 'message.report'; request.auditResource = 'message:' + params.id`

Padrao de implementacao em cada handler:
```typescript
async handler(request, reply) {
  request.auditAction   = 'user.login';
  request.auditResource = null; // ou o resource especifico
  request.auditMetadata = { email: request.body.email }; // apenas dados nao-sensiveis

  // ... logica de negocio existente
}
```

IMPORTANTE: NAO logar senhas, tokens ou segredos em auditMetadata. Apenas dados de identificacao (email, username, IP ja e capturado automaticamente pelo hook).
  </action>
  <acceptance_criteria>
    - grep -r "auditAction" apps/api/src/routes/ retorna no minimo 12 ocorrencias (uma por rota sensivel)
    - Nenhuma rota sensivel listada acima esta sem auditAction
    - auditMetadata nunca contem campos: password, token, secret, totp_code
    - Arquivo compila sem erros TypeScript
  </acceptance_criteria>
  <done>Todas as 12 rotas sensiveis decoradas com auditAction/auditResource/auditMetadata.</done>
</task>

<task type="auto">
  <name>Task 5: BullMQ scheduled job — cleanup de audit_logs (90 dias)</name>
  <files>
    apps/api/src/worker/jobs/cleanup-audit-logs.ts,
    apps/api/src/worker/index.ts
  </files>
  <read_first>
    - .planning/phases/07-hardening/07-CONTEXT.md (secao "Cleanup Job" em Specifics)
    - apps/api/src/worker/index.ts (como jobs existentes sao registrados)
    - apps/api/src/worker/jobs/ (convencoes de jobs existentes)
  </read_first>
  <action>
Criar `apps/api/src/worker/jobs/cleanup-audit-logs.ts`:

```typescript
import { Job } from 'bullmq';
import { DrizzleDB } from '../../db/types';
import { AuditRepository } from '../../repositories/audit.repository';

export const CLEANUP_AUDIT_JOB_NAME = 'cleanup-audit-logs';

export async function cleanupAuditLogs(job: Job, db: DrizzleDB): Promise<{ deleted: number }> {
  const auditRepo = new AuditRepository(db);
  const cutoff    = new Date(Date.now() - 90 * 24 * 60 * 60 * 1000);

  let totalDeleted = 0;
  let deleted: number;

  do {
    deleted       = await auditRepo.deleteBefore(cutoff);
    totalDeleted += deleted;
    if (deleted > 0) {
      job.log(`Deleted ${deleted} audit_logs rows (total so far: ${totalDeleted})`);
    }
  } while (deleted === 1000); // continua enquanto houver mais rows para deletar

  return { deleted: totalDeleted };
}
```

Em `apps/api/src/worker/index.ts`, registrar o job scheduler para execucao diaria as 03:00 UTC:

```typescript
import { QueueScheduler, Worker, Queue } from 'bullmq';
import { CLEANUP_AUDIT_JOB_NAME, cleanupAuditLogs } from './jobs/cleanup-audit-logs';

// Registrar job scheduler (idempotente — upsert)
const cleanupQueue = new Queue('maintenance', { connection: redis });
await cleanupQueue.upsertJobScheduler(
  CLEANUP_AUDIT_JOB_NAME,
  { pattern: '0 3 * * *' }, // diariamente as 03:00 UTC
  { name: CLEANUP_AUDIT_JOB_NAME, data: {} },
);

// Worker handler
worker.on('job', async (job) => {
  if (job.name === CLEANUP_AUDIT_JOB_NAME) {
    return cleanupAuditLogs(job, db);
  }
});
```

Adaptar ao padrao real de registro de workers/queues do projeto (verificar worker/index.ts).
  </action>
  <acceptance_criteria>
    - apps/api/src/worker/jobs/cleanup-audit-logs.ts existe e exporta cleanupAuditLogs
    - Funcao usa loop do-while com LIMIT 1000 (batch deletes)
    - Job agendado com cron `0 3 * * *` (03:00 UTC diariamente)
    - upsertJobScheduler usado (idempotente — sem jobs duplicados em restart)
    - Arquivo compila sem erros TypeScript
  </acceptance_criteria>
  <done>Cleanup job implementado com batched deletes e agendamento diario as 03:00 UTC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Testes Vitest — audit log por acao sensivel</name>
  <files>
    apps/api/tests/plugins/audit.test.ts,
    apps/api/tests/jobs/cleanup-audit-logs.test.ts
  </files>
  <read_first>
    - apps/api/src/plugins/audit.ts
    - apps/api/src/repositories/audit.repository.ts
    - apps/api/tests/ (convencoes de testes existentes — helpers, mocks, setup)
  </read_first>
  <action>
**RED:** Criar testes falhando primeiro.

`apps/api/tests/plugins/audit.test.ts` — testar que cada acao sensivel gera audit record:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import Fastify from 'fastify';
import { auditPlugin } from '../../src/plugins/audit';

describe('auditPlugin', () => {
  let insertMock: ReturnType<typeof vi.fn>;
  let app: ReturnType<typeof Fastify>;

  beforeEach(async () => {
    insertMock = vi.fn().mockResolvedValue(undefined);
    app = Fastify();

    // Mock fastify.db
    app.decorate('db', {});

    // Mock AuditRepository
    vi.doMock('../../src/repositories/audit.repository', () => ({
      AuditRepository: vi.fn().mockImplementation(() => ({ insert: insertMock })),
    }));

    await app.register(auditPlugin);

    // Rota de teste com auditAction decorado
    app.post('/test-login', async (request, reply) => {
      request.auditAction = 'user.login';
      request.auditMetadata = { email: 'test@example.com' };
      return { ok: true };
    });

    await app.ready();
  });

  it('insere audit record para user.login com status 200', async () => {
    const res = await app.inject({ method: 'POST', url: '/test-login' });
    expect(res.statusCode).toBe(200);
    expect(insertMock).toHaveBeenCalledOnce();
    expect(insertMock).toHaveBeenCalledWith(
      expect.objectContaining({ action: 'user.login' })
    );
  });

  it('NAO insere audit record quando statusCode >= 400', async () => {
    app.post('/test-fail', async (request, reply) => {
      request.auditAction = 'user.login';
      reply.status(401);
      return { error: 'Unauthorized' };
    });
    const res = await app.inject({ method: 'POST', url: '/test-fail' });
    expect(res.statusCode).toBe(401);
    expect(insertMock).not.toHaveBeenCalled();
  });

  it('NAO insere audit record quando auditAction nao esta definido', async () => {
    app.get('/test-no-audit', async () => ({ ok: true }));
    await app.inject({ method: 'GET', url: '/test-no-audit' });
    expect(insertMock).not.toHaveBeenCalled();
  });

  it('nao quebra a resposta se insert falhar', async () => {
    insertMock.mockRejectedValue(new Error('DB connection lost'));
    const res = await app.inject({ method: 'POST', url: '/test-login' });
    expect(res.statusCode).toBe(200); // resposta nao deve ser afetada
  });
});
```

`apps/api/tests/jobs/cleanup-audit-logs.test.ts`:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { cleanupAuditLogs } from '../../src/worker/jobs/cleanup-audit-logs';

describe('cleanupAuditLogs', () => {
  it('deleta em batches ate nao haver mais rows', async () => {
    const deleteBefore = vi.fn()
      .mockResolvedValueOnce(1000) // primeiro batch: cheio
      .mockResolvedValueOnce(500)  // segundo batch: ultimo
      .mockResolvedValue(0);

    vi.doMock('../../src/repositories/audit.repository', () => ({
      AuditRepository: vi.fn().mockImplementation(() => ({ deleteBefore })),
    }));

    const mockJob = { log: vi.fn() } as any;
    const result  = await cleanupAuditLogs(mockJob, {} as any);

    expect(deleteBefore).toHaveBeenCalledTimes(2);
    expect(result.deleted).toBe(1500);
  });

  it('retorna 0 quando nao ha rows a deletar', async () => {
    vi.doMock('../../src/repositories/audit.repository', () => ({
      AuditRepository: vi.fn().mockImplementation(() => ({
        deleteBefore: vi.fn().mockResolvedValue(0),
      })),
    }));
    const result = await cleanupAuditLogs({ log: vi.fn() } as any, {} as any);
    expect(result.deleted).toBe(0);
  });
});
```

**GREEN:** Verificar que os testes passam apos implementacao das Tasks 1-5.

Executar: `pnpm --filter @parabuains/api test`
  </action>
  <acceptance_criteria>
    - pnpm --filter @parabuains/api test retorna PASS para todos os testes de audit
    - Teste cobre: insert em operacao bem-sucedida, nao-insert em erro 4xx, nao-insert sem auditAction, resiliencia a falha de DB
    - Teste do cleanup job cobre: batch loop, contagem total, caso vazio
  </acceptance_criteria>
  <done>Testes Vitest passando para auditPlugin e cleanupAuditLogs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| onSend hook → audit_logs | O hook recebe dados do request ja validados pelo JWT middleware — actor_id e confiavel |
| auditMetadata | Handler preenche manualmente — NUNCA incluir senhas, tokens ou segredos |
| Cleanup job → DB | Deleta dados proprios da aplicacao — sem acesso externo; idempotente via batched deletes |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposicao | Mitigacao |
|-----------|-----------|-----------|------------|-----------|
| T-07-01-01 | Information Disclosure | auditMetadata | mitigate | Documentar e testar que auditMetadata nao contem campos sensiveis (password, token, secret) |
| T-07-01-02 | Denial of Service | onSend hook | mitigate | try/catch no insert — falha silenciosa com log; nao bloqueia response |
| T-07-01-03 | Tampering | audit_logs table | accept | Read-only para app user em producao; apenas INSERT via AuditRepository |
| T-07-01-04 | Information Disclosure | audit_logs retention | mitigate | Cleanup job garante dados nao ficam alem de 90 dias (LGPD/GDPR alignment) |
</threat_model>

<verification>
1. `grep -r "auditAction" apps/api/src/routes/` — deve retornar >= 12 ocorrencias
2. `grep -r "auditPlugin" apps/api/src/app.ts` — deve aparecer o registro do plugin
3. `pnpm --filter @parabuains/api test` — todos os testes de audit passam
4. `grep -r "cleanup-audit-logs" apps/api/src/worker/` — job deve estar registrado no worker
5. `pnpm --filter @parabuains/api type-check` — sem erros de TypeScript
</verification>

<success_criteria>
- auditPlugin registrado globalmente no Fastify app
- Todas as 12 acoes sensiveis decoradas com auditAction (login, logout, register, delete account, change email, password change, 2FA enable/disable, friendship create/accept, message delete/report)
- onSend hook insere apenas para respostas 2xx e nao bloqueia response em caso de falha
- BullMQ job agendado com cron 0 3 * * * (03:00 UTC) com batched deletes de 1000 rows
- Testes Vitest passando para plugin e job
</success_criteria>

<output>After completion, create `.planning/phases/07-hardening/07-01-SUMMARY.md`</output>
