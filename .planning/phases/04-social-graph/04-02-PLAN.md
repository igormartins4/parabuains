---
phase: 04-social-graph
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/api/src/modules/feed/feed.repository.ts
  - apps/api/src/modules/feed/feed.service.ts
  - apps/api/src/modules/feed/feed.routes.ts
  - apps/api/src/modules/feed/feed.plugin.ts
  - apps/api/src/modules/notifications/sse.routes.ts
  - apps/api/src/modules/notifications/sse.plugin.ts
  - apps/api/src/queues/notifications.queue.ts
  - apps/api/src/lib/redis-sub.ts
  - apps/web/src/app/api/feed/route.ts
autonomous: true
requirements: [SOCL-06, SOCL-07]

must_haves:
  truths:
    - "GET /v1/feed/birthdays retorna { today: [], upcoming: [] } com amigos dos próximos 30 dias"
    - "Feed é timezone-aware: usa timezone do usuário autenticado para calcular 'hoje'"
    - "Aniversariantes do dia aparecem em 'today' separados dos próximos em 'upcoming'"
    - "BullMQ job 'friendship_accepted' é enfileirado quando pedido aceito (sem consumer em Fase 4)"
    - "SSE endpoint GET /v1/notifications/stream emite heartbeat a cada 30s e fecha após 5min idle"
    - "Redis Pub/Sub publica evento em 'user:{userId}:events' ao aceitar amizade"
  artifacts:
    - path: "apps/api/src/modules/feed/feed.repository.ts"
      provides: "FeedRepository com query de candidatos (janela 33 dias)"
      contains: "MAKE_DATE(EXTRACT(YEAR FROM NOW()"
    - path: "apps/api/src/modules/feed/feed.service.ts"
      provides: "FeedService com ordenação timezone-aware usando Luxon"
      contains: "DateTime.now().setZone(timezone)"
    - path: "apps/api/src/modules/notifications/sse.routes.ts"
      provides: "SSE endpoint com Redis subscriber e heartbeat"
      contains: "reply.raw.write(': heartbeat\\n\\n')"
    - path: "apps/api/src/queues/notifications.queue.ts"
      provides: "BullMQ Queue 'notifications' para uso em Fases 4 e 6"
      contains: "new Queue('notifications'"
  key_links:
    - from: "apps/api/src/modules/friendships/friendship.routes.ts (04-01)"
      to: "apps/api/src/queues/notifications.queue.ts"
      via: "Ao aceitar amizade, enqueue job 'friendship_accepted'"
      pattern: "await notificationsQueue.add('friendship_accepted', { ... })"
    - from: "apps/api/src/modules/friendships/friendship.routes.ts (04-01)"
      to: "apps/api/src/lib/redis-sub.ts"
      via: "Ao aceitar amizade, publish evento SSE no canal do requester"
      pattern: "await redisPub.publish(`user:${requesterId}:events`, ...)"
    - from: "apps/web/src/app/api/feed/route.ts"
      to: "apps/api/src/modules/feed/feed.routes.ts"
      via: "BFF Next.js faz proxy para Fastify API e aplica refinamento Luxon"
      pattern: "fetch(`${INTERNAL_API_URL}/v1/feed/birthdays`"
---

<objective>
Implementar o feed de aniversários (30 dias, timezone-aware) com SSE in-app notifications e BullMQ queue stub para a Fase 6.

Purpose: Depende de 04-01 (FriendshipRepository.listFriends e tabela friendships pronta). O feed é a funcionalidade de maior valor para o usuário — ver quando são os aniversários dos amigos.
Output: Endpoint de feed funcionando com separação hoje/próximos, SSE para notificações em tempo real, e queue BullMQ configurada para uso na Fase 6.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-social-graph/04-CONTEXT.md
@.planning/phases/04-social-graph/RESEARCH.md
@.planning/ROADMAP.md
@packages/db/src/schema/app.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Luxon e criar FeedRepository</name>
  <files>apps/api/package.json, apps/web/package.json, apps/api/src/modules/feed/feed.repository.ts</files>
  <read_first>
    - apps/api/package.json (verificar se luxon já está instalado)
    - packages/db/src/schema/app.ts (campos de users relevantes: birthDate, birthYearHidden, timezone)
    - .planning/phases/04-social-graph/RESEARCH.md §4 (query de candidatos para feed)
  </read_first>
  <action>
Instalar Luxon se não presente:
```bash
cd apps/api && pnpm add luxon
cd apps/api && pnpm add -D @types/luxon
cd apps/web && pnpm add luxon
cd apps/web && pnpm add -D @types/luxon
```

Criar `apps/api/src/modules/feed/feed.repository.ts`:
```typescript
import { sql, eq, and, or } from 'drizzle-orm';
import type { DrizzleDB } from '../../types/db';
import { users, friendships } from '@parabuains/db';

export interface BirthdayCandidate {
  id: string;
  username: string;
  displayName: string;
  avatarUrl: string | null;
  birthDate: string;         // ISO date string "YYYY-MM-DD"
  birthYearHidden: boolean;
  timezone: string;
}

export class FeedRepository {
  constructor(private readonly db: DrizzleDB) {}

  /**
   * Retorna amigos do userId cujo aniversário cai nos próximos ~33 dias (UTC aproximado).
   * O BFF refina a janela exata por timezone.
   */
  async getBirthdayCandidates(userId: string): Promise<BirthdayCandidate[]> {
    const rows = await this.db.execute(sql`
      SELECT
        u.id,
        u.username,
        u.display_name AS "displayName",
        u.avatar_url   AS "avatarUrl",
        u.birth_date   AS "birthDate",
        u.birth_year_hidden AS "birthYearHidden",
        u.timezone
      FROM users u
      WHERE u.id IN (
        SELECT CASE
          WHEN f.requester_id = ${userId} THEN f.addressee_id
          ELSE f.requester_id
        END
        FROM friendships f
        WHERE f.status = 'accepted'
          AND (f.requester_id = ${userId} OR f.addressee_id = ${userId})
      )
      AND (
        -- Aniversário nos próximos 33 dias neste ano
        MAKE_DATE(
          EXTRACT(YEAR FROM NOW())::int,
          EXTRACT(MONTH FROM u.birth_date)::int,
          EXTRACT(DAY FROM u.birth_date)::int
        ) BETWEEN NOW()::date AND (NOW() + INTERVAL '33 days')::date
        OR
        -- Virada de ano: aniversário em janeiro quando estamos em dezembro
        MAKE_DATE(
          EXTRACT(YEAR FROM NOW())::int + 1,
          EXTRACT(MONTH FROM u.birth_date)::int,
          EXTRACT(DAY FROM u.birth_date)::int
        ) BETWEEN NOW()::date AND (NOW() + INTERVAL '33 days')::date
      )
      ORDER BY
        EXTRACT(MONTH FROM u.birth_date),
        EXTRACT(DAY FROM u.birth_date)
    `);

    return rows.rows as BirthdayCandidate[];
  }
}
```
  </action>
  <acceptance_criteria>
    - feed.repository.ts exporta `FeedRepository` e interface `BirthdayCandidate`
    - Query usa `MAKE_DATE` para calcular próximo aniversário
    - Query inclui tratamento de virada de ano (+ 1 ano)
    - `getBirthdayCandidates` usa `db.execute(sql`...`)` (raw SQL via Drizzle)
  </acceptance_criteria>
  <done>Luxon instalado e FeedRepository com query de candidatos de aniversário criado.</done>
</task>

<task type="auto">
  <name>Task 2: FeedService com lógica timezone-aware</name>
  <files>apps/api/src/modules/feed/feed.service.ts</files>
  <read_first>
    - apps/api/src/modules/feed/feed.repository.ts (recém criado — BirthdayCandidate type)
    - .planning/phases/04-social-graph/RESEARCH.md §4 (refinamento Luxon)
    - .planning/phases/04-social-graph/04-CONTEXT.md §D-06 (estrutura da resposta do feed)
  </read_first>
  <action>
Criar `apps/api/src/modules/feed/feed.service.ts`:
```typescript
import { DateTime } from 'luxon';
import { FeedRepository, BirthdayCandidate } from './feed.repository';

export interface BirthdayEntry {
  userId: string;
  username: string;
  displayName: string;
  avatarUrl: string | null;
  nextBirthday: string;   // ISO date "YYYY-MM-DD" no timezone do viewer
  daysUntil: number;
  birthMonthDay: string;  // "MM-DD" para exibição (sem ano)
  birthYear?: number;     // presente apenas se birthYearHidden = false
}

export interface BirthdayFeed {
  today: BirthdayEntry[];
  upcoming: BirthdayEntry[];
}

export class FeedService {
  constructor(private readonly repo: FeedRepository) {}

  async getBirthdayFeed(userId: string, viewerTimezone: string): Promise<BirthdayFeed> {
    const candidates = await this.repo.getBirthdayCandidates(userId);
    const now = DateTime.now().setZone(viewerTimezone);

    const today: BirthdayEntry[] = [];
    const upcoming: BirthdayEntry[] = [];

    for (const candidate of candidates) {
      const entry = this.buildBirthdayEntry(candidate, now);
      if (!entry) continue;

      if (entry.daysUntil === 0) {
        today.push(entry);
      } else if (entry.daysUntil <= 30) {
        upcoming.push(entry);
      }
      // daysUntil > 30: descartar (era candidato extra da janela de 33 dias)
    }

    upcoming.sort((a, b) => a.daysUntil - b.daysUntil);

    return { today, upcoming };
  }

  private buildBirthdayEntry(
    candidate: BirthdayCandidate,
    now: DateTime
  ): BirthdayEntry | null {
    // Extrair mês e dia do birth_date (formato "YYYY-MM-DD" do PostgreSQL date)
    const parts = candidate.birthDate.split('-');
    const month = parseInt(parts[1], 10);
    const day = parseInt(parts[2], 10);
    const year = parseInt(parts[0], 10);

    // Calcular próximo aniversário no timezone do viewer
    let nextBirthday: DateTime;
    try {
      nextBirthday = DateTime.fromObject(
        { year: now.year, month, day },
        { zone: now.zoneName! }
      );
    } catch {
      // 29 de fevereiro em ano não-bissexto — tratar como 1º de março
      try {
        nextBirthday = DateTime.fromObject(
          { year: now.year, month: 3, day: 1 },
          { zone: now.zoneName! }
        );
      } catch {
        return null; // data inválida — pular
      }
    }

    // Se já passou neste ano, usar o próximo ano
    if (nextBirthday.startOf('day') < now.startOf('day')) {
      nextBirthday = nextBirthday.plus({ years: 1 });
    }

    const daysUntil = Math.round(nextBirthday.startOf('day').diff(now.startOf('day'), 'days').days);

    const monthStr = String(month).padStart(2, '0');
    const dayStr = String(day).padStart(2, '0');

    return {
      userId: candidate.id,
      username: candidate.username,
      displayName: candidate.displayName,
      avatarUrl: candidate.avatarUrl,
      nextBirthday: nextBirthday.toISODate()!,
      daysUntil,
      birthMonthDay: `${monthStr}-${dayStr}`,
      ...(candidate.birthYearHidden ? {} : { birthYear: year }),
    };
  }
}
```
  </action>
  <acceptance_criteria>
    - feed.service.ts importa `DateTime` de `luxon`
    - `buildBirthdayEntry` trata 29 de fevereiro com fallback para 1º de março
    - `daysUntil === 0` vai para `today`, `<= 30` vai para `upcoming`, `> 30` descartado
    - `upcoming` é ordenado por `daysUntil ASC`
    - `birthYear` é omitido quando `birthYearHidden === true`
  </acceptance_criteria>
  <done>FeedService com cálculo timezone-aware de próximo aniversário usando Luxon.</done>
</task>

<task type="auto">
  <name>Task 3: Feed Fastify routes + plugin</name>
  <files>apps/api/src/modules/feed/feed.routes.ts, apps/api/src/modules/feed/feed.plugin.ts</files>
  <read_first>
    - apps/api/src/modules/feed/feed.service.ts (recém criado)
    - apps/api/src/modules/users/user.repository.ts (para obter timezone do viewer)
    - apps/api/src/app.ts (para registrar novo plugin)
  </read_first>
  <action>
Criar `apps/api/src/modules/feed/feed.routes.ts`:
```typescript
import type { FastifyInstance } from 'fastify';
import { FeedRepository } from './feed.repository';
import { FeedService } from './feed.service';
import { UserRepository } from '../users/user.repository';

export async function feedRoutes(fastify: FastifyInstance) {
  const feedRepo = new FeedRepository(fastify.db);
  const feedService = new FeedService(feedRepo);
  const userRepo = new UserRepository(fastify.db);

  // GET /v1/feed/birthdays — feed de aniversários dos próximos 30 dias
  fastify.get('/v1/feed/birthdays', { onRequest: [fastify.authenticate] }, async (request, reply) => {
    // Obter timezone do usuário autenticado
    const viewer = await userRepo.findById(request.user.id);
    const viewerTimezone = viewer?.timezone ?? 'UTC';

    const feed = await feedService.getBirthdayFeed(request.user.id, viewerTimezone);
    return reply.send(feed);
  });
}
```

Criar `apps/api/src/modules/feed/feed.plugin.ts`:
```typescript
import fp from 'fastify-plugin';
import { feedRoutes } from './feed.routes';

export const feedPlugin = fp(feedRoutes, {
  name: 'feed',
  dependencies: ['db', 'authenticate'],
});
```

Registrar em `apps/api/src/app.ts`:
```typescript
import { feedPlugin } from './modules/feed/feed.plugin';
// ...
fastify.register(feedPlugin);
```
  </action>
  <acceptance_criteria>
    - feed.routes.ts registra `GET /v1/feed/birthdays` com `onRequest: [fastify.authenticate]`
    - Timezone é obtido do perfil do viewer via `userRepo.findById`
    - Fallback: `viewer?.timezone ?? 'UTC'`
    - feed.plugin.ts usa `fp()` com `dependencies: ['db', 'authenticate']`
  </acceptance_criteria>
  <done>Feed Fastify routes com timezone-aware do viewer e plugin registrado.</done>
</task>

<task type="auto">
  <name>Task 4: BullMQ queue stub + Redis publisher</name>
  <files>apps/api/src/queues/notifications.queue.ts, apps/api/src/lib/redis-pub.ts</files>
  <read_first>
    - apps/api/src/lib/redis.ts (ou onde quer que a conexão Redis esteja — verificar nome do export)
    - apps/api/src/modules/friendships/friendship.routes.ts (04-01 — onde fazer o enqueue ao aceitar)
    - .planning/phases/04-social-graph/04-CONTEXT.md §D-07 (SSE), §D-08 (BullMQ stubs)
  </read_first>
  <action>
Criar `apps/api/src/queues/notifications.queue.ts`:
```typescript
import { Queue } from 'bullmq';
import { redis } from '../lib/redis';

export const notificationsQueue = new Queue('notifications', {
  connection: redis,
  defaultJobOptions: {
    attempts: 3,
    backoff: { type: 'exponential', delay: 2000 },
    removeOnComplete: { count: 100 },
    removeOnFail: { count: 50 },
  },
});

export interface FriendshipAcceptedJob {
  type: 'friendship_accepted';
  recipientId: string;   // quem enviou o pedido original (será notificado)
  actorId: string;       // quem aceitou o pedido
  friendshipId: string;
}

// Fase 6 adicionará mais tipos de jobs:
// - birthday_reminder
// - wall_message_received
```

Criar `apps/api/src/lib/redis-pub.ts` (conexão separada para publisher — ioredis recomenda):
```typescript
import Redis from 'ioredis';

// Conexão separada para publish (não pode ser a mesma usada por BullMQ subscriber)
export const redisPub = new Redis(process.env.REDIS_URL ?? 'redis://localhost:6379', {
  maxRetriesPerRequest: null,
  lazyConnect: true,
  reconnectOnError: () => true,
});

export async function publishUserEvent(userId: string, event: object): Promise<void> {
  const channel = `user:${userId}:events`;
  const message = JSON.stringify(event);
  await redisPub.publish(channel, message);
}
```

**Atualizar** `apps/api/src/modules/friendships/friendship.routes.ts` — na rota POST /:id/accept, adicionar após `service.acceptRequest`:
```typescript
import { notificationsQueue, type FriendshipAcceptedJob } from '../../../queues/notifications.queue';
import { publishUserEvent } from '../../../lib/redis-pub';

// Dentro do handler de POST /v1/friendships/:id/accept:
const result = await service.acceptRequest(id, request.user.id);

// Enqueue BullMQ job para Fase 6 consumir
const job: FriendshipAcceptedJob = {
  type: 'friendship_accepted',
  recipientId: result.requesterId,   // quem enviou o pedido original
  actorId: result.addresseeId,       // quem aceitou
  friendshipId: result.id,
};
await notificationsQueue.add('friendship_accepted', job);

// Publicar evento SSE para notificação in-app imediata
await publishUserEvent(result.requesterId, {
  type: 'friendship_accepted',
  actorId: result.addresseeId,
  friendshipId: result.id,
});

return reply.send(result);
```
  </action>
  <acceptance_criteria>
    - notifications.queue.ts exporta `notificationsQueue` criado com `new Queue('notifications'`
    - notifications.queue.ts exporta interface `FriendshipAcceptedJob`
    - redis-pub.ts exporta `publishUserEvent` função
    - friendship.routes.ts chama `notificationsQueue.add('friendship_accepted', ...)` ao aceitar
    - friendship.routes.ts chama `publishUserEvent` ao aceitar para SSE
  </acceptance_criteria>
  <done>BullMQ queue configurada e Redis publisher integrado ao accept de amizade.</done>
</task>

<task type="auto">
  <name>Task 5: SSE endpoint para notificações in-app</name>
  <files>apps/api/src/modules/notifications/sse.routes.ts, apps/api/src/modules/notifications/sse.plugin.ts</files>
  <read_first>
    - apps/api/src/lib/redis-pub.ts (recém criado — padrão de conexão Redis)
    - apps/api/src/app.ts (como registrar plugins)
    - .planning/phases/04-social-graph/04-CONTEXT.md §D-07 (SSE spec: heartbeat 30s, timeout 5min)
    - .planning/phases/04-social-graph/RESEARCH.md §5 (SSE vs WebSocket decisão)
  </read_first>
  <action>
Criar `apps/api/src/modules/notifications/sse.routes.ts`:
```typescript
import type { FastifyInstance } from 'fastify';
import Redis from 'ioredis';

export async function sseRoutes(fastify: FastifyInstance) {

  // GET /v1/notifications/stream — SSE para notificações in-app
  fastify.get('/v1/notifications/stream', {
    onRequest: [fastify.authenticate],
  }, async (request, reply) => {
    const userId = request.user.id;

    // Configurar headers SSE
    reply.raw.setHeader('Content-Type', 'text/event-stream');
    reply.raw.setHeader('Cache-Control', 'no-cache');
    reply.raw.setHeader('Connection', 'keep-alive');
    reply.raw.setHeader('X-Accel-Buffering', 'no'); // para Nginx
    reply.raw.flushHeaders();

    // Conexão Redis subscriber dedicada para este cliente
    const subscriber = new Redis(process.env.REDIS_URL ?? 'redis://localhost:6379', {
      maxRetriesPerRequest: null,
      lazyConnect: true,
    });
    await subscriber.connect();

    const channel = `user:${userId}:events`;

    // Handler de mensagem
    const onMessage = (chan: string, message: string) => {
      if (chan === channel) {
        reply.raw.write(`data: ${message}\n\n`);
      }
    };
    subscriber.on('message', onMessage);
    await subscriber.subscribe(channel);

    // Heartbeat a cada 30 segundos
    const heartbeat = setInterval(() => {
      try {
        reply.raw.write(': heartbeat\n\n');
      } catch {
        // Conexão fechada
      }
    }, 30_000);

    // Timeout após 5 minutos de inatividade (cliente deve reconectar)
    const timeout = setTimeout(() => {
      reply.raw.end();
    }, 5 * 60 * 1000);

    // Cleanup ao fechar conexão
    const cleanup = async () => {
      clearInterval(heartbeat);
      clearTimeout(timeout);
      subscriber.removeListener('message', onMessage);
      await subscriber.unsubscribe(channel);
      await subscriber.quit().catch(() => {});
    };

    request.socket.on('close', cleanup);
    request.socket.on('error', cleanup);

    // Manter a requisição aberta (não retornar)
    await new Promise<void>((resolve) => {
      request.socket.on('close', resolve);
    });
  });
}
```

Criar `apps/api/src/modules/notifications/sse.plugin.ts`:
```typescript
import fp from 'fastify-plugin';
import { sseRoutes } from './sse.routes';

export const ssePlugin = fp(sseRoutes, {
  name: 'sse',
  dependencies: ['authenticate'],
});
```

Registrar em `apps/api/src/app.ts`:
```typescript
import { ssePlugin } from './modules/notifications/sse.plugin';
// ...
fastify.register(ssePlugin);
```
  </action>
  <acceptance_criteria>
    - sse.routes.ts define `GET /v1/notifications/stream` com `onRequest: [fastify.authenticate]`
    - Headers SSE: `Content-Type: text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`
    - Heartbeat a cada 30_000ms com `': heartbeat\n\n'`
    - Timeout de 5 minutos com `setTimeout(() => reply.raw.end(), 5 * 60 * 1000)`
    - Cleanup remove listener, unsubscribe e fecha conexão Redis ao fechar socket
    - sse.plugin.ts usa `fp()` com `dependencies: ['authenticate']`
  </acceptance_criteria>
  <done>SSE endpoint com Redis Pub/Sub subscriber, heartbeat e cleanup de conexão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → SSE | JWT obrigatório antes de abrir conexão SSE |
| Redis channel | Canal namespaced por userId: `user:{userId}:events` — usuário só recebe seus próprios eventos |
| BullMQ | Jobs enfileirados internamente — nunca por input direto do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | SSE channel | mitigate | Canal Redis scoped por userId autenticado — impossível subscrever canal de outro usuário |
| T-04-07 | Denial of Service | SSE connections | mitigate | Timeout de 5 minutos por conexão; heartbeat detecta clientes mortos |
| T-04-08 | Information Disclosure | Birthday feed | mitigate | birthYear omitido quando birthYearHidden=true; regra da Fase 3 (PROF-09) respeitada |
| T-04-09 | Denial of Service | Redis subscriber leak | mitigate | Cleanup em `socket.on('close')` e `socket.on('error')` garante unsubscribe |
</threat_model>

<verification>
Do repo root:
1. `grep -r "text/event-stream" apps/api/src/` — deve encontrar em sse.routes.ts
2. `grep -r "heartbeat" apps/api/src/modules/notifications/sse.routes.ts` — deve encontrar
3. `grep -r "notificationsQueue.add" apps/api/src/modules/friendships/` — deve encontrar ao aceitar pedido
4. `grep -r "publishUserEvent" apps/api/src/modules/friendships/` — deve encontrar ao aceitar pedido
5. `grep -r "MAKE_DATE" apps/api/src/modules/feed/feed.repository.ts` — deve encontrar
6. `cd apps/api && pnpm tsc --noEmit` — sem erros de tipo
</verification>

<success_criteria>
- Feed retorna `{ today: [], upcoming: [] }` com aniversários ordenados por proximidade
- Feed timezone-aware: usuário com timezone America/Sao_Paulo vê aniversários do seu "hoje"
- 29 de fevereiro tratado graciosamente (sem crash)
- SSE abre conexão, emite heartbeats e fecha após 5 minutos
- BullMQ job enfileirado ao aceitar amizade
</success_criteria>

<output>Após conclusão, criar `.planning/phases/04-social-graph/04-02-SUMMARY.md`</output>
