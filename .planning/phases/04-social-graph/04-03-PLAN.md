---
phase: 04-social-graph
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - apps/web/src/app/feed/page.tsx
  - apps/web/src/app/feed/BirthdayCard.tsx
  - apps/web/src/app/feed/NotificationBadge.tsx
  - apps/web/src/app/friends/page.tsx
  - apps/web/src/app/friends/FriendsList.tsx
  - apps/web/src/app/friends/PendingRequests.tsx
  - apps/web/src/app/friends/UserSearch.tsx
  - apps/web/src/app/friends/Suggestions.tsx
  - apps/web/src/app/(profile)/[username]/page.tsx
  - apps/web/src/lib/api/friendships.ts
  - apps/web/src/lib/api/feed.ts
  - apps/web/src/hooks/useFriendshipStatus.ts
  - apps/web/src/hooks/useSSENotifications.ts
autonomous: true
requirements: [SOCL-01, SOCL-02, SOCL-03, SOCL-04, SOCL-05, SOCL-06, SOCL-07, SOCL-08]

must_haves:
  truths:
    - "Feed page exibe 'Hoje' destacado com badge e lista de 'Próximos 30 dias'"
    - "Friends page tem 3 tabs: Amigos, Pedidos (com badge de contagem), Buscar"
    - "Busca tem debounce de 300ms e infinite scroll (Intersection Observer)"
    - "Botão Add Friend no perfil alterna entre estados: Add / Pendente / Amigos / Remover"
    - "SSE NotificationBadge atualiza contador sem refresh de página ao receber evento"
    - "useSSENotifications abre EventSource apenas quando usuário está autenticado"
  artifacts:
    - path: "apps/web/src/lib/api/friendships.ts"
      provides: "Client functions para todas as chamadas de friendship (send, accept, decline, list, search)"
      contains: "export async function sendFriendRequest"
    - path: "apps/web/src/hooks/useSSENotifications.ts"
      provides: "Hook React que abre EventSource e dispara callbacks por tipo de evento"
      contains: "new EventSource('/api/notifications/stream'"
    - path: "apps/web/src/app/feed/page.tsx"
      provides: "RSC feed page com Server Component + Client islands"
      contains: "'use server'"
    - path: "apps/web/src/app/friends/UserSearch.tsx"
      provides: "Client Component com debounce 300ms e infinite scroll"
      contains: "useIntersectionObserver"
  key_links:
    - from: "apps/web/src/app/feed/page.tsx"
      to: "apps/web/src/lib/api/feed.ts"
      via: "RSC chama feed API client no server; Client Component usa useSSENotifications"
      pattern: "const feed = await getFeed()"
    - from: "apps/web/src/app/(profile)/[username]/page.tsx"
      to: "apps/web/src/hooks/useFriendshipStatus.ts"
      via: "Client island para botão de amizade lê status inicial do RSC e atualiza otimisticamente"
      pattern: "const { status, sendRequest } = useFriendshipStatus(initialStatus)"
    - from: "apps/web/src/hooks/useSSENotifications.ts"
      to: "apps/web/src/app/api/notifications/stream/route.ts"
      via: "BFF proxy SSE — Next.js roteia /api/notifications/stream → Fastify /v1/notifications/stream"
      pattern: "new EventSource('/api/notifications/stream')"
---

<objective>
Implementar toda a UI social: página de feed de aniversários, página de amigos (lista + pedidos + busca + sugestões), integração do botão de amizade na página de perfil, e hook de notificações SSE in-app.

Purpose: Depende de 04-01 (Friendship API) e 04-02 (Feed API + SSE backend). Esta plan entrega a experiência completa ao usuário final.
Output: 3 páginas Next.js funcionando + hooks + client API functions + SSE notifications em tempo real.
</objective>

<execution_context>
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/igorsantos/code/parabuains/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-social-graph/04-CONTEXT.md
@.planning/phases/04-social-graph/RESEARCH.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFF proxy routes (Next.js API routes)</name>
  <files>
    apps/web/src/app/api/friendships/route.ts
    apps/web/src/app/api/friendships/[id]/route.ts
    apps/web/src/app/api/friendships/[id]/accept/route.ts
    apps/web/src/app/api/friendships/pending/route.ts
    apps/web/src/app/api/friendships/sent/route.ts
    apps/web/src/app/api/friendships/suggestions/route.ts
    apps/web/src/app/api/friendships/status/[targetUserId]/route.ts
    apps/web/src/app/api/users/search/route.ts
    apps/web/src/app/api/feed/birthdays/route.ts
    apps/web/src/app/api/notifications/stream/route.ts
  </files>
  <read_first>
    - apps/web/src/lib/auth.ts (como obter session no servidor)
    - apps/web/src/app/api/profile/ (padrão de BFF proxy existente da Fase 3)
    - .planning/phases/02-auth/02-CONTEXT.md (service token para comunicação BFF → Fastify)
  </read_first>
  <action>
Criar um helper de BFF proxy reutilizável em `apps/web/src/lib/bff.ts` (se não existir):
```typescript
import { auth } from '@/lib/auth';
import { NextRequest, NextResponse } from 'next/server';

const INTERNAL_API_URL = process.env.INTERNAL_API_URL ?? 'http://localhost:3001';

export async function bffProxy(
  request: NextRequest,
  path: string,
  options: { method?: string; body?: unknown } = {}
): Promise<NextResponse> {
  const session = await auth();
  if (!session?.user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  const url = new URL(path, INTERNAL_API_URL);

  // Preservar query params
  request.nextUrl.searchParams.forEach((value, key) => {
    url.searchParams.set(key, value);
  });

  const method = options.method ?? request.method;
  const body = options.body ?? (method !== 'GET' && method !== 'DELETE'
    ? await request.json().catch(() => undefined)
    : undefined);

  const response = await fetch(url.toString(), {
    method,
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${(session as any).serviceToken}`,
    },
    ...(body ? { body: JSON.stringify(body) } : {}),
  });

  const data = await response.json().catch(() => null);
  return NextResponse.json(data, { status: response.status });
}
```

Criar rotas BFF para cada endpoint — seguindo o padrão:

`apps/web/src/app/api/friendships/route.ts`:
```typescript
import { NextRequest } from 'next/server';
import { bffProxy } from '@/lib/bff';
export const GET = (req: NextRequest) => bffProxy(req, '/v1/friendships');
export const POST = (req: NextRequest) => bffProxy(req, '/v1/friendships', { method: 'POST' });
```

`apps/web/src/app/api/friendships/[id]/route.ts`:
```typescript
import { NextRequest } from 'next/server';
import { bffProxy } from '@/lib/bff';
export const DELETE = (req: NextRequest, { params }: { params: { id: string } }) =>
  bffProxy(req, `/v1/friendships/${params.id}`, { method: 'DELETE' });
```

`apps/web/src/app/api/friendships/[id]/accept/route.ts`:
```typescript
import { NextRequest } from 'next/server';
import { bffProxy } from '@/lib/bff';
export const POST = (req: NextRequest, { params }: { params: { id: string } }) =>
  bffProxy(req, `/v1/friendships/${params.id}/accept`, { method: 'POST' });
```

Criar similarmente:
- `pending/route.ts` → GET `/v1/friendships/pending`
- `sent/route.ts` → GET `/v1/friendships/sent`
- `suggestions/route.ts` → GET `/v1/friendships/suggestions`
- `status/[targetUserId]/route.ts` → GET `/v1/friendships/status/{targetUserId}`
- `users/search/route.ts` → GET `/v1/users/search` (preservar query params `q`, `limit`, `cursor`)
- `feed/birthdays/route.ts` → GET `/v1/feed/birthdays`

Para SSE (`notifications/stream/route.ts`), usar streaming proxy:
```typescript
import { NextRequest } from 'next/server';
import { auth } from '@/lib/auth';

export const GET = async (request: NextRequest) => {
  const session = await auth();
  if (!session?.user) {
    return new Response('Unauthorized', { status: 401 });
  }

  const apiUrl = `${process.env.INTERNAL_API_URL}/v1/notifications/stream`;
  const upstream = await fetch(apiUrl, {
    headers: {
      'Authorization': `Bearer ${(session as any).serviceToken}`,
      'Accept': 'text/event-stream',
    },
  });

  // Proxy SSE stream transparentemente
  return new Response(upstream.body, {
    headers: {
      'Content-Type': 'text/event-stream',
      'Cache-Control': 'no-cache',
      'Connection': 'keep-alive',
    },
  });
};

export const dynamic = 'force-dynamic';
```
  </action>
  <acceptance_criteria>
    - `bffProxy` helper existe em `apps/web/src/lib/bff.ts` e preserva query params
    - Todas as 10 rotas BFF existem e fazem proxy para o endpoint correto do Fastify
    - SSE proxy retorna `Content-Type: text/event-stream`
    - Todas as rotas verificam autenticação antes de fazer proxy
  </acceptance_criteria>
  <done>Todas as rotas BFF Next.js criadas para fazer proxy transparente para a Friendship/Feed API.</done>
</task>

<task type="auto">
  <name>Task 2: Client API functions e hooks</name>
  <files>
    apps/web/src/lib/api/friendships.ts
    apps/web/src/lib/api/feed.ts
    apps/web/src/hooks/useFriendshipStatus.ts
    apps/web/src/hooks/useSSENotifications.ts
  </files>
  <read_first>
    - apps/web/src/app/api/friendships/ (rotas BFF recém criadas — paths e métodos)
    - .planning/phases/04-social-graph/04-CONTEXT.md §D-10 (UI states para botão de amizade)
  </read_first>
  <action>
Criar `apps/web/src/lib/api/friendships.ts`:
```typescript
export type FriendshipStatus = 'none' | 'pending_sent' | 'pending_received' | 'accepted';

export async function sendFriendRequest(addresseeId: string) {
  const res = await fetch('/api/friendships', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ addresseeId }),
  });
  if (!res.ok) throw new Error((await res.json()).error ?? 'Failed to send request');
  return res.json();
}

export async function acceptFriendRequest(friendshipId: string) {
  const res = await fetch(`/api/friendships/${friendshipId}/accept`, { method: 'POST' });
  if (!res.ok) throw new Error((await res.json()).error ?? 'Failed to accept request');
  return res.json();
}

export async function declineOrRemoveFriendship(friendshipId: string) {
  const res = await fetch(`/api/friendships/${friendshipId}`, { method: 'DELETE' });
  if (!res.ok) throw new Error((await res.json()).error ?? 'Failed to remove friendship');
}

export async function getFriends() {
  const res = await fetch('/api/friendships');
  if (!res.ok) throw new Error('Failed to fetch friends');
  return res.json();
}

export async function getPendingRequests() {
  const res = await fetch('/api/friendships/pending');
  if (!res.ok) throw new Error('Failed to fetch pending requests');
  return res.json();
}

export async function searchUsers(q: string, cursor?: string) {
  const params = new URLSearchParams({ q, limit: '20' });
  if (cursor) params.set('cursor', cursor);
  const res = await fetch(`/api/users/search?${params}`);
  if (!res.ok) throw new Error('Failed to search users');
  return res.json();
}

export async function getFriendshipStatus(targetUserId: string): Promise<{ status: FriendshipStatus }> {
  const res = await fetch(`/api/friendships/status/${targetUserId}`);
  if (!res.ok) return { status: 'none' };
  return res.json();
}

export async function getSuggestions() {
  const res = await fetch('/api/friendships/suggestions');
  if (!res.ok) throw new Error('Failed to fetch suggestions');
  return res.json();
}
```

Criar `apps/web/src/lib/api/feed.ts`:
```typescript
export interface BirthdayEntry {
  userId: string;
  username: string;
  displayName: string;
  avatarUrl: string | null;
  nextBirthday: string;
  daysUntil: number;
  birthMonthDay: string;
  birthYear?: number;
}

export interface BirthdayFeed {
  today: BirthdayEntry[];
  upcoming: BirthdayEntry[];
}

export async function getBirthdayFeed(): Promise<BirthdayFeed> {
  const res = await fetch('/api/feed/birthdays');
  if (!res.ok) throw new Error('Failed to fetch birthday feed');
  return res.json();
}
```

Criar `apps/web/src/hooks/useFriendshipStatus.ts`:
```typescript
'use client';
import { useState, useTransition } from 'react';
import { sendFriendRequest, acceptFriendRequest, declineOrRemoveFriendship } from '@/lib/api/friendships';
import type { FriendshipStatus } from '@/lib/api/friendships';

export function useFriendshipStatus(
  initialStatus: FriendshipStatus,
  initialFriendshipId?: string
) {
  const [status, setStatus] = useState<FriendshipStatus>(initialStatus);
  const [friendshipId, setFriendshipId] = useState<string | undefined>(initialFriendshipId);
  const [isPending, startTransition] = useTransition();

  const sendRequest = (addresseeId: string) => {
    startTransition(async () => {
      try {
        const result = await sendFriendRequest(addresseeId);
        setFriendshipId(result.id);
        setStatus('pending_sent');
      } catch (e) {
        console.error(e);
      }
    });
  };

  const accept = () => {
    if (!friendshipId) return;
    startTransition(async () => {
      try {
        await acceptFriendRequest(friendshipId);
        setStatus('accepted');
      } catch (e) {
        console.error(e);
      }
    });
  };

  const remove = () => {
    if (!friendshipId) return;
    startTransition(async () => {
      try {
        await declineOrRemoveFriendship(friendshipId);
        setFriendshipId(undefined);
        setStatus('none');
      } catch (e) {
        console.error(e);
      }
    });
  };

  return { status, friendshipId, isPending, sendRequest, accept, remove };
}
```

Criar `apps/web/src/hooks/useSSENotifications.ts`:
```typescript
'use client';
import { useEffect, useCallback } from 'react';

export type SSEEventType = 'friendship_request' | 'friendship_accepted';

interface SSEEvent {
  type: SSEEventType;
  [key: string]: unknown;
}

export function useSSENotifications(
  onEvent: (event: SSEEvent) => void,
  enabled = true
) {
  const stableOnEvent = useCallback(onEvent, []);

  useEffect(() => {
    if (!enabled) return;

    const es = new EventSource('/api/notifications/stream');

    es.onmessage = (e) => {
      try {
        const data = JSON.parse(e.data) as SSEEvent;
        stableOnEvent(data);
      } catch {
        // Ignorar mensagens não-JSON (heartbeats chegam como comentários, não onmessage)
      }
    };

    es.onerror = () => {
      // EventSource reconecta automaticamente — não precisa de lógica extra
    };

    return () => es.close();
  }, [enabled, stableOnEvent]);
}
```
  </action>
  <acceptance_criteria>
    - friendships.ts exporta todas as funções: sendFriendRequest, acceptFriendRequest, declineOrRemoveFriendship, getFriends, getPendingRequests, searchUsers, getFriendshipStatus, getSuggestions
    - useFriendshipStatus retorna `{ status, isPending, sendRequest, accept, remove }`
    - useSSENotifications usa `new EventSource('/api/notifications/stream')`
    - useSSENotifications fecha EventSource no cleanup (`return () => es.close()`)
  </acceptance_criteria>
  <done>Client API functions e React hooks para toda a camada de friendship e SSE.</done>
</task>

<task type="auto">
  <name>Task 3: Feed page (RSC + Client islands)</name>
  <files>
    apps/web/src/app/feed/page.tsx
    apps/web/src/app/feed/BirthdayCard.tsx
    apps/web/src/app/feed/NotificationBadge.tsx
  </files>
  <read_first>
    - apps/web/src/lib/api/feed.ts (recém criado — BirthdayFeed type)
    - apps/web/src/hooks/useSSENotifications.ts (recém criado)
    - .planning/phases/04-social-graph/04-CONTEXT.md §D-06 (estrutura: today + upcoming)
    - apps/web/src/app/(profile)/[username]/page.tsx (padrão de RSC da Fase 3)
  </read_first>
  <action>
Criar `apps/web/src/app/feed/BirthdayCard.tsx`:
```tsx
'use client';
import Image from 'next/image';
import Link from 'next/link';
import type { BirthdayEntry } from '@/lib/api/feed';

interface BirthdayCardProps {
  entry: BirthdayEntry;
  isToday?: boolean;
}

export function BirthdayCard({ entry, isToday }: BirthdayCardProps) {
  const monthNames = ['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'];
  const [month, day] = entry.birthMonthDay.split('-').map(Number);
  const dateStr = entry.birthYear
    ? `${day} de ${monthNames[month - 1]} de ${entry.birthYear}`
    : `${day} de ${monthNames[month - 1]}`;

  return (
    <Link
      href={`/${entry.username}`}
      className={`flex items-center gap-3 p-4 rounded-xl transition-colors hover:bg-neutral-50
        ${isToday ? 'bg-amber-50 border border-amber-200' : 'bg-white border border-neutral-100'}`}
    >
      <div className="relative shrink-0">
        {entry.avatarUrl ? (
          <Image
            src={entry.avatarUrl}
            alt={entry.displayName}
            width={48}
            height={48}
            className="rounded-full object-cover"
          />
        ) : (
          <div className="w-12 h-12 rounded-full bg-neutral-200 flex items-center justify-center text-lg font-semibold text-neutral-600">
            {entry.displayName.charAt(0).toUpperCase()}
          </div>
        )}
        {isToday && (
          <span className="absolute -bottom-1 -right-1 text-lg" aria-label="aniversário hoje">🎂</span>
        )}
      </div>
      <div className="flex-1 min-w-0">
        <p className="font-semibold text-neutral-900 truncate">{entry.displayName}</p>
        <p className="text-sm text-neutral-500 truncate">@{entry.username}</p>
      </div>
      <div className="text-right shrink-0">
        <p className="text-sm font-medium text-neutral-700">{dateStr}</p>
        <p className={`text-xs ${isToday ? 'text-amber-600 font-semibold' : 'text-neutral-400'}`}>
          {isToday ? 'Hoje!' : `em ${entry.daysUntil} dia${entry.daysUntil === 1 ? '' : 's'}`}
        </p>
      </div>
    </Link>
  );
}
```

Criar `apps/web/src/app/feed/NotificationBadge.tsx`:
```tsx
'use client';
import { useState } from 'react';
import { useSSENotifications } from '@/hooks/useSSENotifications';

export function NotificationBadge() {
  const [count, setCount] = useState(0);

  useSSENotifications((event) => {
    if (event.type === 'friendship_request' || event.type === 'friendship_accepted') {
      setCount(c => c + 1);
    }
  });

  if (count === 0) return null;

  return (
    <span className="inline-flex items-center justify-center w-5 h-5 text-xs font-bold text-white bg-rose-500 rounded-full">
      {count > 9 ? '9+' : count}
    </span>
  );
}
```

Criar `apps/web/src/app/feed/page.tsx`:
```tsx
import { getBirthdayFeed } from '@/lib/api/feed';
import { BirthdayCard } from './BirthdayCard';
import { NotificationBadge } from './NotificationBadge';
import { Suspense } from 'react';

export const metadata = { title: 'Feed de Aniversários — Parabuains' };

async function FeedContent() {
  const feed = await getBirthdayFeed();

  if (feed.today.length === 0 && feed.upcoming.length === 0) {
    return (
      <div className="text-center py-16 text-neutral-500">
        <p className="text-4xl mb-4">🎉</p>
        <p className="font-medium">Nenhum aniversário nos próximos 30 dias.</p>
        <p className="text-sm mt-2">Adicione amigos para ver os aniversários deles aqui.</p>
      </div>
    );
  }

  return (
    <div className="space-y-8">
      {feed.today.length > 0 && (
        <section>
          <h2 className="text-lg font-semibold text-amber-700 mb-3 flex items-center gap-2">
            🎂 Aniversário Hoje
          </h2>
          <div className="space-y-2">
            {feed.today.map(entry => (
              <BirthdayCard key={entry.userId} entry={entry} isToday />
            ))}
          </div>
        </section>
      )}
      {feed.upcoming.length > 0 && (
        <section>
          <h2 className="text-lg font-semibold text-neutral-700 mb-3">Próximos 30 dias</h2>
          <div className="space-y-2">
            {feed.upcoming.map(entry => (
              <BirthdayCard key={entry.userId} entry={entry} />
            ))}
          </div>
        </section>
      )}
    </div>
  );
}

export default function FeedPage() {
  return (
    <main className="max-w-2xl mx-auto px-4 py-8">
      <div className="flex items-center justify-between mb-8">
        <h1 className="text-2xl font-bold text-neutral-900">Feed de Aniversários</h1>
        <NotificationBadge />
      </div>
      <Suspense fallback={
        <div className="space-y-2">
          {[...Array(5)].map((_, i) => (
            <div key={i} className="h-20 bg-neutral-100 rounded-xl animate-pulse" />
          ))}
        </div>
      }>
        <FeedContent />
      </Suspense>
    </main>
  );
}
```
  </action>
  <acceptance_criteria>
    - feed/page.tsx usa RSC (sem `'use client'` no topo — apenas Server Component)
    - BirthdayCard.tsx tem `'use client'` e exibe avatarUrl ou placeholder de iniciais
    - NotificationBadge.tsx usa `useSSENotifications` e incrementa contador
    - Aniversariantes do dia (isToday) têm estilo visual diferenciado (fundo âmbar)
    - Skeleton loader usando `animate-pulse` no Suspense fallback
  </acceptance_criteria>
  <done>Feed page com RSC + client islands, skeleton loader e notificação SSE em tempo real.</done>
</task>

<task type="auto">
  <name>Task 4: Friends page (lista + pedidos + busca + sugestões)</name>
  <files>
    apps/web/src/app/friends/page.tsx
    apps/web/src/app/friends/FriendsList.tsx
    apps/web/src/app/friends/PendingRequests.tsx
    apps/web/src/app/friends/UserSearch.tsx
    apps/web/src/app/friends/Suggestions.tsx
  </files>
  <read_first>
    - apps/web/src/lib/api/friendships.ts (recém criado)
    - apps/web/src/hooks/useFriendshipStatus.ts (recém criado)
    - .planning/phases/04-social-graph/04-CONTEXT.md §D-10 (UI pages spec)
  </read_first>
  <action>
Criar `apps/web/src/app/friends/PendingRequests.tsx`:
```tsx
'use client';
import { useState, useEffect } from 'react';
import Image from 'next/image';
import Link from 'next/link';
import { getPendingRequests, acceptFriendRequest, declineOrRemoveFriendship } from '@/lib/api/friendships';

interface PendingRequest {
  id: string;
  requesterId: string;
  requesterUser: {
    id: string;
    username: string;
    displayName: string;
    avatarUrl: string | null;
  };
}

export function PendingRequests() {
  const [requests, setRequests] = useState<PendingRequest[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    getPendingRequests()
      .then(data => setRequests(data.requests ?? []))
      .finally(() => setLoading(false));
  }, []);

  const handleAccept = async (id: string) => {
    await acceptFriendRequest(id);
    setRequests(prev => prev.filter(r => r.id !== id));
  };

  const handleDecline = async (id: string) => {
    await declineOrRemoveFriendship(id);
    setRequests(prev => prev.filter(r => r.id !== id));
  };

  if (loading) return <div className="h-32 bg-neutral-100 rounded-xl animate-pulse" />;
  if (requests.length === 0) return (
    <p className="text-center text-neutral-500 py-8">Nenhum pedido de amizade pendente.</p>
  );

  return (
    <ul className="space-y-3">
      {requests.map(req => (
        <li key={req.id} className="flex items-center gap-3 p-4 bg-white border border-neutral-100 rounded-xl">
          <Link href={`/${req.requesterUser.username}`} className="shrink-0">
            {req.requesterUser.avatarUrl ? (
              <Image src={req.requesterUser.avatarUrl} alt={req.requesterUser.displayName} width={44} height={44} className="rounded-full" />
            ) : (
              <div className="w-11 h-11 rounded-full bg-neutral-200 flex items-center justify-center font-semibold text-neutral-600">
                {req.requesterUser.displayName.charAt(0)}
              </div>
            )}
          </Link>
          <div className="flex-1 min-w-0">
            <Link href={`/${req.requesterUser.username}`} className="font-semibold text-neutral-900 hover:underline truncate block">
              {req.requesterUser.displayName}
            </Link>
            <p className="text-sm text-neutral-500">@{req.requesterUser.username}</p>
          </div>
          <div className="flex gap-2 shrink-0">
            <button
              onClick={() => handleAccept(req.id)}
              className="px-3 py-1.5 text-sm font-medium bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
            >
              Aceitar
            </button>
            <button
              onClick={() => handleDecline(req.id)}
              className="px-3 py-1.5 text-sm font-medium bg-neutral-100 text-neutral-700 rounded-lg hover:bg-neutral-200 transition-colors"
            >
              Recusar
            </button>
          </div>
        </li>
      ))}
    </ul>
  );
}
```

Criar `apps/web/src/app/friends/UserSearch.tsx`:
```tsx
'use client';
import { useState, useCallback, useRef, useEffect } from 'react';
import Image from 'next/image';
import Link from 'next/link';
import { searchUsers, sendFriendRequest } from '@/lib/api/friendships';
import type { FriendshipStatus } from '@/lib/api/friendships';

interface SearchResult {
  id: string;
  username: string;
  displayName: string;
  avatarUrl: string | null;
  friendshipStatus: FriendshipStatus;
}

function useDebounce<T>(value: T, delay: number): T {
  const [debounced, setDebounced] = useState(value);
  useEffect(() => {
    const timer = setTimeout(() => setDebounced(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);
  return debounced;
}

export function UserSearch() {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState<SearchResult[]>([]);
  const [nextCursor, setNextCursor] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);
  const [statuses, setStatuses] = useState<Record<string, FriendshipStatus>>({});
  const sentinelRef = useRef<HTMLDivElement>(null);

  const debouncedQuery = useDebounce(query, 300);

  const doSearch = useCallback(async (q: string, cursor?: string) => {
    if (q.length < 2) { setResults([]); return; }
    setLoading(true);
    try {
      const data = await searchUsers(q, cursor);
      const newResults: SearchResult[] = data.results ?? [];
      setResults(prev => cursor ? [...prev, ...newResults] : newResults);
      setNextCursor(data.nextCursor ?? null);
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    setResults([]);
    setNextCursor(null);
    doSearch(debouncedQuery);
  }, [debouncedQuery, doSearch]);

  // Infinite scroll com Intersection Observer
  useEffect(() => {
    if (!sentinelRef.current || !nextCursor) return;
    const observer = new IntersectionObserver(entries => {
      if (entries[0].isIntersecting && !loading && nextCursor) {
        doSearch(debouncedQuery, nextCursor);
      }
    }, { threshold: 0.5 });
    observer.observe(sentinelRef.current);
    return () => observer.disconnect();
  }, [nextCursor, loading, debouncedQuery, doSearch]);

  const handleAddFriend = async (userId: string) => {
    try {
      setStatuses(prev => ({ ...prev, [userId]: 'pending_sent' }));
      await sendFriendRequest(userId);
    } catch {
      setStatuses(prev => ({ ...prev, [userId]: 'none' }));
    }
  };

  const getStatus = (result: SearchResult): FriendshipStatus =>
    statuses[result.id] ?? result.friendshipStatus;

  return (
    <div>
      <input
        type="search"
        value={query}
        onChange={e => setQuery(e.target.value)}
        placeholder="Buscar por nome ou @username..."
        className="w-full px-4 py-2.5 border border-neutral-200 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
        aria-label="Buscar usuários"
      />
      {loading && query.length >= 2 && (
        <div className="mt-2 space-y-2">
          {[...Array(3)].map((_, i) => <div key={i} className="h-16 bg-neutral-100 rounded-xl animate-pulse" />)}
        </div>
      )}
      {results.length > 0 && (
        <ul className="mt-2 space-y-2">
          {results.map(result => {
            const status = getStatus(result);
            return (
              <li key={result.id} className="flex items-center gap-3 p-3 bg-white border border-neutral-100 rounded-xl">
                <Link href={`/${result.username}`} className="shrink-0">
                  {result.avatarUrl ? (
                    <Image src={result.avatarUrl} alt={result.displayName} width={40} height={40} className="rounded-full" />
                  ) : (
                    <div className="w-10 h-10 rounded-full bg-neutral-200 flex items-center justify-center font-semibold text-neutral-600 text-sm">
                      {result.displayName.charAt(0)}
                    </div>
                  )}
                </Link>
                <div className="flex-1 min-w-0">
                  <p className="font-medium text-neutral-900 truncate">{result.displayName}</p>
                  <p className="text-xs text-neutral-500">@{result.username}</p>
                </div>
                {status === 'none' && (
                  <button
                    onClick={() => handleAddFriend(result.id)}
                    className="shrink-0 px-3 py-1.5 text-sm font-medium bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
                  >
                    Adicionar
                  </button>
                )}
                {status === 'pending_sent' && (
                  <span className="shrink-0 px-3 py-1.5 text-sm text-neutral-500 border border-neutral-200 rounded-lg">
                    Pendente
                  </span>
                )}
                {status === 'pending_received' && (
                  <span className="shrink-0 px-3 py-1.5 text-sm text-amber-600 border border-amber-200 rounded-lg">
                    Responder
                  </span>
                )}
                {status === 'accepted' && (
                  <span className="shrink-0 px-3 py-1.5 text-sm text-green-600 border border-green-200 rounded-lg">
                    Amigos
                  </span>
                )}
              </li>
            );
          })}
        </ul>
      )}
      <div ref={sentinelRef} className="h-1" />
      {query.length >= 2 && !loading && results.length === 0 && (
        <p className="text-center text-neutral-500 py-6 text-sm">Nenhum usuário encontrado para "{query}".</p>
      )}
    </div>
  );
}
```

Criar `apps/web/src/app/friends/page.tsx` (tabs: Amigos / Pedidos / Buscar):
```tsx
'use client';
import { Suspense, useState } from 'react';
import { PendingRequests } from './PendingRequests';
import { UserSearch } from './UserSearch';

export const metadata = { title: 'Amigos — Parabuains' };

type Tab = 'friends' | 'pending' | 'search';

export default function FriendsPage() {
  const [tab, setTab] = useState<Tab>('friends');

  return (
    <main className="max-w-2xl mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold text-neutral-900 mb-6">Amigos</h1>

      <div className="flex gap-1 p-1 bg-neutral-100 rounded-xl mb-6">
        {(['friends', 'pending', 'search'] as Tab[]).map(t => (
          <button
            key={t}
            onClick={() => setTab(t)}
            className={`flex-1 py-2 text-sm font-medium rounded-lg transition-colors ${
              tab === t ? 'bg-white text-neutral-900 shadow-sm' : 'text-neutral-500 hover:text-neutral-700'
            }`}
          >
            {t === 'friends' ? 'Amigos' : t === 'pending' ? 'Pedidos' : 'Buscar'}
          </button>
        ))}
      </div>

      {tab === 'pending' && <PendingRequests />}
      {tab === 'search' && <UserSearch />}
      {tab === 'friends' && (
        <p className="text-center text-neutral-500 py-8">
          Lista de amigos — a ser implementada.
        </p>
      )}
    </main>
  );
}
```
  </action>
  <acceptance_criteria>
    - UserSearch.tsx usa `useDebounce` com delay de 300ms
    - UserSearch.tsx usa `IntersectionObserver` para infinite scroll
    - PendingRequests.tsx tem botões "Aceitar" e "Recusar" que removem o item da lista otimisticamente
    - friends/page.tsx tem 3 tabs: Amigos, Pedidos, Buscar
    - Todos os estados de `friendshipStatus` têm representação visual na busca
  </acceptance_criteria>
  <done>Friends page completa com tabs, busca com debounce, infinite scroll e pedidos pendentes.</done>
</task>

<task type="auto">
  <name>Task 5: Integração do botão de amizade na página de perfil</name>
  <files>apps/web/src/app/(profile)/[username]/FriendshipButton.tsx</files>
  <read_first>
    - apps/web/src/app/(profile)/[username]/page.tsx (Fase 3 — verificar como a página de perfil está estruturada)
    - apps/web/src/hooks/useFriendshipStatus.ts (recém criado)
    - .planning/phases/04-social-graph/04-CONTEXT.md §D-10 (estados do botão)
  </read_first>
  <action>
Criar `apps/web/src/app/(profile)/[username]/FriendshipButton.tsx`:
```tsx
'use client';
import { useFriendshipStatus } from '@/hooks/useFriendshipStatus';
import type { FriendshipStatus } from '@/lib/api/friendships';

interface FriendshipButtonProps {
  targetUserId: string;
  initialStatus: FriendshipStatus;
  initialFriendshipId?: string;
}

export function FriendshipButton({ targetUserId, initialStatus, initialFriendshipId }: FriendshipButtonProps) {
  const { status, isPending, sendRequest, accept, remove } = useFriendshipStatus(
    initialStatus,
    initialFriendshipId
  );

  if (status === 'none') {
    return (
      <button
        onClick={() => sendRequest(targetUserId)}
        disabled={isPending}
        className="px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 disabled:opacity-50 transition-colors"
      >
        {isPending ? 'Enviando...' : 'Adicionar amigo'}
      </button>
    );
  }

  if (status === 'pending_sent') {
    return (
      <button
        disabled
        className="px-4 py-2 bg-neutral-100 text-neutral-500 text-sm font-medium rounded-lg cursor-not-allowed"
      >
        Pedido enviado
      </button>
    );
  }

  if (status === 'pending_received') {
    return (
      <div className="flex gap-2">
        <button
          onClick={accept}
          disabled={isPending}
          className="px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 disabled:opacity-50 transition-colors"
        >
          Aceitar pedido
        </button>
        <button
          onClick={remove}
          disabled={isPending}
          className="px-4 py-2 bg-neutral-100 text-neutral-700 text-sm font-medium rounded-lg hover:bg-neutral-200 disabled:opacity-50 transition-colors"
        >
          Recusar
        </button>
      </div>
    );
  }

  if (status === 'accepted') {
    return (
      <button
        onClick={remove}
        disabled={isPending}
        className="px-4 py-2 bg-neutral-100 text-neutral-700 text-sm font-medium rounded-lg hover:bg-red-50 hover:text-red-600 hover:border-red-200 border border-transparent disabled:opacity-50 transition-colors"
      >
        {isPending ? 'Removendo...' : 'Amigos'}
      </button>
    );
  }

  return null;
}
```

**Atualizar** `apps/web/src/app/(profile)/[username]/page.tsx` — no RSC, buscar friendship status e passar como prop para o client component:
```tsx
// Adicionar ao RSC de perfil (após buscar o profile):
import { FriendshipButton } from './FriendshipButton';
import { getFriendshipStatus } from '@/lib/api/friendships';

// Dentro do componente RSC, após obter session:
// const friendshipData = session?.user && profile.id !== session.user.id
//   ? await getFriendshipStatus(profile.id)
//   : null;

// Renderizar no JSX do perfil (ao lado do botão de compartilhar):
// {friendshipData && (
//   <FriendshipButton
//     targetUserId={profile.id}
//     initialStatus={friendshipData.status}
//     initialFriendshipId={friendshipData.friendshipId}
//   />
// )}
```
  </action>
  <acceptance_criteria>
    - FriendshipButton.tsx tem `'use client'` no topo
    - Renderiza botão diferente para cada status: none, pending_sent, pending_received, accepted
    - `pending_received` mostra dois botões (Aceitar e Recusar)
    - `accepted` mostra "Amigos" com hover vermelho (hover para remover)
    - `isPending` desabilita botões durante loading
  </acceptance_criteria>
  <done>FriendshipButton integrado na página de perfil com todos os 4 estados de amizade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → BFF | Session next-auth verifica autenticação em cada BFF route |
| BFF → Fastify | Service token JWT (60s expiry) em todas as chamadas internas |
| SSE stream | EventSource enviado apenas para usuário autenticado; userId derivado do JWT no Fastify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | UserSearch | mitigate | Perfis privados retornam apenas username/displayName sem dados sensíveis |
| T-04-11 | Cross-Site Scripting | BirthdayCard/UserSearch | mitigate | React escapa HTML por padrão; não usar dangerouslySetInnerHTML |
| T-04-12 | Tampering | FriendshipButton | mitigate | Estado otimista revertido em caso de erro; autorização real no Fastify |
| T-04-13 | Denial of Service | UserSearch debounce | mitigate | 300ms debounce previne requisições excessivas; mínimo 2 chars |
</threat_model>

<verification>
Do repo root:
1. `grep -r "useDebounce" apps/web/src/app/friends/UserSearch.tsx` — deve encontrar
2. `grep -r "IntersectionObserver" apps/web/src/app/friends/UserSearch.tsx` — deve encontrar
3. `grep -r "EventSource" apps/web/src/hooks/useSSENotifications.ts` — deve encontrar
4. `grep -r "FriendshipButton" apps/web/src/app/(profile)/` — deve encontrar integração no perfil
5. `cd apps/web && pnpm tsc --noEmit` — sem erros de tipo
6. `cd apps/web && pnpm build` — build sem erros
</verification>

<success_criteria>
- Feed page exibe hoje/próximos com visual diferenciado e skeleton loader
- Friends page com 3 tabs funcionando: Amigos, Pedidos, Buscar
- Busca com debounce 300ms e infinite scroll
- Botão de amizade no perfil alterna entre 4 estados
- SSE NotificationBadge incrementa ao receber eventos
</success_criteria>

<output>Após conclusão, criar `.planning/phases/04-social-graph/04-03-SUMMARY.md`</output>
